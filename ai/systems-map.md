# Systems map

| System | Responsibility | Depends on | Status |
| --- | --- | --- | --- |
| Unity URP foundation | Rendering profiles, six build scenes, PC renderer settings | Unity 6000.5.5f1, URP 17.5.0 | Current |
| PS1 world composite | Footprint-average the final post-processed Game-camera image to `640x360` by default (lower `426x240`/`320x180` options), apply percentage-driven intoxication warp/ghost/chromatic/warmth/exposure/vignette parameters, blend 35% perceptual-space RGB555 without screen-space dithering and point-upscale it | `Runtime/Rendering`, `IntoxicationRenderState`, `Ps1PresentationProfile`, `Ps1Composite` material/shader, `PC_Renderer.asset` | Implemented |
| City composition | Exact six-scene bootstrap and runtime roots | `Runtime/Core`, scene IDs | Implemented |
| Startup waking opening | Start a fresh run behind a black build-index-0 boundary, pass one consumed `OpeningSleep` arrival into the existing Home, begin in the bed loop, hold the first Home shot on a silent `05:59` clock whose complete display flickers briefly at three-second intervals across five input-locked seconds, then reveal localized PS1 Wake Up/Quit choices while retaining that silent flickering `05:59` shot; only Wake switches to solid `06:00`, hides the menu and starts a three-second unscaled alarm hold, after which the ring stops and the camera plus six-second `3x` opening-only wake begin, continuously settling into the active gameplay shot without a handoff cut | `MainMenuRoot`, `HomeOpening{Controller,Timeline}`, `HomeBedInteraction`, `HomeFixedCameraController`, `GameSessionState` | Implemented |
| City layout model | Generate and validate a deterministic `12 x 12` city, four urban districts, a `4 x 4` central park, street/park path kinds, four graph-separated bars and one player home across an actual bar frontage when available; start the default fresh session `12 m` from their shared street approach, bound fallback placement to `48 m` by traversable street distance and give the default home an `8.8 m` third-floor-capable mass | `Runtime/World/CityLayout*.cs`, `CityDistrict`, `CityTravelDistance`, `PlayerHomeBalconyGeometry` | Implemented |
| City world builder | Construct chunked night-toned streets and park paths with matching static mesh colliders, a physically walkable lawn/plaza, district-varied buildings, trees, benches and gated hedges, plus distinct bar approaches and a recognizable home facade, porch, mailbox, light, walkable approach and balcony matching the Home window/door geometry; assign one explicitly packaged shared URP/Lit material to ordinary runtime primitives and reuse specialized materials and geometry | `CityWorldBuilder.cs`, `CityDistrict.cs`, `BarBuildingMarker.cs`, `PlayerHomeEntranceGeometry`, `PlayerHomeBalconyGeometry`, `RuntimePrimitiveFactory`, `RuntimePrimitiveLit` | Implemented |
| Road-edge fences | Derive the exposed perimeter of street rectangles only, remove every bar entrance, player-home entrance and park-gate interval, then batch collider-free rails and inset posts into `48 m` spatial chunks | `RoadFencePlanner`, `RoadFencePlan`, `RoadFenceWorldBuilder`, entrance geometry, primitive factory | Implemented |
| Noir city presentation | Limit City visibility with `0.070` exponential-squared fog, a matching terminal camera backdrop and a `48 m` far clip while keeping BarInterior clear at `220 m`; follow the player with a more visible 36-particle fog field; apply City-only post-processing, hard directional shadows and no camera MSAA; batch lamp fixtures/bulbs into `48 m` chunks, build depth-tested halos, plan signals and pool at most 12 shadowless spot/point lights | `RuntimeSceneSetup`, `CityNightFixture*`, `CityNightWorld*`, `CityNightAtmosphere`, `CityFogField`, `CityLightHalo`, shared emissive/atmosphere resources | Implemented |
| Scene music | Background-stream `city_theme` only in City, `bar_theme` only in BarInterior and optional `stairwell_theme` only in StairwellInterior through a mild low-pass filter and the shared `Music` group; tolerate a missing optional track and stop each scene-local player on Single-mode transition | `SceneMusicPlayer`, `CityMusicPlayer`, `BarMusicPlayer`, `StairwellMusicPlayer`, `GameAudioMixer`, `Resources/Audio/{CityMusic,BarMusic,StairwellMusic}` | Implemented |
| Common audio mix | Route music, ambience beds/details, world/gameplay SFX and UI through one Resources mixer; keep UI dry, preserve `-6 dB` master headroom under a compressor, feed dedicated reverb/echo returns and select City/Bar/Stairwell/Home/DoorTransition snapshots with short non-door transitions | `GameAudioMixer`, `BarPromenadeAudio.mixer`, `AudioMixerAssetSetup`, scene roots | Implemented |
| Retro SFX and ambience | Generate deterministic mono `22050 Hz` UI/world/bar effects with bounded pools, cooldowns and voices; keep quiet City/Bar/Home/Stairwell beds, then layer a four-source Home soundscape and three-source Stairwell soundscape with quantized eight-second loops, sparse seeded cues and layout-derived anchors. Home schedules distinct closed/open refrigerator loops together at one spatial anchor and applies a smooth equal-power crossfade from the current door amount | `RetroSfx`, `RetroAudioService`, `RetroAmbience`, `InteriorSoundscape{Synthesis,AnchorPlanner}`, `HomeSoundscape`, `StairwellSoundscape`, scene roots and gameplay callers | Implemented |
| Home alarm clock | Keep one validated bed-relative low-poly clock/nightstand in every Home visit; reuse one 28-segment display plus two punctuation elements to flicker `05:59` continuously through the five-second silent menu boundary and switch to solid `06:00` only on Wake, then generate a looping mechanical ring for the following three-second clock hold, route its fully spatial source through `SFX/World`, rattle the model and stop it before wake motion or on cleanup | `HomeAlarmClockPlan`, `HomeAlarmClockBuilder`, `HomeAlarmClockSynthesis`, `HomeAlarmClock`, `GameAudioMixer` | Implemented |
| Road and park navigation | Constrain the player's XZ motion to an indexed union of street, bar-apron and park-lawn rectangles with four continuous park entrances, while matching surface colliders and the controller's `0.28 m` step offset own vertical climbing | `RoadWalkableArea.cs`, `CityParkPlan`, player motor, city surface colliders | Implemented |
| City route planning | Build deterministic ordered shortest paths over generated street and park-path edges with a binary min-heap | `Runtime/Map`, `CityLayout` | Implemented |
| Player motor | Accelerate camera-relative keyboard/gamepad movement to `2.6 m/s` at `6.5 m/s²`, brake at `11 m/s²`, feed constrained displacement back into velocity, preserve heading while idle and reserve immediate planar stops for modal/transition/teleport boundaries | Input System, CharacterController, `IWalkableArea` | Implemented |
| Third-person chase camera | In City and BarInterior, keep very close `2.6 m / 53°` exterior and `2.2 m / 57°` interior framing with raised `1.4 m / 1.3 m` focus points that place the hero below frame center; use weighty damped yaw/focus with bounded lag and teleport snapping; layer subtle deterministic idle/walk motion plus percentage-driven intoxication focus/pitch/roll and balance/fall reactions over free yaw orbit; shorten immediately against obstacles and recover outward smoothly without rotating the player | `PlayerCameraFollow.cs`, `IntoxicationStageRules`, `BarMinigameModalLock`, Input System, physics | Implemented |
| Home fixed camera | Keep one Main Camera at authored position/FOV rather than following the player; hard-cut among the approved main shot at `(-4.48, 3.00, -3.25)`, Euler `(28°, 55°, 0°)`, `64°` FOV, the bathroom shot at `(1.82, 2.20, 0.86)`, Euler `(30°, 38°, 0°)`, `92°` FOV and a third balcony shot; retain the current shot through wider hold bounds, fall back to MainRoom outside specialized zones and block the bed-side camera pocket with dedicated junk; while fixed control is active, opt the player billboard into exact `-camera.forward`/`camera.up` plane alignment to preserve its `64 x 96` aspect, then reset that mode on release; ignore orbit input and preserve only quarter-strength intoxication/balance/fall rotation around the fixed base pose | `HomeCameraShot`, `HomeCameraShotSelector`, `HomeFixedCameraController`, `BillboardSprite`, fixed-pose mode in `PlayerCameraFollow` | Implemented |
| Sprite presentation | Select one of eight unique views from the player-camera angle with 5-degree hysteresis; compose body plus eight upper/lower limb layers; preserve the grounded projected gait and depth sorting while layering continuous intoxication sway, arm spread, knee bend, balance lean and side-specific visual fall/recovery without mirroring | `Runtime/Player`, `PlayerIntoxicationPose`, `Resources/Player/PlayerDirectional{,Parts,BodyExpressions}Atlas.png` | Implemented |
| Animated sprite interactions | Advance a frame-rate-independent `Idle -> Entering -> Looping -> Exiting` timeline with optional extra holds on individual loop frames and a validated per-request exit-duration multiplier that resets to `1x`; slice one point-filtered `8 x 8` atlas into 64 frames on an outer camera-facing root, preserve authored handedness on an inner visual, perspective-project an optional contextual world axis into that plane, optionally render the temporary silhouette through one shared depth-independent overlay material, interpolate both hip position and entry/exit roll, lock the motor throughout, expose interaction input only during the persistent loop, and capture/restore the nine visible rig renderers plus dynamic/contact shadows on completion or cleanup | `PlayerAnimatedInteractionTimeline`, `PlayerAnimatedInteractionController`, `PlayerAnimatedInteractionResources`, `PlayerRuntime`, `BillboardSprite` | Implemented |
| Home bed sleep | Place exactly one reachable trigger on the bed's open `xMax` side; use the first `E` for 24 lie-down frames at `12 fps`, loop 16 sleeping frames at `4 fps` with an extra `0.25 s` full-inhale delay and extra `0.75 s` post-exhale rest for a `5 s` breath cycle until a second `E`, then play 24 wake-up frames at `12 fps`; align the authored head-left pose to the perspective-projected `+X` head-to-foot bed axis, keep the head at the `xMin` pillow with balanced head/foot margins and the complete opaque loop silhouette above the bedding, localize both prompts in RU/EN, hide/restore the crumpled shirt with its per-interaction state, and cancel with complete player restoration if the owning bed is disabled or destroyed | `HomeBedInteractionPlan`, `HomeBedInteraction`, `HomeInteriorRoot`, `Resources/Player/PlayerBedSleepAtlas.png`, localization catalogs | Implemented |
| Home refrigerator | Derive one prominent worn refrigerator, clear player-width approach, trigger, first-person camera pose, sound/light anchors and eight stable storage slots from the Home kitchen plan; split the physical counter around its footprint and move the table `0.30 m` deeper to preserve the route; compose a hollow lined cabinet with three shelves, drawer, two door bins, frost/grime/rust and initial vodka, egg and open-stew occupants. Register each occupied slot through a pure item catalog and item view with localized name/description, preview transform, renderers and a tight non-blocking selection trigger. On interaction, capture modal ownership, keep the puppet visible through the unscaled Bezier first-person approach, hide its rig and shadows exactly when the procedural sleeved hand appears, animate the handle, seal and `102°` door and hold the lit open state. While the ordinary interactor is suspended, bind the visible close prompt directly to the same guarded `RequestClose` path used by keyboard/gamepad input. Within that existing lock, support pointer hover plus keyboard/gamepad selection, then fly the chosen item through `Browsing -> FlyingIn -> Inspecting -> FlyingOut` to a slowly rotating centered preview over a dark backdrop with localized title, description and placeholder `Take`/`Use`/`Back` actions; `Take`/`Use` do not mutate storage or session state. Return the item before the outer door may close and restore its exact parent, sibling index, local transform, collider and renderer colors on normal return or lifecycle cleanup; restore the puppet, fixed shot, input and HUD when the outer interaction completes. Crossfade synchronized closed/open refrigerator loops with equal-power gains and use an emissive strip/halo rather than another realtime `Light` | `HomeRefrigerator{Plan,WorldBuilder,View,Interaction,InteractionTimeline,FirstPersonHand}`, `HomeRefrigeratorItem{Catalog,View,InspectionController,InspectionTimeline,InspectionView}`, `HomeInterior{LayoutPlanner,WorldBuilder,Root}`, `HomeSoundscape`, `RetroSfx`, `RetroUiTheme`, localization catalogs | Implemented |
| Player shadows | Cast one camera-independent nine-part alpha-clipped puppet toward the main directional light and remap the live gait/intoxication/fall pose into its light-relative authored view; expand, rotate and offset the shared analytic contact quad for a fallen puppet without moving the physical player root or enabling practical-light shadows | `PlayerDynamicShadow`, `PlayerContactShadow`, `PlayerShadowResources`, `Player{SpriteShadowCaster,ContactShadow}.shader`, main directional light | Implemented |
| Interaction/UI | Select nearby bar/home entrances, exits, the Home bed and refrigerator, activity stations and the ordinary-drink counter; show localized contextual world prompts as full pointer-click targets whose callbacks repeat the same availability guards and action path as E/Enter/gamepad South, including the refrigerator-owned modal close; retain refrigerator-item hover names and action-only modal buttons without persistent key-binding guides or control-hint footers, and use the appropriate state-preserving interaction or modal input ownership through the crisp retro theme | `PlayerInteractor`, `InteractionPromptView`, `Runtime/Interaction`, `Runtime/UI`, `RetroUiTheme`, localization catalogs | Implemented |
| Bar minigame catalog | Own one explicit ordered set of activity IDs, localized labels/prompts and factories used by both normal interiors and debug launches; currently register cocktail mixing, beer pong, Split the G and Tinctures in a Row without game-specific debug-window code | `BarMinigameCatalog`, `BarMinigameDefinition`, `IBarMinigame` | Implemented |
| F9 minigame debug window | List every registered game in `City` and `BarInterior`, close a conflicting map, minigame or pre-commit bar browser before taking the modal lock, refuse to interrupt a committed physical drink service, adjust the real session intoxication in clamped `-20/+20` steps through the Left/Right arrow keys or clickable buttons while preserving drink context, and launch isolated instances that do not persist drinking state or complete a bar visit | `MinigameDebugWindow`, `BarMinigameCatalog`, `GameSessionState`, `BarMinigameModalLock`, scene roots | Implemented |
| Structured session diagnostics | Write one bounded UTF-8 NDJSON stream with build/session/scene/seed context, correlated transitions and minigame runs, state-change/result events and Unity warnings/errors; rotate at 5 MiB, keep three archives, default tests to off, and expose `F8` snapshot plus `Shift+F8` directory access | `Runtime/Diagnostics`, instrumented state boundaries, `MinigameDebugWindow`, `ai/debug-log.md` | Implemented |
| Bar activity routing | Assign the first four stable row-major bars to cocktail mixing, beer pong, Split the G and Tinctures in a Row through one pure resolver, fall back to cocktails for later bars, preserve the activity through transition, then resolve its registered factory | `BarActivityKind`, `BarActivityAssignment`, `BarMinigameCatalog`, `BuildingLot`, `BarEntrance`, `GameSessionState` | Implemented |
| City map UI | Display district colors and localized labels, distinct park land/paths, player/bars, a dedicated labeled home icon, green completed visits with a count, and a separately badged ordered itinerary on a logical `640x360` retro canvas | `CityMapController.cs`, `CityMapView.cs`, `CityDistrict`, localization catalogs, `RetroUiTheme`, Input System | Implemented |
| Scene transition | Keep one guard across the full city/interior transfer, load `DoorTransition` in Single mode, preload the requested destination with activation blocked, and activate it only after the door sequence reaches its final blackout | `SceneTransitionService`, `SceneIds`, `GameSessionState`, Unity async scene loading | Implemented |
| Door transition presentation | Run a deterministic `3.15 s` unscaled fixed-camera sequence in a black void: reveal, turn the handle, swing the low-poly door outward toward the player over a solid black doorway sprite, push the camera and fade fully to black; tint the door lighting warm when entering and cold when exiting, with generated latch and hinge-creak cues | `DoorTransitionRoot`, `DoorTransitionTimeline`, `DoorTransitionDirection`, `RuntimeSceneSetup`, `RetroSfx` | Implemented |
| Session state | Reset a complete new run on launch and preserve seed, active bar/activity, ordered route, visited-bar set, explicit bar/home return kind, stairwell side, consumed Home arrival kind, a `$999` default cash wallet, intoxication, last alcohol, consumed-drink count and deterministic balance delay/sequence | `GameSessionState.cs`, `CityReturnKind`, `HomeArrivalKind` | Implemented |
| Bar drink retail and physical service | Offer nine localized ordinary drinks at fixed integer prices through a natural seated-eye first-person counter scene in every bar; hide and exactly restore the green order markers across the modal lifecycle; map the full row to nine individual 3D bottles with solid colliders, selection triggers, kinematic rigidbodies and mouth anchors; validate affordability and maximum intoxication before one atomic cash/drink commit, then reject exit while low-poly arms pick up the selected bottle, pour through a world-space stream, drink from the matching tumbler, pint, wine glass, shot glass or snifter for exactly three seconds and return the empty vessel to the counter; return to persistent browsing for repeat orders and reserve camera return plus presentation restoration for the dedicated Exit action or lifecycle teardown, without refunding a committed purchase; keep water non-sobering and exclude special Moonshine | `BarDrinkCatalog`, `BarDrinkPresentationCatalog`, `DrinkPurchaseRules`, `BarDrinkService{Plan,Timeline,WorldBuilder,View}`, `BarDrink{Bottle,Vessel}View`, `BarDrinkFirstPersonArms`, `BarDrinkShopController`, `BarDrinkShopView`, `BarCounterStation`, `GameSessionState` | Implemented |
| Cocktail domain | Define four bases and ingredient compatibility; generate a deterministic seven-item shelf with four compatible additions and three traps; score and advance a three-round session | `Runtime/Cocktails`, persisted `DrinkId` values | Implemented |
| Cocktail minigame | Run same-scene modal base/addition/serve input, commit drinking state after every serving, report completion through the shared minigame contract, retain bad-mix score/intoxication penalties and terminate at the explicit `MaxIntoxicationReached` outcome without a separate timed status | `CocktailMinigameController.cs`, `CocktailMinigameView.cs`, `IBarMinigame` | Implemented |
| Cocktail presentation | Slice a 4x4 pixel-art atlas into IMGUI UV cells and animate ingredient travel/tilt, pouring, glass fill, success sparks, bad bubbles, shake, stage results and final rank in a responsive retro layout | `CocktailSpriteLibrary.cs`, `CocktailMinigameView.cs`, `RetroUiTheme`, `Resources/Cocktails` | Implemented |
| Beer-pong domain | Simulate an aimed ball at fixed 120 Hz with swept table/cup-mouth contacts, rim and table restitution, six standing cups, ten throws, clean/bank scoring, early-clear bonus and terminal outcomes | `Runtime/BeerPong/BeerPong{Types,TableLayout,Physics,Session}.cs` | Implemented |
| Beer-pong minigame | Run a modal 2D second-bar activity, project physical x/y/z coordinates onto the table backdrop, persist a light-beer penalty after each miss and report terminal completion to the interior root | `BeerPongMinigameController`, `BeerPongMinigameView`, `BeerPongProjection` | Implemented |
| Beer-pong presentation | Draw the point-filtered 640x360 table and 4x4 ball/hand/cup/effect atlas, projected shadows, cup reactions, compact aim/power feedback and localized results | `BeerPongSpriteLibrary`, `Resources/BeerPong`, `RetroUiTheme` | Implemented |
| Split the G domain | Advance a normalized pint from countdown through one irreversible held sip, settling and scored result; preserve frame-chunk invariance, five tolerance bands, up to three fresh glasses and the best session result | `SplitTheGSettings`, `SplitTheGSession`, `SplitTheGScoring` | Implemented |
| Split the G minigame | Run the modal third-bar hold/release activity for Space, LMB and gamepad South; hide the exact level while drinking/settling, persist the actual dark-beer fraction immediately and complete on Continue, glass three or maximum intoxication | `SplitTheGMinigameController`, `BarMinigameModalLock`, `GameSessionState` | Implemented |
| Split the G presentation | Draw a point-filtered 640x360 bar backdrop and 4x4 pint/hand/foam/effect atlas with glass tilt, target line, obscured liquid, settling foam, localized result cards and generated gulp SFX | `SplitTheGMinigameView`, `SplitTheGSpriteLibrary`, `Resources/SplitTheG`, `RetroUiTheme`, `RetroSfx` | Implemented |
| Tincture-match domain | Generate a seeded match-free `7x7` board with five normal flavors, at least three legal swaps and at most one `XXX`; resolve unique clears, gravity, refills, cascades up to `x5`, 15 accepted moves and deterministic dead-board reshuffles | `Runtime/TinctureMatch`, `TinctureMatchGenerator`, `TinctureMatchResolver`, `TinctureMatchSession` | Implemented |
| Tincture-match minigame | Run the modal fourth-bar board for mouse click/drag, keyboard and gamepad; reject invalid swaps without spending a move, expose `XXX` flavor clears, and immediately persist only an activated `Moonshine` for +24 intoxication | `TinctureMatchMinigameController`, `BarMinigameModalLock`, `DrinkRules`, `GameSessionState` | Implemented |
| Tincture-match presentation | Draw the logical `640x360` view with a point-filtered backdrop and 4x4 atlas of five symbol-coded shots, literal `XXX` moonshine and swap/clear/cascade/reshuffle effects; localize RU/EN feedback and play generated swap, match and burst SFX | `TinctureMatchMinigameView`, `TinctureMatchSpriteLibrary`, `Resources/TinctureMatch`, `RetroUiTheme`, `RetroSfx` | Implemented |
| Intoxication stages and presentation | Map `0–100` into Sober plus five named 20-point ranges, interpolate movement speed from `1.00` to `0.70`, drive escalating puppet/camera/world-composite effects in City, BarInterior, HomeInterior and StairwellInterior and show a crisp five-segment named HUD only above zero | `IntoxicationStageRules`, `IntoxicationStatusController`, `PlayerIntoxicationPose`, `IntoxicationRenderState`, `IntoxicationHudView`, scene roots | Implemented |
| Balance checks and falling | Above `60`, preserve a seeded interval/sequence across scene loads, simulate an inertial arrow at fixed `120 Hz`, render a `140°` overhead arc with shrinking green sector and risk meter, accept arrows/A-D/D-pad/stick, and on failure hold modal ownership through visual fall/down/rise recovery while the upright physical root remains stationary | `BalanceChallengeModel`, `IntoxicationStatusController`, `BalanceCheckView`, `BarMinigameModalLockOptions.BalanceCheck`, `PlayerSpriteRig`, `PlayerContactShadow` | Implemented |
| Bar interior layout and world | Generate and validate one deterministic `22 x 16 x 4.8 m` shared layout with seven zones, four connected clear paths, aligned blocking footprints, one reachable activity fixture and one reachable counter station; compose the shell, long backlit counter, five ordinary stools plus the physical drink-service stool, booths, four social tables, stage, entrance details and activity-specific set dressing from that plan | `BarInteriorLayout{Plan,Planner,Validator}`, `BarInteriorWorldBuilder`, `BarDrinkServiceWorldBuilder`, `BarInteriorRoot` | Implemented |
| Player home interior | Generate and validate one compact `10 x 8 x 3.4 m` main-room/bathroom plan with clear entry, main and bathroom-access paths; keep six main-room furniture footprints—including blocking camera-corner junk and the table position that clears the refrigerator approach—plus toilet, shower, sink and the separate alarm-clock dressing out of protected circulation; compose the impoverished, cluttered old alcoholic's flat and full tiled bathroom at runtime, then replace the right-wall blackout with a real window and open glazed door connected to a walkable balcony at `4.7 m` street elevation; open-looking rails retain invisible safety colliders | `HomeInteriorLayout{Plan,Planner,Validator}`, `HomeBalconyLayout{Plan,Planner,Validator}`, `HomeAlarmClockPlan`, `HomeRefrigeratorPlan`, `HomeInteriorWorldBuilder`, `HomeBalconyWorldBuilder`, `HomeBathroomBuilder`, `HomeInteriorDressingBuilder`, `HomeInteriorRoot`, `HomeEntrance`, `HomeExit` | Implemented |
| Player-home stairwell | Route every home visit through a separate side-aware `8.6 x 9.6 x 6.25 m` stairwell scene: connect the street lobby, middle landing and apartment door with 48 visible steps over three seam-free ramp colliders and radius-safe overlapping navigation corridors; block the flight above the hero's floor across its full width and standing height; dress the space with stained concrete, rusty rails, pipes, ventilation, cabinets, grilles, trash and visible debris; hard-cut between three height-selected fixed shots with hysteresis, each framing an exposed suspended HDR tube/halo and stronger flickering practical pool; retain the green desaturated grade, bounded dust, concrete bed, spatial ventilation/electrical layers, seeded industrial cues under a dark reverb/echo profile and optional scene-local `stairwell_theme` slot | `StairwellLayout{Plan,Planner,Validator}`, `StairwellWorldBuilder`, `StairwellDressingBuilder`, `StairwellInteriorRoot`, `StairwellFixedCameraController`, `StairwellInteriorAtmosphere`, `StairwellAmbiencePlayer`, `StairwellSoundscape`, `StairwellMusicPlayer`, `GameAudioMixer`, `Stairwell{StreetExit,ApartmentEntrance}`, `GameSessionState` | Implemented |
| Stairwell cat | Perch one seated pixel-art cat with its back to the camera on the `Middle Landing Back Rail`; keep the sprite aligned to every fixed-camera plane, select a head turn toward the player, run ordinary idle motion plus a rare eight-frame grooming sequence roughly every 36 seconds, and expose a non-blocking localized placeholder through the shared `IInteractable` path | `StairwellCat{Plan,Actor,SpriteLibrary,LookSelector,IdleModel}`, `StairwellCatInteraction`, `BillboardSprite`, `Resources/Stairwell/Cat/StairwellCatAtlas` | Implemented |
| Home exterior context | Reconstruct a bounded same-seed view of the actual player-home street inside `HomeInterior`: transform nearby roads, lots, stable windows, lamps and signals into Home-local coordinates while omitting gameplay/collision and never creating a second City root, player, camera or realtime street-light pool; keep the walkable balcony and its matching City facade on one shared geometry contract | `HomeExteriorContextPlan`, `HomeExteriorContextPlanner`, `HomeExteriorViewBuilder`, `PlayerHomeBalconyGeometry`, `CityNightFixturePlanner` | Implemented |
| Player home atmosphere | Keep Home fog-free and post-processed under a weaker hard-shadow directional/ambient base; retain exactly two bounded shadowless practical lights, each physically co-located with a visible HDR emitter and depth-tested halo—the dirty-yellow hanging lamp and cold bathroom tube—add one cold shadowed cookie Spot aimed through the window, and reuse one shared transparent glass shader/material for window and door; also own a runtime Bloom/color/exposure/vignette/grain profile with cleanup, at most 12 shared-material dust motes, a calm room bed, synchronized closed/open spatial refrigerator loops, balcony night air, sparse domestic cues and a short damped no-echo profile | `RuntimeSceneSetup.EnsureHomeInterior`, `HomeInteriorAtmosphere`, `HomeBalconyResources`, `HomeWindowGlass.shader`, `HomeAmbiencePlayer`, `HomeSoundscape`, `GameAudioMixer`, `CityLightHalo`, shared emissive/atmosphere materials | Implemented |
| Bar crowd | Plan 12 stable role-based NPCs under a cap of 14, slice one shared transparent 3x2 pixel atlas, run centralized `8 Hz` decisions plus lightweight per-frame poses/walking, billboard after the chase camera and sort whole NPCs before/behind the layered player by camera depth | `Runtime/Bar/NPC`, `Resources/Bar/Npc/BarNpcAtlas.png`, bar layout anchors | Implemented |
| Bar cinematic atmosphere | Run six shadowless warm/cool practical lights, bar-only URP post-processing, bounded dust, scene-local spatial crowd/rare cues and a collision-checked skippable `1.35 s` quadratic single-camera arrival reveal; preserve DoorTransition without post-processing and restore follow/orbit state before any modal opens | `BarInteriorAtmosphere`, `BarSoundscape`, `BarArrival{Timeline,Presentation}`, `RuntimeSceneSetup`, bar light/audio anchors | Implemented |
| Automated tests | Build-scene order, new-session and one-shot Home arrival state, opening timeline/menu lifecycle, alarm placement/synthesis/routing/cleanup, prompt callback lifecycle plus generic and refrigerator-modal click dispatch, refrigerator geometry/slots/item catalog/selection triggers, outer and nested timelines, hover/click inspection, placeholder actions and exact transform/modal cleanup, all nine physical bar bottles plus five vessel mappings/fill/stream/first-person transaction lifecycle, determinism, home placement/interior/bathroom/balcony/stairwell plans and same-seed exterior reconstruction, City/Home facade transforms, connected balcony and stair walkability, safety colliders, sealed upper debris and the cat's perch/atlas/idle/look/interaction contracts, window light/glass and Home/Stairwell presentation, physical street-to-apartment traversal, bed interaction and fixed shots, bounded scene atmosphere/audio, mixer DSP topology/snapshot values/routing, distinct closed/open refrigerator synthesis, equal-power mixing and soundscape lifecycle, minigame catalog/factories, isolated F9 launch and `±20` state adjustment, modal restoration, drinking/balance state, percentage-driven player presentation, cocktail/beer-pong/Split-the-G/tincture behavior, retro UI/audio, PS1 GPU presentation and complete City/Stairwell/Home round trips | `Assets/Tests` | Implemented |

Primary intended flow:

```text
build index 0 -> MainMenu -> BeginNewGame + HomeArrival.OpeningSleep
                           -> HomeInterior sleeping opening
                              -> clock shot: periodic 05:59 flicker + 5 s locked silence
                              -> same silent 05:59 shot: Wake Up/Quit
                              -> Wake: solid 06:00 + 3 s alarm hold
                              -> alarm stops -> smooth 6 s camera/wake
                              -> ordinary Home control
                              -> StairwellInterior -> City
seed -> layout data -> validation -> world builder
             -> districts + central park -> district-specific world
             -> indexed street/park navigation -> player
             -> weighted route planner -> district-aware city map
             -> street perimeter -> bar/park-gate-aware chunked fences
             -> night fixture plan -> chunked lamps + signals
player + lamp anchors -> bounded light pool -> light halos
player + seed -> player-following local fog field
player -> interaction -> activity-aware scene transition <-> session state
                    -> DoorTransition scene -> preloaded destination
                    -> home layout plan -> validation -> runtime home world
                                       -> bathroom + collider-free dressing
                                       -> refrigerator plan + clear approach
                                          -> split counter + worn hollow cabinet
                                          -> 6 shelf slots + 2 door slots
                                          -> vodka / egg / open stew can
                                             -> item catalog + registered item views
                                             -> hover tint + localized cursor name
                                          -> modal first-person open/inspect/close
                                             + hand / light / synchronized spatial hums
                                             -> nested PS1 item inspection
                                                -> fly to center + dark backdrop + rotation
                                                -> name/description + Take/Use/Back placeholders
                                                -> exact item restore before outer close
                                       -> balcony plan -> window/open door
                                          -> connected deck at 4.7 m street elevation
                                          -> open rails + invisible safety colliders
                                       -> same-seed bounded street reconstruction
                                          -> roads/lots/windows/lamps/signals visuals
                                          -> no duplicate City gameplay/light pool
                                       -> three authored shots + hold hysteresis
                                          + opt-in exact billboard plane/aspect
                                       -> open-side bed trigger
                                          -> E: lie -> persistent sleep -> wake
                                          -> reusable animated interaction
                                             + motor/rig/shadow restoration
                                             + projected bed axis / handedness
                                       -> bed-relative alarm clock/nightstand
                                          -> 3 s post-Wake spatial mechanical ring
                                          -> silent dressing after wake/normal arrival
                                       -> two aligned practical Light/emitter/halo pairs
                                          + cold shadowed window cookie Spot
                                          + shared transparent glass
                                          + grade + dust + Home loop
                                       -> exit -> StairwellInterior
                                          -> street exit -> home return kind -> City
                    -> bar layout plan -> validation -> runtime world
                                       -> clear routes + activity fixture
                                       -> six practical lights + local grade
                                       -> 12-NPC crowd + spatial soundscape
                                       -> skippable single-camera reveal
                    -> generic bar station -> minigame catalog
                                           -> cocktail rules/offers
                                           or beer-pong physics/session
                                           or Split the G timer/scoring/session
                                           or tincture board/cascades/XXX
                                           -> drinking progress -> session state
                                           -> completed visit -> city map
                    -> counter station -> physical nine-bottle browser
                                       -> seated first-person camera + arms
                                       -> atomic cash + drinking transaction
                                       -> bottle pickup + matching 3D vessel
                                       -> world-space pour/fill + 3 s drink
                                       -> vessel return -> persistent browser
                                       -> explicit Exit -> camera return
stairwell middle rail -> StairwellCatActor -> fixed-camera rear-view billboard
                                             + player-tracking head
                                             + idle / rare 8-frame grooming
                      -> StairwellCatInteraction -> localized placeholder
session intoxication -> five-stage profile -> motor + puppet + camera
                                      -> PS1 world distortion
                                      -> above 60 -> deterministic balance QTE
                                         -> success or visual fall/recovery
F9 -> close conflicting modal -> session intoxication +/-20
                              -> minigame catalog -> isolated debug instance
F8 -> structured state snapshot -> immediate debug.log flush
session/scene/gameplay result boundaries -> correlated rotating debug.log
scene root -> GameAudioMixer scene snapshot
           -> matching scene-local music player -> Music -> Single transition stops it
           -> matching procedural ambience player -> Ambience/Beds
           -> Home spatial soundscape -> synchronized closed/open fridge loops
                                      -> equal-power door crossfade -> Ambience/Details
           -> Stairwell spatial soundscape -----------------------> Ambience/Details
gameplay/UI events -> RetroAudioService -> bounded pools -> SFX/World, SFX/Gameplay or UI
Music/details/world sends -> scene-tuned reverb/echo returns -> Master
URP post-processing -> averaged low-resolution image -> subtle RGB555 blend -> point upscale
world composite -> crisp retro IMGUI overlay
```
