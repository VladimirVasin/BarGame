# Systems map

| System | Responsibility | Depends on | Status |
| --- | --- | --- | --- |
| Unity URP foundation | Rendering profiles, seven build scenes, PC renderer settings | Unity 6000.5.5f1, URP 17.5.0 | Current |
| PS1 world composite | Footprint-average the final post-processed Game-camera image to `640x360` by default (lower `426x240`/`320x180` options), apply percentage-driven intoxication warp/ghost/chromatic/warmth/exposure/vignette parameters, blend 35% perceptual-space RGB555 without screen-space dithering and point-upscale it | `Runtime/Rendering`, `IntoxicationRenderState`, `Ps1PresentationProfile`, `Ps1Composite` material/shader, `PC_Renderer.asset` | Implemented |
| City composition | Exact seven-scene bootstrap and runtime roots | `Runtime/Core`, scene IDs | Implemented |
| Startup waking opening | Start a fresh run behind a black build-index-0 boundary, pass one consumed `OpeningSleep` arrival into the existing Home, begin in the bed loop, hold the first Home shot on a silent frozen `05:59` clock whose complete display flickers briefly at three-second intervals across five input-locked seconds, then reveal localized PS1 Wake Up/Quit choices while retaining that shot; only Wake atomically sets session time to `06:00`, starts it, hides the menu and begins a three-second unscaled alarm hold, after which the ring stops and the camera plus six-second `3x` opening-only wake begin, continuously settling into active gameplay without a handoff cut | `MainMenuRoot`, `HomeOpening{Controller,Timeline}`, `HomeBedInteraction`, `HomeFixedCameraController`, `GameSessionState` | Implemented |
| Session clock and day/night rules | Reset each fresh session to a frozen `05:59`; accept the startup Wake once, start at `06:00`, then advance persistent time and day index from scaled delta at `1.0` game minute per real second (`24 h = 1440 s = 24 min`). Sample night before `06:00`, smooth dawn `06:00-07:00`, day `07:00-18:00`, smooth dusk `18:00-19:00` and night from `19:00`; preserve the state across scene loads and stop naturally at `timeScale = 0` | `GameTimeState`, `GameTimeRuntime`, `GameTimeDayNightRules`, `GameSessionState`, runtime bootstrap | Implemented |
| Session time displays | Show the same current session `HH:MM` on the physical Home alarm clock and in the inventory Status panel; keep the inventory value live from session state and naturally fixed while that menu owns `timeScale = 0` | `HomeAlarmClock`, `InventoryView`, `GameSessionState`, localization catalogs | Implemented |
| Gameplay pause menu | In City, Bar, Supermarket, Home and Stairwell free gameplay, let Escape/gamepad Start acquire the existing fullscreen modal lock, freeze scaled time, pause listener-owned gameplay audio while keeping UI SFX live, and draw a localized `640x360` Resume/Start Over/Quit menu. Keep save/load and settings absent, require default-No confirmation before destructive actions, restore every captured global/input/HUD/camera state on resume or lifecycle cleanup, leave Escape with an existing child modal, reject pause during scene transitions/Home opening, and keep the bar arrival reveal intact | `PauseMenu{Model,Controller}`, `BarMinigameModalLock`, `RetroAudioService`, gameplay scene roots, localization catalogs | Implemented |
| Hero inventory | In City, Bar, Supermarket, Home and Stairwell free gameplay, open a localized fullscreen item screen with `I` or gamepad North, capture the shared modal lock, freeze scaled time while preserving the ambient audio bed, and restore exact motor/interactor/camera/HUD/time state on toggle, cancel, transition or lifecycle cleanup without leaking Escape into pause. Present compact intoxication, hunger and stress bars, dollar cash, the dedicated transparent portrait rendered from the production 3D hero, visible point-filtered item icons, a five-column ordered stack grid and selected description on the logical `640x360` canvas. Render the selected item as a live rotating low-resolution 3D model in both the lower panel and Examine view through a hidden lifecycle-owned camera/light/RenderTexture stage, sharing physical refrigerator and supermarket models plus inventory keys/lighter and creating no per-instance materials. Expose contextual Eat/Drink through pointer, `U` and gamepad West beside Examine/Close; keep the menu open, clamp selection and refresh feedback/preview after an atomic use. Start each new run with apartment keys and a lighter; preserve inventory and collected world-source IDs across scene loads, reset them with the session, and atomically transfer refrigerator pickups or supermarket purchases through stable source IDs | `Inventory{Types,State,MenuModel,ItemModelFactory,ConsumableCatalog}`, `Inventory{Controller,View,IconLibrary,ItemPreviewRenderer}`, `Resources/Player/Player3DPortrait`, `PlayerNeedsRules`, `GameSessionState`, `HomeRefrigeratorInventoryAdapter`, `SupermarketProductCatalog`, `BarMinigameModalLock`, gameplay scene roots, localization catalogs | Implemented |
| Hunger, stress and consumption | Keep session hunger and stress as clamped `0-100` values where higher is worse; start and reset both at zero, preserve them across ordinary scene loads, and currently add neither passively. Give every existing food an explicit relief value and poor-food minimum hunger of `20`; reject a no-effect use without removing its stack. Give every alcohol an independent stress-relief value, consume the inventory vodka bottle as four atomic servings, and reject it at maximum intoxication without mutation. Commit stress relief exactly once with retail service, actual cocktail alcohol, beer-pong misses, proportional Split-the-G sips and Tincture `XXX`; water, bad-mix penalties, debug snapshots and non-persistent F9 runs add no relief | `PlayerNeedsRules`, `InventoryConsumableCatalog`, `DrinkRules`, `GameSessionState`, inventory and minigame controllers | Implemented |
| Inventory-backed target interactions | Give a world target a reusable single-stack item requirement plus localized Talk response, confirmation and missing-item feedback. Open a default-Talk `Talk`/`Interact` menu under the shared modal lock, use default-No confirmation, support pointer/keyboard/gamepad input, and keep the ordinary prompt unavailable while feedback is visible. On Yes, let the handler preflight all presentation resources first, then atomically remove the still-present requirement and begin execution exactly once; preparation failure, stale inventory, No, cancel and lifecycle teardown consume nothing, while a thrown startup refunds the just-committed item. Restore input/HUD/camera/modal state through an idempotent, ownership-scoped handler cleanup contract | `InventoryItemRequirement`, `InventoryTargetInteraction{Definition,Model,Controller}`, `IInventoryTargetInteractionHandler`, `GameSessionState`, `PlayerInteractor`, `InteractionPromptView`, localization catalogs | Implemented |
| City blueprint and layout model | Resolve one immutable, stable-ID `CityBlueprint` for the session; classify areas as `UrbanBuilt` or `NonUrbanOpen`, keep identity separate from reusable archetype, validate normalized connected sparse cells and center/north placement policies, and generate roads only over their road-grid footprint. The default coastal blueprint retains the connected `12 x 12` lot core, fixed centered `4 x 4` park and four required-bar urban areas, adds a full-width northern beach/water pair, then extends east with a reachable `4 x 4` Lake and `3 x 2` Cemetery. Generic Lake and Cemetery profiles produce typed shore/water or cemetery surfaces and one deterministic street access; the legacy rectangular overload remains compatible. Continue placing four graph-separated bars, one home, one eligible supermarket and default district public lots under the existing distance and frontage rules | `CityBlueprint`, `CityBlueprintCatalog`, `CityLayoutGenerator`, `CityLayout`, `CitySurfacePlan`, `CityDistrict`, `CityTravelDistance`, entrance geometry | Implemented |
| District public places | Reserve one non-building, non-park, non-bar, non-home and non-primary-landmark lot in every default urban district; require both lot dimensions to meet `CityLayoutGenerator.MinimumDistrictPointLotDimension` (`18 m`) and safely emit no authored POIs for smaller custom blocks; otherwise prefer the most street sides, then distance from the primary landmark, then a stable seeded rank. Expose canonical public bounds and full-width street approaches; build Old Town's waterworks court, Residential's drying yard, Industrial's weighbridge and Nightlife's last-route island as free-standing physical places with intentional obstacle/surface colliders. Keep the last-route island non-emissive, ground its departure board on two supports and dress it with weathered route plates, layered posters, a bin, bottles, a discarded timetable and a lost scarf; reconstruct nearby sites without gameplay colliders outside Home | `CityDistrictPointOfInterestPlan`, `CityDistrictPointOfInterestPlanner`, `CityLandUseKind.DistrictPointOfInterest`, `CityLayoutGenerator`, `CityDistrictPointOfInterestWorldBuilder`, `HomeExteriorContextPlan`, `HomeExteriorViewBuilder` | Implemented |
| City world builder | Construct chunked noir-toned streets and park paths with matching static mesh colliders; build only canonical blueprint surfaces instead of one rectangular ground slab, with separate batched buildable/park, beach, lake-shore, cemetery and non-colliding water treatments. Give Lake a stone water edge and sparse shore landmarks, and Cemetery a gated perimeter, entry path, ordered graves and trees while keeping water and street-access clearance authoritative. Retain the walkable lawn/plaza, four default district public places, district-varied buildings, distinct entrances, supermarket and recognizable home facade/balcony while reusing shared materials and geometry | `CityWorldBuilder.cs`, `CitySurfacePlan`, `CityOpenAreaDecorationPlan`, `CityOpenAreaWorldBuilder`, `CityExteriorAppearance`, `CityDistrictPointOfInterestWorldBuilder`, facade builders, entrance geometry, `RuntimePrimitiveFactory` | Implemented |
| City visual diversity | Derive one stable ordered decoration plan from the city, fence and night-fixture plans; guarantee one district silhouette/facade recipe for every ordinary building lot, one primary building landmark per urban district and two park landmarks, then add clearance-aware frontage, roadside and park clusters from 24 visual families. Public-place lots are a separate land use and never receive a building core or facade POI. Expand decoration recipes as collider-free, light-free and shadowless boxes, batched into at most six shared-material styles per `48 m` chunk, with every facade oriented to its real road frontage | `CityDecoration{Descriptor,Plan,Planner,Validator}`, `CityDecorationWorldBuilder`, `CityWorldBuilder`, `RoadFencePlan`, `CityNightFixturePlan`, `RuntimePrimitiveFactory` | Implemented |
| Road-edge fences | Derive the exposed perimeter of street rectangles only; remove every bar, supermarket, player-home and park-gate entrance interval, every complete district-public side and each canonical beach/lake/cemetery access; then batch the remaining collider-free rails and inset posts into `48 m` spatial chunks | `RoadFencePlanner`, `RoadFencePlan`, `CityOpenAreaAccessDescriptor`, `RoadFenceWorldBuilder`, entrance geometry, primitive factory | Implemented |
| Supermarket interior and finite shelf purchases | Enter one separate runtime-composed `16 x 11 x 3.6 m` scene with protected aisles, three shelf sections, decorative checkout/stockroom fixtures and one decorative cashier. Put exactly one physical unit of egg, vodka, closed stew, instant noodles and day-old bread on those shelves. Keep one modal browser open while pointer/keyboard/gamepad previous-next navigation cycles across all stocked shelves, skips empty shelves and reuses each shelf's authored fixed camera position/FOV while centering its rotation on the selected product's visual bounds; place muted clickable arrows directly beside the product. On confirm, atomically validate a stable source, catalog offer, cash and stack capacity, then deduct cash, add one inventory item and remove the bought model/collider immediately. Filter committed source IDs when rebuilding the scene so sold stock stays absent until a new game; keep `ClosedStewCan` distinct from the cat-compatible `OpenStewCan` | `SupermarketInterior{LayoutPlan,LayoutPlanner,LayoutValidator,WorldBuilder,WorldResult,Root}`, `Supermarket{ShelfView,ProductView}`, `SupermarketShelf{Station,ShopController,ShopView}`, `SupermarketProductCatalog`, `SupermarketPurchaseRules`, `GameSessionState` | Implemented |
| City day/night presentation | Apply the shared time sample to City's directional/ambient/reflection lighting and scale its lamp/bar-light/halo night factor across dawn and dusk, while retaining hard shadows and disabled camera MSAA. Make stable day/night samples no-op, keep recurring phase updates free of environment-cubemap regeneration and suspend nearest-lamp reassignment while the light factor is zero. Keep `0.070` exponential-squared fog, its matching terminal backdrop, the `48 m` far clip, 36-particle `CityFogField`, `CityNoirVolumeProfile` and retained runtime Exp2 variant unchanged at every hour; keep Bar, Supermarket and Stairwell visuals outside this cycle | `RuntimeSceneSetup`, `CityDayNightController`, `GameTimeDayNightRules`, `GraphicsSettings`, `CityNightFixture*`, `CityNightWorld*`, `CityNightAtmosphere`, `CityFogField`, `CityLightHalo` | Implemented |
| Scene music | Background-stream `city_theme`, `bar_theme`, optional `supermarket_theme`, optional `stairwell_theme` and optional `home_theme` only under their matching roots through a mild low-pass filter and shared `Music` group. Start every scene theme at zero and fade it in with an unscaled smooth envelope; cap the apartment theme at `0.35` source volume instead of the shared `0.65` scene-music level. Preload a requested destination while the old scene remains alive, wait for its theme to fade fully out, then activate. Home alone fades `home_theme` to zero and pauses it in the Balcony camera zone, preserving its sample for an indoor fade-in resume. Keep the balcony-smoking vignette's optional `smoking_theme` on its separate animation-driven envelope and tolerate every missing optional clip | `SceneMusicPlayer`, scene music players, `HomeMusicPlayer`, `HomeSmokingMusicPlayer`, `SceneTransitionService`, `GameAudioMixer`, `Resources/Audio/{CityMusic,BarMusic,SupermarketMusic,StairwellMusic,HomeMusic,SmokingMusic}` | Implemented |
| Common audio mix | Route music, ambience beds/details, world/gameplay SFX and UI through one Resources mixer; keep UI dry, preserve `-6 dB` master headroom under a compressor, feed dedicated reverb/echo returns and select City/Bar/Stairwell/Home/DoorTransition snapshots with short non-door transitions | `GameAudioMixer`, `BarPromenadeAudio.mixer`, `AudioMixerAssetSetup`, scene roots | Implemented |
| Retro SFX and ambience | Generate deterministic mono `22050 Hz` UI/world/bar effects with bounded pools, cooldowns and voices; keep quiet City/Bar/Home/Stairwell beds, then layer a five-source Home soundscape and three-source Stairwell soundscape with quantized loops, sparse seeded cues and layout-derived anchors. Home plays its closed/open refrigerator loops `4 dB` louder while preserving synchronized equal-power door mixing, and a dedicated source at the visible bathroom tube plays generated electrical crackle on the same applied-factor changes that drive the real flicker | `RetroSfx`, `RetroAudioService`, `RetroAmbience`, `InteriorSoundscape{Synthesis,AnchorPlanner}`, `HomeBathroomLightFlicker`, `HomeSoundscape`, `StairwellSoundscape`, scene roots and gameplay callers | Implemented |
| Home alarm clock | Keep one validated bed-relative low-poly clock/nightstand in every Home visit; reuse one 28-segment display plus two punctuation elements to flicker frozen `05:59` through the silent startup menu, switch to solid `06:00` only on Wake, then follow persistent session hours/minutes without rebuilding geometry. Generate a looping mechanical ring for the following three-second clock hold, route its fully spatial source through `SFX/World`, rattle the model and stop it before wake motion or on cleanup | `HomeAlarmClockPlan`, `HomeAlarmClockBuilder`, `HomeAlarmClockSynthesis`, `HomeAlarmClock`, `GameSessionState`, `GameAudioMixer` | Implemented |
| Road, park and open-area navigation | Constrain the player's XZ motion to an indexed union of streets, entrance aprons, park lawn, district-public ground, walkable blueprint open-land surfaces and their street approaches. Retain four continuous park entrances, reach the default beach, Lake shore and Cemetery from their street approaches, and exclude every water surface; matching colliders and the controller's `0.28 m` step offset own vertical climbing | `RoadWalkableArea.cs`, `CityParkPlan`, `CitySurfaceDescriptor`, `CityOpenAreaAccessDescriptor`, public-place descriptors, player motor | Implemented |
| City route planning | Build deterministic ordered shortest paths over generated street and park-path edges with a binary min-heap | `Runtime/Map`, `CityLayout` | Implemented |
| Player motor | Accelerate camera-relative keyboard/gamepad movement to `2.6 m/s` at `6.5 m/s²`, brake at `11 m/s²`, feed constrained displacement back into velocity and preserve heading while idle. Also expose a bounded scripted interaction approach that keeps the ordinary gait, actual constrained displacement, facing and footsteps while manual input is locked, then settles at an exact authored root position/rotation without overshoot. Require the authored Y level to match the grounded root instead of teleporting vertically; mark `1.5 s` of constrained position/rotation no-progress as stalled, and stop/reset the approach on cancellation, transition, input restoration or teleport | Input System, CharacterController, `IWalkableArea` | Implemented |
| Third-person chase camera | In City and BarInterior, keep very close `2.6 m / 53°` exterior and `2.2 m / 57°` interior framing with raised `1.4 m / 1.3 m` focus points that place the hero below frame center; use weighty damped yaw/focus with bounded lag and teleport snapping; layer subtle deterministic idle/walk motion plus percentage-driven intoxication focus/pitch/roll and balance/fall reactions over free yaw orbit; shorten immediately against obstacles and recover outward smoothly without rotating the player | `PlayerCameraFollow.cs`, `IntoxicationStageRules`, `BarMinigameModalLock`, Input System, physics | Implemented |
| Home fixed camera | Keep one Main Camera at authored position/FOV rather than following the player; hard-cut among the three unchanged MainRoom, Bathroom and Balcony poses: the approved main shot at `(-4.48, 3.00, -3.25)`, Euler `(28°, 55°, 0°)`, `64°` FOV, the bathroom shot at `(1.82, 2.20, 0.86)`, Euler `(30°, 38°, 0°)`, `92°` FOV and the balcony shot; retain the current shot through wider hold bounds, fall back to MainRoom outside specialized zones and block the bed-side camera pocket with dedicated junk. Keep the same world-oriented modular 3D hero in all shots and preserve only quarter-strength intoxication/balance/fall camera rotation around the fixed base pose | `HomeCameraShot`, `HomeCameraShotSelector`, `HomeFixedCameraController`, fixed-pose mode in `PlayerCameraFollow`, `Player3DCharacterPresentation` | Implemented |
| Home player visibility | Register logical furniture, attached dressing, door and visible balcony-rail renderer groups explicitly during Home composition; derive five camera-plane samples from the combined live 3D hero renderer bounds, then reveal a whole group when its bounds cross a camera ray to head, left/right chest or pelvis while feet-only occlusion remains natural. Move each blocking group toward its authored `0.15-0.23` minimum through one shared opaque alpha-clip dither material, hold briefly and restore smoothly after the sightline clears. Preserve Forward+ additional lights/cookies plus clipped shadow, depth and depth-normal output; preserve every collider and GameObject, exclude shell/glass/lights and force full opacity while opening, refrigerator or animated Home presentation owns the camera/player | `HomeOcclusionRegistry`, `HomeOcclusionResolver`, `HomePlayerOcclusionController`, `IPlayerPresentation.Renderers`, `HomeOccluderDither` material/shader, Home builders | Implemented |
| Modular 3D hero presentation | Instantiate one `Resources/Player/Player3D.prefab` through `PlayerFactory` in City, Bar, Supermarket, Home and Stairwell. Preserve the `1.75 m` Generic hierarchy, 73 independent mesh objects, 16 required anatomical bindings, a 31-bone armature with six non-deforming sockets, physical left/right asymmetry and one shared URP/Lit material with per-mesh palette blocks. Drive a four-second breathing/weight-shift Idle and an eight-phase one-second Walk with independent elbow, knee and ankle articulation; damp-blend them from real planar velocity over `0.14 s` in and `0.20 s` out while gait cadence follows the visible blend. Drive deterministic face bones plus additive intoxication/balance poses, keep Animator root motion off and sample left/right Fall/Down/Rise actions without moving the physical player root | `Player3DResources`, `Player3DAssetRegistry`, `Player3DCharacterPresentation`, `PlayerPresentation`, `PlayerFactory`, `Assets/Player3D` | Implemented |
| Continuous 3D player interactions | Capture separate authored entry root/pelvis/facing, action pelvis and exit root/pelvis/facing poses; keep the ordinary mesh and contact shadow visible during the grounded constrained `Positioning` approach, hold one neutral endpoint render frame, then deterministically sample registered `Enter -> Loop -> Exit` Generic clips on the same rig. Sample clip time before aligning its pelvis anchor, reject unreachable entry height, cancel stalled/transitioned/lifecycle-aborted approaches with owned restoration, hold the terminal exit pose and defer input unlock through the final ordinary `LateUpdate`. Root motion, Animator transitions, Animation Events, renderer swaps and opacity fades own no gameplay transaction | `PlayerAnimatedInteractionTimeline`, `PlayerAnimatedInteractionController`, `IPlayerClipPresentation`, `Player3DCharacterPresentation`, `PlayerMotor`, `PlayerRuntime` | Implemented |
| Home bed sleep | Place exactly one reachable trigger on the bed's open `xMax` side; on the first interactive `E`, visibly guide the 3D hero to the grounded entry, hold neutral for one rendered frame and sample `BedEnter` on the continuous rig. Loop `BedSleepLoop` with the existing breathing holds until a second `E` samples `BedExit`, including the terminal pose, then restore at the independent exit root/pelvis/facing. Keep startup sleeping as a direct loop begin before the first gameplay frame, retain the opening-only exit multiplier, localized prompts, pillow alignment and lifecycle cancellation | `HomeBedInteractionPlan`, `HomeBedInteraction`, `PlayerAnimatedInteractionController`, `Resources/Player/Player3D.prefab`, localization catalogs | Implemented |
| Home balcony smoking | At the Home-local balcony point around `(6.60, 0.04, -1.45)`, visibly guide the 3D hero to a grounded city-facing entry, hold neutral for one rendered frame and sample `SmokeEnter`, `SmokeLoop` and `SmokeExit` on the same world rig. Attach the cigarette to the registered right-hand socket, preserve the held `9.5 s` melancholic loop and calm-boundary queued exit, and restore the independent exit pose without a renderer/shadow handoff. Preserve the `38°` city-biased quadratic camera path, bounded continuous drift, exact two-second shot restoration and optional interaction-local music envelopes | `HomeBalconySmokingPlan`, `HomeBalconySmokingInteraction`, `HomeBalconySmokingTimeline`, `HomeBalconySmokingCameraDrift`, `HomeSmokingMusicPlayer`, `Player3DAssetRegistry`, `HomeInteriorRoot`, localization catalogs | Implemented |
| Home refrigerator | Derive one prominent worn refrigerator, clear player-width approach, trigger, first-person camera pose, sound/light anchors and eight stable storage slots from the Home kitchen plan; split the physical counter around its footprint and move the table `0.30 m` deeper to preserve the route; compose a hollow lined cabinet with three shelves, drawer, two door bins, frost/grime/rust and initial vodka, egg and open-stew occupants. Register each occupied slot through a pure item catalog and item view with localized name/description, preview transform, renderers and a tight non-blocking selection trigger. On interaction, capture modal ownership, keep the world 3D hero visible through the unscaled Bezier approach, then acquire an owner-scoped renderer/contact-shadow visibility lease exactly when a camera-local right-arm subset from the production prefab appears; animate the handle, seal and `102°` door and hold the lit open state. While the ordinary interactor is suspended, bind the visible close prompt directly to the same guarded `RequestClose` path used by keyboard/gamepad input. Within that lock, support pointer hover plus keyboard/gamepad selection, then fly the chosen item through `Browsing -> FlyingIn -> Inspecting -> FlyingOut` to a slowly rotating centered preview over a dark backdrop with localized title, description and `Take`/`Use`/`Back`. `Take` atomically records the source, adds the stack, unregisters/removes the model and keeps the slot empty after reconstruction; `Use` remains unavailable. Return an untaken item before close and restore its exact hierarchy/transform/collider/colors on normal return or cleanup; dispose the subset and visibility lease to restore the world presentation, fixed shot, input and HUD. Crossfade synchronized closed/open loops with equal-power gains and use an emissive strip/halo instead of another realtime `Light` | `HomeRefrigerator{Plan,WorldBuilder,View,Interaction,InteractionTimeline,FirstPersonHand}`, `Player3DFirstPersonSubset`, `PlayerPresentationVisibility`, `HomeRefrigeratorItem{Catalog,View,InspectionController,InspectionTimeline,InspectionView}`, `HomeRefrigeratorInventoryAdapter`, `GameSessionState`, `HomeInterior{LayoutPlanner,WorldBuilder,Root}`, `HomeSoundscape`, `RetroSfx`, `RetroUiTheme`, localization catalogs | Implemented |
| Player shadows | Let every production hero mesh cast and receive ordinary URP lighting/shadows from the real 3D pose in all five gameplay roots. Retain one shared analytic contact quad at the grounded root; derive its planted width/depth/alpha from presentation metrics and expand, rotate and offset it during left/right fall clips without moving the physical root or enabling practical-light shadows | `Player3DCharacterPresentation`, `PlayerContactShadow`, `PlayerPresentationMetrics`, `PlayerShadowResources`, `PlayerContactShadow.shader`, scene directional lights | Implemented |
| Interaction/UI | Select nearby bar/home entrances, exits, the Home bed, balcony-smoking dock, refrigerator and inventory-backed targets, activity stations and the ordinary-drink counter; show localized contextual world prompts as full pointer-click targets whose callbacks repeat the same availability guards and action path as E/Enter/gamepad South, including the smoking exit and refrigerator-owned modal close. Let temporary target feedback replace and disable the ordinary prompt for its bounded duration; retain refrigerator-item hover names and action-only modal buttons without persistent key-binding guides or control-hint footers, and use the appropriate state-preserving interaction or modal input ownership through the crisp retro theme | `PlayerInteractor`, `InteractionPromptView`, `InventoryTargetInteractionController`, `Runtime/Interaction`, `Runtime/UI`, `RetroUiTheme`, localization catalogs | Implemented |
| Bar minigame catalog | Own one explicit ordered set of activity IDs, localized labels/prompts and factories used by both normal interiors and debug launches; currently register cocktail mixing, beer pong, Split the G and Tinctures in a Row without game-specific debug-window code | `BarMinigameCatalog`, `BarMinigameDefinition`, `IBarMinigame` | Implemented |
| F9 debug window | List every registered game in `City` and `BarInterior`, close a conflicting map, minigame or pre-commit bar browser before taking the modal lock, refuse to interrupt a committed physical drink service, adjust the real session intoxication in clamped `-20/+20` steps while preserving drink context, and launch isolated minigame instances. In City, also toggle a scene-local test-teleport map mode without changing normal route state | `MinigameDebugWindow`, `BarMinigameCatalog`, `GameSessionState`, `BarMinigameModalLock`, `CityMapController`, scene roots | Implemented |
| Structured session diagnostics | Write one bounded UTF-8 NDJSON stream with build/session/scene/seed context, correlated transitions and minigame runs, state-change/result events and Unity warnings/errors; rotate at 5 MiB, keep three archives, default tests to off, and expose `F8` snapshot plus `Shift+F8` directory access | `Runtime/Diagnostics`, instrumented state boundaries, `MinigameDebugWindow`, `ai/debug-log.md` | Implemented |
| Bar activity routing | Assign the first four stable row-major bars to cocktail mixing, beer pong, Split the G and Tinctures in a Row through one pure resolver, fall back to cocktails for later bars, preserve the activity through transition, then resolve its registered factory | `BarActivityKind`, `BarActivityAssignment`, `BarMinigameCatalog`, `BuildingLot`, `BarEntrance`, `GameSessionState` | Implemented |
| City map UI | Project the centered blueprint map bounds and draw only canonical active surfaces, including distinct park, beach, water, lake-shore and cemetery colors. Preserve a readable `22 px/cell` logical scale; clip overflow, focus on the player when opened and pan independently on X/Y through WASD, right stick, wheel/Shift+wheel or middle/right drag, with indicators only on overflowing axes. Place every area label on a real member cell so sparse or concave shapes do not label empty space; retain player/bars, labeled home, grocery marker, visited/route state and four default public-place markers/legend. In F9 test-teleport mode, make every canonical lot selectable by pointer or ordered keyboard focus, replace the route panel with `Teleport? / Yes`, and move the hero to the confirmed lot's frontage return point (or nearest generated route fallback) while facing its door; ordinary route semantics remain unchanged when the mode is off | `CityMapController.cs`, `CityMapView.cs`, `CityMapViewport.cs`, `MinigameDebugWindow.cs`, `BuildingLot.ReturnPosition`, localization catalogs, `RetroUiTheme`, Input System | Implemented |
| Scene transition | Keep one guard across the full city/interior transfer, load `DoorTransition` in Single mode, preload the requested destination with activation blocked, and activate it only after the door sequence reaches its final blackout | `SceneTransitionService`, `SceneIds`, `GameSessionState`, Unity async scene loading | Implemented |
| Door transition presentation | Run a deterministic `3.15 s` unscaled fixed-camera sequence in a black void: reveal, turn the handle, swing the low-poly door outward toward the player over a solid black doorway sprite, push the camera and fade fully to black; tint the door lighting warm when entering and cold when exiting, with generated latch and hinge-creak cues | `DoorTransitionRoot`, `DoorTransitionTimeline`, `DoorTransitionDirection`, `RuntimeSceneSetup`, `RetroSfx` | Implemented |
| Session state | Reset a complete new run on launch and preserve the selected stable city-blueprint ID with its seed, active bar/activity, ordered route, visited-bar set, explicit bar/home return kind, stairwell side, consumed Home arrival kind, frozen/running game time plus day index, a `$999` default cash wallet, zero-default hunger/stress, intoxication, last alcohol, consumed-drink count and deterministic balance delay/sequence. City and the bounded Home exterior resolve the same blueprint ID and seed | `GameSessionState.cs`, `CityBlueprintCatalog`, `GameTimeState`, `CityReturnKind`, `HomeArrivalKind` | Implemented |
| Bar drink retail and physical service | Offer nine localized ordinary drinks at fixed integer prices through a natural seated-eye first-person counter scene in every bar; hide and exactly restore the green order markers across the modal lifecycle; map the full row to nine individual 3D bottles with solid colliders, selection triggers, kinematic rigidbodies and mouth anchors; validate affordability and maximum intoxication before one atomic cash/drink commit, then reject exit while left/right camera-local arm subsets filtered from the production player prefab pick up the selected bottle, pour through a world-space stream, drink from the matching tumbler, pint, wine glass, shot glass or snifter for exactly three seconds and return the empty vessel to the counter; acquire/release the owner-scoped world-presentation lease exactly with those subsets, return to persistent browsing for repeat orders and reserve camera return plus exact presentation restoration for Exit or lifecycle teardown without refunding a committed purchase; keep water non-sobering and exclude special Moonshine | `BarDrinkCatalog`, `BarDrinkPresentationCatalog`, `DrinkPurchaseRules`, `BarDrinkService{Plan,Timeline,WorldBuilder,View}`, `BarDrink{Bottle,Vessel}View`, `BarDrinkFirstPersonArms`, `Player3DFirstPersonSubset`, `PlayerPresentationVisibility`, `BarDrinkShopController`, `BarDrinkShopView`, `BarCounterStation`, `GameSessionState` | Implemented |
| Cocktail domain | Define four bases and ingredient compatibility; generate a deterministic seven-item shelf with four compatible additions and three traps; score and advance a three-round session | `Runtime/Cocktails`, persisted `DrinkId` values | Implemented |
| Cocktail minigame | Run same-scene modal base/addition/serve input, commit drinking state after every serving, report completion through the shared minigame contract, retain bad-mix score/intoxication penalties and terminate at the explicit `MaxIntoxicationReached` outcome without a separate timed status | `CocktailMinigameController.cs`, `CocktailMinigameView.cs`, `IBarMinigame` | Implemented |
| Cocktail presentation | Slice a 4x4 pixel-art atlas into IMGUI UV cells and animate ingredient travel/tilt, pouring, glass fill, success sparks, bad bubbles, shake, stage results and final rank in a responsive retro layout | `CocktailSpriteLibrary.cs`, `CocktailMinigameView.cs`, `RetroUiTheme`, `Resources/Cocktails` | Implemented |
| Beer-pong domain | Simulate an aimed ball at fixed 120 Hz with swept table/cup-mouth contacts, rim and table restitution, six standing cups, ten throws, clean/bank scoring, early-clear bonus and terminal outcomes | `Runtime/BeerPong/BeerPong{Types,TableLayout,Physics,Session}.cs` | Implemented |
| Beer-pong minigame | Run a modal 2D second-bar activity, project physical x/y/z coordinates onto the table backdrop, persist a light-beer penalty after each miss and report terminal completion to the interior root | `BeerPongMinigameController`, `BeerPongMinigameView`, `BeerPongProjection` | Implemented |
| Beer-pong presentation | Draw the point-filtered 640x360 table and 4x4 ball/hand/cup/effect atlas, projected shadows, cup reactions, compact aim/power feedback and localized results | `BeerPongSpriteLibrary`, `Resources/BeerPong`, `RetroUiTheme` | Implemented |
| Split the G domain | Advance a normalized pint from countdown through one irreversible held sip, settling and scored result; preserve frame-chunk invariance, five tolerance bands, up to three fresh glasses and the best session result | `SplitTheGSettings`, `SplitTheGSession`, `SplitTheGScoring` | Implemented |
| Split the G minigame | Run the modal third-bar hold/release activity for Space, LMB and gamepad South; hide the exact level while drinking/settling, persist the actual dark-beer fraction immediately and complete on Continue, glass three or maximum intoxication | `SplitTheGMinigameController`, `BarMinigameModalLock`, `GameSessionState` | Implemented |
| Split the G presentation | Draw a point-filtered 640x360 bar backdrop and 4x4 pint/hand/foam/effect atlas with glass tilt, target line, obscured liquid, settling foam, localized result cards and generated gulp SFX | `SplitTheGMinigameView`, `SplitTheGSpriteLibrary`, `Resources/SplitTheG`, `RetroUiTheme`, `RetroSfx` | Implemented |
| Tincture-match domain | Generate a seeded match-free `7x7` board with five normal flavors, at least three legal swaps and at most one `XXX`; resolve unique clears, gravity, refills, cascades up to `x5`, 15 accepted moves and deterministic dead-board reshuffles | `Runtime/TinctureMatch`, `TinctureMatchGenerator`, `TinctureMatchResolver`, `TinctureMatchSession` | Implemented |
| Tincture-match minigame | Run the modal fourth-bar board for mouse click/drag, keyboard and gamepad; reject invalid swaps without spending a move, expose `XXX` flavor clears, and immediately persist only an activated `Moonshine` for +24 intoxication | `TinctureMatchMinigameController`, `BarMinigameModalLock`, `DrinkRules`, `GameSessionState` | Implemented |
| Tincture-match presentation | Draw the logical `640x360` view with a point-filtered backdrop and 4x4 atlas of five symbol-coded shots, literal `XXX` moonshine and swap/clear/cascade/reshuffle effects; localize RU/EN feedback and play generated swap, match and burst SFX | `TinctureMatchMinigameView`, `TinctureMatchSpriteLibrary`, `Resources/TinctureMatch`, `RetroUiTheme`, `RetroSfx` | Implemented |
| Intoxication stages and presentation | Map `0–100` into Sober plus five named 20-point ranges, interpolate movement speed from `1.00` to `0.70`, drive escalating 3D pelvis/chest/arm/leg bone poses plus camera/world-composite effects in City, BarInterior, SupermarketInterior, HomeInterior and StairwellInterior, show a crisp five-segment named HUD only above zero, and lower the session level during free unscaled gameplay with recovery accelerating from about 12 seconds per point at maximum to 3 seconds per point near sober; pause recovery under modal ownership so committed modal snapshots remain authoritative and reset the presentation to neutral on cleanup | `IntoxicationStageRules`, `GameSessionState`, `IntoxicationStatusController`, `Player3DCharacterPresentation`, `IntoxicationRenderState`, `IntoxicationHudView`, scene roots | Implemented |
| Balance checks and falling | Above `60`, preserve a seeded interval/sequence across scene loads, simulate an inertial arrow at fixed `120 Hz`, render a `140°` overhead arc with shrinking green sector and risk meter, accept arrows/A-D/D-pad/stick without disabling locomotion during warning/active play, and only on failure stop the motor while modal ownership samples the registered left/right `Fall`, `Down` and `Rise` Generic actions over the existing `0.45 + 1.20 + 1.00 s` phases. Keep the upright physical root stationary, move the analytic contact patch with the signed direction and restore the neutral rig on completion/cleanup | `BalanceChallengeModel`, `IntoxicationStatusController`, `BalanceCheckView`, `BarMinigameModalLockOptions.BalanceCheck`, `PlayerFallAnimationTimeline`, `Player3DCharacterPresentation`, `PlayerContactShadow` | Implemented |
| Bar interior layout and world | Generate and validate one deterministic `22 x 16 x 4.8 m` shared layout with seven zones, four connected clear paths, aligned blocking footprints, one reachable activity fixture and one reachable counter station; compose the shell, long backlit counter, five ordinary stools plus the physical drink-service stool, booths, four social tables, stage, entrance details and activity-specific set dressing from that plan | `BarInteriorLayout{Plan,Planner,Validator}`, `BarInteriorWorldBuilder`, `BarDrinkServiceWorldBuilder`, `BarInteriorRoot` | Implemented |
| Player home interior | Generate and validate one compact `10 x 8 x 3.4 m` main-room/bathroom plan with clear entry, main and bathroom-access paths; keep six main-room furniture footprints—including blocking camera-corner junk and the table position that clears the refrigerator approach—plus toilet, shower, sink and the separate alarm-clock dressing out of protected circulation; compose the impoverished, cluttered old alcoholic's flat and full tiled bathroom at runtime, then replace the right-wall blackout with a real window and open glazed door connected to a walkable balcony at `4.7 m` street elevation; open-looking rails retain invisible safety colliders | `HomeInteriorLayout{Plan,Planner,Validator}`, `HomeBalconyLayout{Plan,Planner,Validator}`, `HomeAlarmClockPlan`, `HomeRefrigeratorPlan`, `HomeInteriorWorldBuilder`, `HomeBalconyWorldBuilder`, `HomeBathroomBuilder`, `HomeInteriorDressingBuilder`, `HomeInteriorRoot`, `HomeEntrance`, `HomeExit` | Implemented |
| Player-home stairwell | Route every home visit through a separate side-aware `8.6 x 9.6 x 6.25 m` stairwell scene: connect the street lobby, middle landing and apartment door with 48 visible steps over three seam-free ramp colliders and radius-safe overlapping navigation corridors; block the flight above the hero's floor across its full width and standing height; dress the space with stained concrete, rusty rails, pipes, ventilation, cabinets, grilles, trash and visible debris; hard-cut between three height-selected fixed shots with hysteresis, each framing an exposed suspended HDR tube/halo and stronger flickering practical pool; retain the green desaturated grade, bounded dust, concrete bed, spatial ventilation/electrical layers, seeded industrial cues under a dark reverb/echo profile and optional scene-local `stairwell_theme` slot | `StairwellLayout{Plan,Planner,Validator}`, `StairwellWorldBuilder`, `StairwellDressingBuilder`, `StairwellInteriorRoot`, `StairwellFixedCameraController`, `StairwellInteriorAtmosphere`, `StairwellAmbiencePlayer`, `StairwellSoundscape`, `StairwellMusicPlayer`, `GameAudioMixer`, `Stairwell{StreetExit,ApartmentEntrance}`, `GameSessionState` | Implemented |
| Stairwell cat | Perch one seated pixel-art cat with its back to the camera on the `Middle Landing Back Rail`; keep that NPC sprite aligned to every fixed-camera plane, select a head turn toward the player and run ordinary idle plus rare eight-frame grooming. On interaction, open the reusable Talk/Interact target menu: Talk preserves the response; Interact without stew shows missing-item feedback; Interact with stew asks default-No `Feed the cat?`. Yes validates the player's registered `CatFeedEnter/Loop/Exit` clips and the cat feeding atlas before atomically consuming one `OpenStewCan`, then visibly guides the 3D hero to the grounded middle-shot entry root/pelvis/facing; stale height or a stalled route cancels without presentation. Exact alignment holds neutral for one rendered frame, then samples the continuous player clips and starts the cat's top-first 16-frame sprite track at the player loop. The terminal hero pose precedes restoration at the independent exit; cat idle/look, modal ownership and all presentation state restore on exit or abnormal cleanup without resurrecting the consumed source | `StairwellCat{Plan,Actor,SpriteLibrary,LookSelector,IdleModel,FeedingPlan,FeedingTimeline,FeedingSpriteLibrary}`, `StairwellCatInteraction`, `InventoryTargetInteractionController`, `PlayerAnimatedInteractionController`, `Player3DCharacterPresentation`, `GameSessionState`, `Resources/Stairwell/Cat/StairwellCatFeedingAtlas` | Implemented |
| Home exterior context | Reconstruct a bounded view of the actual player-home street from the same session blueprint ID and seed inside `HomeInterior`: transform nearby roads, lots, stable windows, lamps, signals and the same city-decoration descriptors/recipes into Home-local coordinates while omitting gameplay/collision and never creating a second City root, player or camera. Share City's Lit ground/road/facade/window recipe and passive bar-front geometry; only the Balcony shot activates the retained fixture result as a bounded local street/bar light pool, scaled by the current night factor | `HomeExteriorContextPlan`, `HomeExteriorContextPlanner`, `HomeExteriorViewBuilder`, `CityBlueprintCatalog`, `CityExteriorAppearance`, `CityBarFacadeWorldBuilder`, `CityDecorationWorldBuilder`, `PlayerHomeBalconyGeometry`, `CityNightFixturePlanner`, `HomeDayNightController` | Implemented |
| Player home atmosphere | Keep MainRoom and Bathroom fog-free and post-processed under a weaker hard-shadow directional/ambient base; retain exactly two bounded shadowless practical lights, each physically co-located with a visible HDR emitter and depth-tested halo—the dirty-yellow hanging lamp and cold bathroom tube. Keep the bathroom point pool, emitter and halo on their deterministic unscaled flicker, and blend only the shadowed window Spot from cold night to warm daylight. When the Balcony shot is active, apply the shared time-of-day lighting and night-fixture factor while temporarily retaining City's exact fog/background/`48 m` cap, runtime grade and 36-particle local fog field; restore the captured Home state on every indoor transition, disable or destroy. Also own at most 12 shared-material dust motes, a calm room bed, synchronized closed/open spatial refrigerator loops, balcony night air, sparse domestic cues and a short damped no-echo profile | `RuntimeSceneSetup`, `HomeDayNightController`, `HomeInteriorAtmosphere`, `HomeBalconyExteriorAtmosphere`, `CityNightAtmosphere`, `CityFogField`, `HomeBathroomLight{Fixture,Flicker}`, `HomeBalconyResources`, `HomeWindowGlass.shader`, `HomeAmbiencePlayer`, `HomeSoundscape`, `GameAudioMixer`, `CityLightHalo` | Implemented |
| Bar crowd | Plan 12 stable role-based NPCs under a cap of 14, slice one shared transparent 3x2 pixel atlas, run centralized `8 Hz` decisions plus lightweight per-frame poses/walking, billboard after the chase camera and sort whole NPCs before/behind the 3D player by camera depth | `Runtime/Bar/NPC`, `Resources/Bar/Npc/BarNpcAtlas.png`, bar layout anchors | Implemented |
| Bar cinematic atmosphere | Run six shadowless warm/cool practical lights, bar-only URP post-processing, bounded dust, scene-local spatial crowd/rare cues and a collision-checked skippable `1.35 s` quadratic single-camera arrival reveal; preserve DoorTransition without post-processing and restore follow/orbit state before any modal opens | `BarInteriorAtmosphere`, `BarSoundscape`, `BarArrival{Timeline,Presentation}`, `RuntimeSceneSetup`, bar light/audio anchors | Implemented |
| Automated tests | Build-scene order, new-session and one-shot Home arrival state, opening timeline/menu lifecycle, alarm placement/synthesis/routing/cleanup, prompt callback lifecycle plus generic, smoking-exit and refrigerator-modal click dispatch, balcony-smoking dock/facing/clip/music/camera, bounded deterministic drift, continuous phase clock, queued-exit exact restoration, inventory catalog/stacks/navigation/new-run reset/world-source idempotence/modal exclusion/exact restoration/five-root installation, reusable inventory-target safe defaults, stale-requirement/atomic-consumption and lifecycle cleanup, cat Talk/missing/No/Yes plus continuous-player/cat-track synchronization, production Blender manifest/model/animation import, shared prefab registry, independent anatomical meshes, ordinary 3D locomotion/face/status/fall/contact-shadow presentation, contextual 3D clip sampling/pelvis alignment/cleanup, first-person subset/material/visibility restoration and dedicated 3D portrait, refrigerator geometry/slots/item catalog/selection triggers, bar bottles/vessels/transactions, deterministic Home/City/Stairwell plans, atmosphere/audio, pause, minigames, PS1 GPU presentation and scene round trips | `Assets/Tests`, `tools/build-player-3d-model.py` validators | Implemented |
| Automated test audio guard | Silence global listener output for every EditMode and PlayMode run without pausing sources or DSP, then restore the previous volume | `BarPromenade.TestSupport`, `AutomaticTestAudioMute` | Implemented |

Primary intended flow:

```text
build index 0 -> MainMenu -> BeginNewGame + HomeArrival.OpeningSleep
                           -> HomeInterior sleeping opening
                              -> clock shot: periodic 05:59 flicker + 5 s locked silence
                              -> same silent 05:59 shot: Wake Up/Quit
                              -> Wake: solid 06:00 + start session clock
                                 -> scaled time: 24 h per 1440 real seconds
                                 -> Home clock + City/Home day/night lighting
                              -> 3 s alarm hold
                              -> alarm stops -> smooth 6 s camera/wake
                              -> ordinary Home control
                              -> StairwellInterior -> City
blueprint ID + seed -> immutable blueprint -> sparse layout validation
             -> UrbanBuilt / NonUrbanOpen areas + stable IDs
             -> fixed central park + north beach/water
             -> default Lake/Cemetery surfaces + accesses + landmarks
             -> typed active-cell surfaces -> world builder + city map
             -> road-grid areas -> district-specific world
             -> one nearest eligible Residential supermarket
                -> branded storefront + walkable approach + return point
             -> four district public lots
                -> full-width street accesses -> fence openings
                -> public ground/approaches -> indexed navigation -> player
                -> fixture reservations -> lamps/signals stay clear
                -> physical public-place recipes
                -> bounded same-blueprint/seed Home exterior reconstruction
                -> open map lots + kind-specific POI markers/legend
             -> indexed street/park/open-land navigation -> player
                -> water excluded
             -> weighted route planner -> district-aware city map
             -> street perimeter -> building/park/public/open-area-aware fences
             -> night fixture plan -> chunked lamps + signals
five gameplay roots -> PlayerFactory -> Resources/Player/Player3D.prefab
                                      -> Generic Idle/Walk + face/status bones
                                      -> mesh shadows + analytic contact patch
                                      -> continuous bed/smoke/cat/fall actions
                                      -> camera-local bar/fridge arm subsets
inventory -> dedicated Player3DPortrait
player + lamp anchors -> bounded light pool -> light halos
player + seed -> player-following local fog field
player -> interaction -> activity-aware scene transition <-> session state
                    -> DoorTransition scene -> preloaded destination
                    -> supermarket layout -> validated 16x11x3.6 shop
                                           -> three finite physical shelves
                                              -> noodles + day-old loaf
                                              -> vodka + closed stew can
                                              -> chicken egg
                                           -> decorative checkout + cashier
                                           -> shelf fixed-view browser
                                              -> atomic cash/inventory/source purchase
                                              -> immediate physical removal
                                              -> sold source filtered on re-entry
                    -> home layout plan -> validation -> runtime home world
                                       -> bathroom + collider-free dressing
                                       -> refrigerator plan + clear approach
                                          -> split counter + worn hollow cabinet
                                          -> 6 shelf slots + 2 door slots
                                          -> vodka / egg / open stew can
                                             -> item catalog + registered item views
                                             -> hover tint + localized cursor name
                                          -> modal first-person open/inspect/close
                                             + production-prefab right-arm subset
                                             + world-visibility lease
                                             + light / synchronized spatial hums
                                             -> nested PS1 item inspection
                                                -> fly to center + dark backdrop + rotation
                                                -> name/description + Take/Use/Back placeholders
                                                -> exact item restore before outer close
                                       -> balcony plan -> window/open door
                                          -> connected deck at 4.7 m street elevation
                                          -> open rails + invisible safety colliders
                                          -> smoking entry/exit dock around (6.60, 0.04, -1.45)
                                             -> first E: guided walk + city-facing +X
                                             -> neutral 3D render-frame settle
                                             -> SmokeEnter -> held SmokeLoop
                                             -> right-hand cigarette socket
                                             -> second E queues safe SmokeExit
                                             -> held terminal pose + exit-root restore
                                             -> continuous mesh/contact shadows
                                             -> 38-degree city-biased push-in
                                                -> 0.33 m +X look offset
                                                -> hero near 0.37 viewport X
                                             -> local slow drift
                                                -> continuous 13-23 s harmonics
                                                -> camera-blend envelope, no FOV pulse
                                             -> drift zero + exact smooth restore
                                             -> optional smoking_theme fade in/out
                                       -> same-blueprint/seed bounded street reconstruction
                                          -> roads/lots/windows/lamps/signals visuals
                                          -> no duplicate City gameplay/light pool
                                       -> three authored shots + hold hysteresis
                                          -> same world-oriented 3D hero
                                       -> open-side bed trigger
                                          -> grounded Positioning walk/turn
                                          -> neutral 3D render frame
                                          -> BedEnter -> BedSleepLoop -> BedExit
                                          -> reusable animated interaction
                                             + separate entry/action/exit anchors
                                             + sample then pelvis-anchor alignment
                                             + no-progress/transition cancellation
                                             + continuous rig/contact-shadow restoration
                                       -> bed-relative alarm clock/nightstand
                                          -> 3 s post-Wake spatial mechanical ring
                                          -> silent dressing after wake/normal arrival
                                       -> two aligned practical Light/emitter/halo pairs
                                          + synchronized cold shadowed bathroom-spill Spot
                                          + cold shadowed window cookie Spot
                                          + four owned local realtime lights maximum
                                          + separate scene Directional light
                                          + shared transparent glass
                                          + grade + dust + Home loop
                                       -> exit -> StairwellInterior
                                          -> street exit -> home return kind -> City
                    -> bar layout plan -> validation -> runtime world
                                       -> clear routes + activity fixture
                                       -> six practical lights + local grade
                                       -> 12-NPC crowd + spatial soundscape
                                       -> skippable single-camera reveal
                    -> generic bar station -> minigame catalog
                                           -> cocktail rules/offers
                                           or beer-pong physics/session
                                           or Split the G timer/scoring/session
                                           or tincture board/cascades/XXX
                                           -> drinking progress -> session state
                                           -> completed visit -> city map
                    -> counter station -> physical nine-bottle browser
                                       -> seated first-person camera
                                          + prefab-derived left/right arm subsets
                                          + world-visibility lease
                                       -> atomic cash + drinking transaction
                                       -> bottle pickup + matching 3D vessel
                                       -> world-space pour/fill + 3 s drink
                                       -> vessel return -> persistent browser
                                       -> explicit Exit -> camera return
stairwell middle rail -> StairwellCatActor -> fixed-camera rear-view billboard
                                             + player-tracking head
                                             + idle / rare 8-frame grooming
                      -> InventoryTargetInteractionController
                         -> Talk -> existing localized response
                         -> Interact -> OpenStewCan check
                            -> missing feedback or default-No confirmation
                            -> validate player clips + cat sprite track
                            -> atomic one-can removal
                            -> middle-shot player enter
                               -> CatFeedEnter/Loop/Exit + cat feeding sprite
                               -> terminal pose + exact cleanup
session intoxication -> five-stage profile -> motor + 3D bones + camera
                                      -> PS1 world distortion
                                      -> above 60 -> deterministic balance QTE
                                         -> success or visual fall/recovery
F9 -> close conflicting modal -> session intoxication +/-20
                              -> minigame catalog -> isolated debug instance
                              -> City test-teleport toggle -> map lot selection
                                                           -> confirm -> player motor teleport
F8 -> structured state snapshot -> immediate debug.log flush
session/scene/gameplay result boundaries -> correlated rotating debug.log
scene root -> GameAudioMixer scene snapshot
           -> matching scene-local music player -> wait for streamed clip data
                                                -> Music -> fade in
Single transition -> preload destination -> fade current music out
                                      -> hold activation -> activate destination
           -> matching procedural ambience player -> Ambience/Beds
           -> Home spatial soundscape -> synchronized closed/open fridge loops
                                      -> equal-power door crossfade -> Ambience/Details
                                      -> bathroom flicker edge -> tube crackle
           -> Home music -> Balcony shot -> fade out + pause
                         -> indoor shot -> same-sample fade-in resume
           -> Stairwell spatial soundscape -----------------------> Ambience/Details
gameplay/UI events -> RetroAudioService -> bounded pools -> SFX/World, SFX/Gameplay or UI
Music/details/world sends -> scene-tuned reverb/echo returns -> Master
URP post-processing -> averaged low-resolution image -> subtle RGB555 blend -> point upscale
world composite -> crisp retro IMGUI overlay
```
