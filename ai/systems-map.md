# Systems map

An index, , not a specification. One row per system: what it guarantees, where its
code lives, and whether it is real.

Detail belongs elsewhere and is not repeated here:

- normative decisions and exact tuning values: `ai/architecture-notes.md`;
- current end-to-end behaviour: `ai/project-overview.md`;
- file-by-file layout: `ai/system-tree.md`;
- player-visible history: `ai/release-notes.md`.

## Status vocabulary

| Status | Meaning |
| --- | --- |
| `Current` | Implemented and verified in the repository. |
| `Partial` | Implemented, but a named part of *this system's own* intent is missing. The row must state the gap. |
| `Planned` | Intended; no implementation in the repository. |
| `Deferred` | Explicitly outside the present milestone; see `ai/project-overview.md`. |

A row never carries a status outside this table. Product-level scope cuts
(save data, economy, weather extras beyond rain) are `Deferred` in
`ai/project-overview.md` and do not make an otherwise complete system
`Partial`.

## Systems

| System | Guarantee | Key files | Status |
| --- | --- | --- | --- |
| Mountain Road Blender misc wave 1 | Eight kinds / `102` placements use `19` passive Blender meshes in `12` batches; the plan retains placement, semantic roots, collision and sounds. | `MountainRoadMiscAssetProvider`, `MountainRoadWorldBuilder`, `MountainRoadMiscAssetSetup`, `build-mountain-road-misc-3d-model.py` | `Current` |
| Mountain Road composition rhythm | Three hairpins, the bridge and terminal approach reserve local forest openings while far mass keeps the horizon shut; five unequal, footprint-cleared debris bands replace even spacing. Outer-bank rails, visible outward kerbs and the upper-shelf chair are plan-validated. | `MountainRoadCompositionRules`, `MountainRoad{Planner,Validator,SceneryMeshFactory,SurfaceMeshFactory}` | `Current` |
| Mountain terminal cafe | Passive fixed-metre Blender set (`51` meshes / `4,970` tris / `45` anchors / six dynamic props), including the full wall header and continuous fascia above its open door; six neutral semantic detail sheets, exact 17-collider plan shell, seven stools and ten-clip silent drink/wipe/walk/pour cast. Three patron cups drain/refill; the hero faces the counter but is never served. | `MountainRoadCafe*`, `MountainRoadCafeAssetSetup`, `build-mountain-road-cafe-3d-model.py`, art bible §10f | `Current` |
| City Blender misc catalog | CityMisc `4.9.0`: `82` kinds / `122` assemblies / `259` passive role meshes / `46,542` triangles; plans retain placement, collision and dynamics. | `CityMiscAssetProvider{,.LateCatalog}`, affected City builders, `CityMiscAssetSetup`, `build-city-misc-3d-model.py` | `Current` |
| Nightlife arch shelter | The traversable `[10;5]`–`[11;5]` gap retains its guarded service terrace, sparse survival tableau, one five-part barrel fire/light/crackle and roof-only rain shelter. Fifteen shell/prop renderers now carry measured surface albedos, and the three silent residents are textured Hero-Avatar `NpcHumanV2` prefabs with independent restrained loops; they remain colliderless, non-reactive and non-interactive. | `CityArchShelter{Plan,Placement,Planner,Validator,WorldBuilder,Presentation,SurfaceAppearance,Resident*}`, `CityRainField` | Current |
| City ground water network | Flush municipal ironwork on the `Roadside` anchor: a gutter grate with a valve lid every `~52 m`, a welded-shut standpipe over a dry trough every `~150 m`. Collision tier `None`, so nothing here is an obstacle and the walk mask is untouched. | `CityDecorationPlanner`, `CityDecorationWorldBuilder`, `CityStaticCollisionBuilder`, `CityMiscAssetProvider` | `Current` |
| District ground | The buildable ground is one mesh per district instead of one for the whole city, each carrying a wear cast resolved from `CityDistrictArtProfile.Wear` — the channel's first consumer. Seamless by construction: surfaces cut on `26 m` cell edges, which are street centrelines. | `CityWorldBuilder.BuildDistrictGround`, `CityExteriorAppearance.ResolveDistrictGroundTint`, `CityTerrainSurfaceAreaFilter` | `Current` |
| City Blender ordinary buildings | Every ordinary lot uses its district's fixed-metre v2 wrapper (`28` semantic meshes / `194` UV2 window slots total). Six opaque roles bind `24` district sheets through side-atlas, per-face `0..1` or metric UVs; glass keeps slot-driven emission. The generator rejects competing exterior coplanar faces and broad opaque layers closer than `0.03 m`, foundations are inset `0.08 m`, and landmark/core dressing own complementary roof/facade surfaces. Home reuses whole prototypes and keeps the clipped fallback only at the apartment half-space seam. | `CityBuilding{AssetRegistry,AssetProvider,PrototypePlacement,PrototypeWorldBuilder,SurfaceAppearance,WindowSlotAppearance}`, `CityDecoration{Planner,Validator}`, `CityWorldBuilder`, `HomeExteriorViewBuilder`, `build-city-buildings-3d-model.py`, `build-city-building-surface-textures.py`, `city_building_coplanarity.py` | `Current` |
| City Blender low-rise landmarks | Bar, supermarket and 209-1-inspired `player_home_exterior_v1` are complete passive semantic exteriors with inset foundations. The home keeps a `13 x 12 x 8.8 m` body, outward `2.3 m` gallery and exactly one lit street window; Unity retains collision and gameplay anchors. | `CityBarFacadeWorldBuilder`, `CitySupermarketFacadeWorldBuilder`, `CityPlayerHomeExteriorWorldBuilder`, the three asset registries | `Current` |
| Unity URP foundation | Eleven build scenes and PC renderer settings with an explicit project-owned Neutral/Bloom/Vignette baseline volume instead of a sample-scene profile. | Unity `6000.5.10f1`, URP `17.5.0`, `PC_{RPAsset,Renderer}.asset`, `PCPresentationBaselineVolumeProfile.asset` | Current |
| PS1 presentation | Footprint-average the post-processed frame to `640x360` (or a centered 4:3 crop to `480x360`), apply intoxication warp/ghost/chroma, Bayer-dither and 35% RGB555, then point-upscale under scanlines. An opt-in project-owned Lit vertex snap adds polygon jitter to world geometry while preview and reflection cameras stay stable. | `Runtime/Rendering`, `IntoxicationRenderState`, `Ps1Composite`, `Ps1Lit`, `Ps1VertexJitter`, `Ps1PresentationProfile`, `GraphicsEffectsSettings` | Current |
| Depth of field tiers | Subtle always-on Gaussian far blur in every scene grade (city `8-28 m` inside the fog band) and a shared priority-10 Bokeh volume for modal close-ups (bar counter, fridge, graves, board games, bus seat, arrivals), both behind one player toggle. | `RuntimeSceneSetup.AddGaussianDepthOfField`, `DepthOfFieldSettingsBinder`, `CinematicDepthOfField`, `CityNoirVolumeProfile`, `*Atmosphere` | Current |
| Runtime area composition | Exact eleven-scene bootstrap with eight gameplay roots; `MountainRoad`, `AreaLoading`, `ChurchInterior` and `AlpineVillage` are appended at indices `7`/`8`/`9`/`10`. | `Runtime/Core`, `Runtime/Scenes`, `SceneIds`, `ProjectSceneSetup` | Current |
| Startup waking opening | Fresh run: frozen `05:59`, five-second ordinary-input lock, Wake Up/Quit, then a continuous wake. Home F9 may instead start `06:00`, enter City at home and open the debug-teleport map. | `MainMenuRoot`, `HomeOpening{Controller,Timeline}`, `HomeDebugCityMapShortcut`, `CityGameRoot`, `GameSessionState` | Current |
| Session clock and day/night rules | Persistent scaled clock from `06:00` at one game minute per real second (`24 h = 1440 s`); midnight advances an unbounded calendar day, while night/dawn/day/dusk sampling survives scene loads and stops at `timeScale = 0`. | `GameTimeState`, `GameTimeRuntime`, `GameTimeDayNightRules`, `GameSessionState` | Current |
| Session day/time displays | The Home alarm clock shows `HH:MM`; inventory shows `DAY N · HH:MM`, and one queued overlay announces day 1 after Wake and each later midnight once gameplay is unobstructed. | `HomeAlarmClock`, `InventoryView`, `GameDayAnnouncementView`, `GameSessionState` | Current |
| Gameplay pause menu | Escape/Start takes the shared modal lock in all eight gameplay scenes, freezes scaled time, pauses gameplay audio and draws a localized Resume/Options/Start Over/Quit menu with default-No confirmation. The Options page toggles six graphics settings live (four effects default-on; the 4:3 pillarbox and the vertex jitter default-off), persisted through `PlayerPrefs` (`graphics.*`, the project's first use). The list is longer than the panel, so it scrolls: the window follows the selection and the wheel nudges it, and rows are declared in one table rather than hand-numbered at each call site. | `PauseMenu{Model,Controller}`, `GraphicsEffectsSettings`, `BarMinigameModalLock`, `RetroAudioService`, scene roots | Current |
| Hero inventory | Localized fullscreen item screen under the shared modal lock in all eight gameplay scenes: needs bars, cash, production V2 portrait, stack grid, live rotating 3D item preview and contextual Eat/Drink/Examine. Stacks and world-source IDs persist across scene loads. | `Inventory{Types,State,MenuModel,ItemModelFactory,ConsumableCatalog,Controller,View,IconLibrary,ItemPreviewRenderer}`, `Resources/Player/Player3DV2Portrait`, `GameSessionState` | Current |
| Hunger, stress, fatigue and consumption | Clamped `0-100` session needs driven by the same scaled clock (hunger over `1440` game minutes, fatigue over `1080`); food, alcohol and completed bed rest each commit relief exactly once. **Gap:** the values drive no gameplay debuff yet. | `PlayerNeedsProgressionState`, `PlayerNeedsRules`, `InventoryConsumableCatalog`, `DrinkRules`, `GameSessionState` | Partial |
| Inventory-backed target interactions | Reusable Talk/Interact target menu with a single-stack item requirement, default-No confirmation and atomic one-shot consumption that refunds a thrown startup. | `InventoryItemRequirement`, `InventoryTargetInteraction{Definition,Model,Controller}`, `IInventoryTargetInteractionHandler`, `PlayerInteractor` | Current |
| Quest log and journal | Catalog-driven session quests (`FeedTheCat` auto-active on a new game, completed exactly once when the cat feeding begins); J/Select opens a localized journal under the shared modal lock in all eight gameplay scenes; a quest-reserved item refuses ordinary eating with an "I'm not hungry yet" line. | `Quest{Types,LogState}`, `JournalController`, `GameSessionState`, `StairwellCatInteraction`, `InventoryController` | Current |
| Stairwell quest descent gate | While `FeedTheCat` is active, crossing `0.35 m` below the cat's landing on the way down shows the Silent Hill style "feed the cat first" line and walks the hero back up to the landing facing the cat before input returns. | `StairwellQuestDescentBlocker`, `PlayerMotor.MoveTowardsInteractionPose`, `InteractionPromptView`, `StairwellInteriorRoot` | Current |
| City blueprint and layout model | One immutable stable-ID blueprint per session: connected sparse cells, typed areas, one Residential bar across from home, supermarket and public lots. Explicit custom layouts retain multi-bar graph separation. The default outer Street circuit is mandatory across both road bridges; interior loops stay seeded. | `CityBlueprint`, `CityBlueprintCatalog`, `CityLayoutGenerator`, `CityLayout`, `CitySurfacePlan`, `CityDistrict`, `CityTravelDistance` | Current |
| City elevation and exterior stairs | One deterministic river-valley plan gives default road nodes about `8.1 m` of range with a peak near `10.08 m`; ordinary ground and beach use one continuous sampled terrain contract, the sea stays at datum `0`, and every urban district keeps one guarded stair street. Declared decks may cross non-walkable water; the south tunnel alone adds a bounded single-level interior stub. | `CityElevation{Plan,Planner,Validator,Rebaser,StairPlacement}`, `CityTerrainSurfacePlan`, `CityTerrainSurfaceWorldBuilder`, `CityExteriorStair{Plan,Planner,Validator,WorldBuilder}`, `CityRoadGroundBoundaryPlan`, `CityTerrainSafetyWorldBuilder` | Current |
| City mountain boundary and open tunnel | `default-coastal` closes west/south around the non-traversable river cave and one gate-free `8 x 5.5 m` portal. A collidered `12 m` entry leads into a `72 m` opaque faceted shell with no rear cap; its westward bend and far-plane contract hide the end, while portal sheltering gives the player-following rain a dry core, clears local fog particles and hides the camera-relative ridge shell. | `CityMountainBoundary*`, `CityMountainBackdrop*`, `CityMountainSurfaceAppearance`, `CityTunnelShelterController`, `CityMountain{Physical,Backdrop}.shader` | Current |
| South tunnel travel stub | Walking `8 m` inward shows one localized refusal and visibly guides the ordinary rig back to `6.5 m`, with crossing rearm and owned cleanup. What does cross is the Ferryman's car (see below): the refusal needs no gate for it, because `CanEngage` already requires `Motor.InputEnabled` and a seated passenger has none. **Gap:** on foot the portal is still a dead end by design — there is no walking route to the Mountain Road. | `CityTunnelTravel{Plan,Planner,Controller,CrossingModel}`, `InteractionPromptView`, `PlayerMotor` | Partial |
| Mountain Road area | `620 m` / `26.1 m` car-scale climb: ten R`7.5 m` hairpins at ≤`8%` grade and a `50 m` bridge over a ≥`25 m` gorge, then a level `20 m` terrace run onto the same `42 x 27 m` terminal, which is held `4 m` clear of every climbing stretch. A colliderless asphalt pocket reuses shared physics; the `76 m` terrain envelope grounds perimeter ridges clear of road/trees. The terminal is now a dressed transfer yard rather than a turning circle with two landmarks on it: `85` parts in ten batches over existing sheets and existing tints, a `0.66 m` retaining wall with two three-riser flights onto a terrace, a `1.02 m` parapet standing `0.35 m` inside the rim (which is what finally makes the mask's own clamp invisible), a cut rock face along the east rim under the cable, a bench and the cafe's middle empty stool on the shared city sit offer, one mercury practical the atmosphere owns, four more causal sounds and a third map landmark. **The mountain opens once**: `MountainRoadTerrainSampler.ApplyBrinkFall` cuts a `-27` degree wedge (`9` plus `3` of taper) `26 m` down from the back rim, applied to the FINAL height AFTER the plateau's exterior blend — earlier and that blend lifts the cliff back to pad height over exactly the twelve metres the cliff is made of — while the interior early return keeps the pad, the seam and the drivable surface untouched by construction. The bearing was measured, not chosen: swept from the rim the ridges leave `-44` to `-10` clear inside the `120 m` far plane and stand shoulder to shoulder either side, so nothing had to be moved and they became its jambs. Through it, `MountainRoadVista*` hangs a matte at `81-105 m` on the lighthouse island's two existing shaders — valley bed, the switchback he came up, a grain of city and a horizon at eye level, all measured from `y = 0`, the height of the tunnel mouth — with its windows lit by `NightFactor` through the same per-minute apply that moves the sun, and not one Light in it. Silent four-role cafe/cableway remain. The climb is driven: `LastRouteMountainDrivePlanner` reads the route out at a metre into one `LastRouteCarDrivePath` from six metres inside the tunnel to the middle of the apron, so it inherits `TunnelToCafe_IsOneUnbrokenDrivableSurface`'s own corridor proof, and the car parks nose-in without turning — the cafe's nearest corner stands `8.24 m` from the apron centre against a validated `8.05 m`, so no U-turn of a usable radius fits. While the car is driving it burns two shadow-casting beams and a wide spill, switched by the journey itself (`LastRouteCarHeadlights.Follow`) and ramped `1.2 s` up / `2.5 s` down; the area keeps its ORDINARY grade at every hour underneath them. A build that instead took the sun, ambient, reflection and fog out for the duration — so the beams were the only light in the world — came up as a black frame with two blown-white pools and was pulled on sight; `MountainRoadRideGrade` and its plumbing are gone, and the two lighting files are byte-identical to what they were before it. The forest bends: crowns wear a second URP Lit clone that displaces in all four passes including ShadowCaster, and both wind and snow strengthen with altitude. | `MountainRoad*`, `MountainRoadBridge*`, `MountainRoadTerminal*`, `MountainRoadTerminalSite{Plan,Planner,Validator,WorldBuilder}`, `MountainRoadVista{Plan,Planner,Resources,WorldBuilder,LightsController}`, `MountainRoadSeatPlanner`, `LastRouteFerrymanTalkInteraction`, `MountainRoadCafe*`, `MountainCableway*`, `LastRouteMountainDrivePlanner`, `MountainRoadWeather{Rules,Shaper}`, `MountainRoadWindDriver`, `LastRouteCarHeadlights`, `Ps1LitFoliage.shader` | Current |
| Village above the cableway | Freely walkable uphill snow bowl. Route distance drives `0.45 m` off-route snow and side/rear curtains; a house aperture preserves the landmark rhythm. Stable village-only ridge handoff fixes distant crawl. The snow is a colliderless skin — fitted ribbons at `RibbonCrossStep 0.4 m` (a coarser pitch BRIDGED crossing routes) plus a `1 m` sheet, buried a cell into each other — and it **presses down**: one float per vertex, a `0.55 m` stamp, refill tied to snowfall, `10 Hz` re-upload; no RT and no vertex displacement, which would cost a third verbatim `Ps1Lit` clone against a snap that quantises the amplitude. `IPlayerFootstepSurface` hands the motor's step to the area, which picks `FootstepSnow`/`FootstepSoil` by visible depth and throws a mote burst — the split is what makes a route audible. Lane skin and path ribbons sample ground at EVERY vertex and ride `LaneSkinLift 0.08`: laid flat at a centreline height they were cut open by curving ground (`423/2490` probes, worst `0.44 m`), which read as snow on the street and was not. | `AlpineVillage{SnowPlan,SnowTreading,SnowKickup,PeripheralStorm*,RidgeAppearance,WorldBuilder}`, `AlpineVillageRoot`, `IPlayerFootstepSurface`, `RetroSfx` | Partial — mother, dinner and chapel interior are unwritten. |
| The cableway carries | Boarding at the mountain terminal opens: the gate swings back and the faded "boarding closed" sign stays hanging over it, because nobody took it down. Pressing `E` brakes the line to a stop with a cabin ON the boarding point — `v = cruise * sqrt(d / brake)` against the distance REMAINING, final step clamped to it, so the dock is exact to a centimetre at any frame rate rather than an asymptote nobody can seat a passenger against; a cabin already sitting there is sent round the whole loop, because the offer is "the next one". The controller had to stop deriving position from `elapsedSeconds * CabinSpeed` and accumulate distance instead, which is what makes any of it possible. **Boarding is outboard of the outbound track**: the gap between the two tracks is `1.15 m` and the bullwheel's own pedestal foot fills it, so the first dock sat inside a pillar — physics shoved the hero clear at spawn and he then walked at a point inside solid steel, with one `entry was blocked` line to say so, and only PlayMode found it. The platform's height is derived from the cabin's hang so the step is a fixed `0.42 m` at both terminals, measured to the skirt top and not the box underside (`0.40 m` lower, which would restore the climb it removes); it stands `0.85 m` proud of the pad, over the hero's step offset, so it carries three treads. The cabin's doorway is an aperture by omission — no leaf, therefore no door-phase contract, therefore the bus's stock boarding clips. The passenger rides from `MountainCablewayController.Moved`, raised in the same call that posed the cabins, and is never reparented. The far end only ARMS in `Awake` and holds under an already-black screen until `IsTransitioning` clears plus one frame; the seat plan is re-solved against that station before anything reads it, and the loop resumes through `BeginPositionedLoop`. The two terminals differ by kind: drive below with motor, reducer and shaft; tension carriage and weight stack above, with no motor voice at all. `F10` skips the climb through the black. **The line has no end to see.** Twice the far end was dressed to be looked at - a snow ridge planted across the rope, then a gallery the rope ran into - and both were a visible END on a journey meant to feel long, which the capture series (`AreaCaptureFixture.MountainCableway`) showed plainly. Both lines are `230 m` now: the rope runs on past each scene's own draw range (`120 m` on the road, `110 m` in the village, both far clearer than the city's `48`) and the far turn is clipped before it is drawn, from the platform and the seat alike. The cut is a plain `RideCutDistance 73 m` mid-span (`6 m` clear of any tower, `157 m` of rope still ahead against `HiddenRunMargin 10` over the far plane), `LastVisibleDistance` is that constant and the ride controller is untouched. `MountainRoadPlanner.StandsAcrossTheLine` drops any perimeter ridge whose drawn crest would reach within `3 m` of the cabin on either track, so the rings part where the rope goes through and the terminal validator refuses a plan where one did not; the far turn is a bare bullwheel (`BuildFarTurnBeyondTheHaze`); eight cabins on a `470 m` loop; the village's cut is a `7 + 12 m` valley that follows the rope to the end of the mesh and never closes. | `AlpineCableway{RidePlan,CabinSeat,RideController,RideFactory}`, `MountainCableway{Controller,DriveRules,WorldBuilder}`, `MountainRoadCablewayPlan`, `MountainRoadRidgeGeometry`, `AlpineVillage{Planner,TerrainSampler}`, `MountainRoad{Planner,TerminalPlanner,TerminalValidator,SceneryMeshFactory}`, `GameSessionState.IsRidingAVehicle`, `LastRouteRide{FadeView,SkipHintView}` | Current |
| City river and embankments | A `10 m` north-south channel preserves all 144 lots. Two `3 m` promenades, three typed bridges and four lower landings own the waterside route; at the south, water and bed continue over `48 m` behind the mountain while both promenades stay walkable from `Z=-156` to collidered rock stops at `Z=-182`. The cave beyond is never walkable. Low wall lanterns at `13 m` pitch keep their emissive lenses and fog halos lit day and night; sparse upper promenade lamps and pooled realtime spill now ride the §20 fixture floor — always burning, breathing between two thirds by day and full at night. Shared measured solids and the hand-written URP water shader retain the existing current, depth, foam and weather treatment; the shader defaults are the river's wave tune (`0.08 / 4.8 m` — the old `3.4 m` put the second train under the 1 m grid's Nyquist and it aliased into noise), read through the same slope-gain/facet/crest shading as the sea. | `CityRiver{Definition,Plan,Planner,Resources,SurfaceAppearance,WorldBuilder}`, `CityWaterSurfaceFactory`, `CityRiverWater.shader`, `SurfaceAppearanceCore`, `CityBlueprintCatalog`, `CityLayout`, `RoadWalkableArea` | Current |
| Yard wheelchair rider | One isolated `pipeback_roller_v1` NPC drifts forever around the yard site contract's authored ring — ridden on bare ground, nothing drawn for the lap — in a ragged ground-locked push-and-coast rhythm (`1.35 m` cycle, `1.42x` surge to `0.62x` coast). The presentation adopts the static wheel/caster meshes under their authored `PIVOT_*` anchors, so tyres, rims, spokes and casters roll with covered distance (differential and forward roll sign included) while `PipebackIdle`/`PipebackRoll` animate the skeletal rider and the bellows pumps on the push phase. A serialized provider keeps the prefab outside `Resources` and the pedestrian pool. | `YardWheelchair{Motion,Plan,Presentation,Actor,Factory,Provider}`, `Resources/City/YardWheelchairProvider.asset`, `CityGameRoot` | Current |
| Bar-side yard composition | The walkable roadless gap immediately left of the home-frontage bar holds the dead tree and sparse traces, plus a phone booth and dumpster leaning on the bar wall clear of the rider's circuit via shared reserved-ground anchors. Nothing is drawn for the circuit itself: the chair rides bare ground on the yard site contract's authored ring. Its dead lamp stays dead; one deliberately unreal cold `240`-intensity Spot on the neighbouring supermarket keeps the full circuit inside a bright inner cone, casts hard `0.95` shadows day and night outside `NightFactor`, has no volumetric beam and never tracks the rider. | `CityOpenAreaDecoration{Plan,Planner}`, `CityOpenAreaWorldBuilder`, `HomeYardUtilityPlanner`, `ai/city-zones-art-bible.md` §10a | Current |
| Cemetery precinct | Dedicated deterministic dressing of the `3 x 2` eastern cemetery: textured gravel alleys from an arched leaf-open iron gate, hash-varied graves in six monument silhouettes over three stone tints with post-grounded оградки and offerings, perimeter birches/firs, sittable timber benches beside the main alley (fed into the shared bench-sit pass via `CityWorldResult.CemeteryPlan`), and a `4-9` lamp chain of gas mantles along its whole length that burns always under the §20 fixture floor (two thirds by day, full at night, halos never removed); stone/gravel/soil batches carry four dedicated `CityCemetery*Albedo` sheets. | `CityCemetery{Plan,Planner,WorldBuilder,SurfaceAppearance}`, `CityBenchSitPlan.CreateAll`, `tools/build-cemetery-textures.py`, `ai/city-zones-art-bible.md` §10c | Current |
| Church precinct and interior | A `4 x 2` precinct immediately north of the cemetery carries a `44 x 23 x 32 m` Blender-authored neo-Gothic Catholic church whose City exterior stays at `0.55` scale and now stands at a `10 m` west-street setback. A stone approach/forecourt leads to its west door; the north lawn/garden has two sittable benches, two small trees, six clipped shrubs and two restrained beds. One maintained `3 m` opening continues the cemetery's middle cross alley into the south church path over a safe shared threshold without becoming a second street gate; cemetery NPC/work routes keep the west gate. The separate interior retains its narthex, nave, side aisles, four piers, pews, confessionals and inaccessible sanctuary/high altar, loaded from an independent prefab. | `CityChurch*`, `CityChurchCourtyardPlan`, `CityChurchCemeteryPassagePlan`, `CityCemetery{Plan,Planner,WorldBuilder}`, `CityBenchSitPlan`, `ChurchInterior*`, `Church{Entrance,Exit}`, `tools/build-church-3d-model.py`, `Resources/{Church,City}` | Current |
| Residential courtyard pockets | At most four ≤`1.05 m` facade-side pockets choose among six domestic variants, remain clear of entries/POIs/laundry, and admit at most five colliderless residents; fringe Yards admit none. | `CityCourtyardPocket{Planner,Geometry}`, `CityCourtyardResident{Plan,Factory,Presentation}`, `CityStaticCollisionBuilder` | `Current` |
| City yards | The west stone terraces alone add one grounded unoccupied mason cart; the other four typed perimeter Yards and the residual former-lake Yard add no separate work vignette, while their authored service-belt infrastructure remains. | `CityFringeYard*`, `CityFringeYardLifePlanner`, `CityMountainBoundaryMeshFactory`, `CityNightAtmosphere` | `Current` |
| District public places | One non-building public lot per default urban district at `18 m` minimum sides, with canonical bounds and full-width street approaches, built as four free-standing physical places. Five scripted POI albedos pave every public ground and fully texture the drying yard (frames, laundry cloth, bench) and the last route island (painted metal, layered poster paper, timber, cloth rags). Two night-scaled POI realtime lights: the drying yard's pole floodlight and the island's mast service floodlight. | `CityDistrictPointOfInterest{Plan,Planner,WorldBuilder}`, `CityPointOfInterestSurfaceAppearance`, `CityNightSiteLightRegistry`, `CityLandUseKind`, `CityLayoutGenerator` | Current |
| Drying yard babushkas | Three authored staged NPCs on the Residential public place: two grandmothers beat the rack's hung carpets with Soviet plastic beaters on desynchronized loops, one smokes apart watching. One shared staged model with both hand props, role-enabled; colliderless with attention magnets, outside the pedestrian pool, stances derived from the same recipe transform as the drawn carpets. | `DryingYardBabushka{Provider,Plan,Presentation,Factory}`, `CityDistrictPointOfInterestWorldBuilder.TryDescribeBabushkaStances`, `CityPedestrianAssetSetup`, `build-city-pedestrian-3d-model.py` | Current |
| Weighbridge attendants and needle | Two authored staged NPCs on the Industrial cold weighbridge: the weigher reads the indicator beside the mechanism — never across the deck axis — checking the dial, leaning to the linkage and chalking the deck edge, while the weighed worker paces the deck's long axis slaved to his clip's normalized time and stands square at centre through the authored `0.36-0.64` pause. The scale answers weight: a City-root controller eases the `Scale Needle` off its authored `28°` rest (`34°` sweep, `0.45 s` attack / `0.90 s` release) while the worker's pause or the hero stands on the walkable deck; the Home-exterior rebuild never claims the registry slot. One shared staged model, chalk enabled only for the weigher; colliderless with attention magnets, outside the pedestrian pool. | `WeighbridgeAttendant{Provider,Plan,Presentation,Factory}`, `CityWeighbridgeNeedleController`, `CityWeighbridgeIndicatorRegistry`, `CityDistrictPointOfInterestWorldBuilder.TryDescribeWeighbridge{Stances,Deck}`, `CityPedestrianAssetSetup`, `build-city-pedestrian-3d-model.py` | Current |
| Cemetery mourner | The cemetery's one scripted transient NPC: while the hero is within `28 m` of the grounds and the `180 s` cooldown has passed, a woman in deep mourning (near-black coat and veil, `988` triangles, all four palette variants dark with the bouquet as the one colour note) spawns on the street by the director's spirit — `76 m` away or outside the camera's horizontal cone — walks in through the one gate along the main alley to a deterministic random unenclosed grave, lays her bouquet at the authored offering spot, sobs for exactly `30 s`, wipes her eyes and walks out; the instance and her laid bouquet despawn together. The rite finishes even if the hero leaves; only past `88 m` unseen does it end early. Idle slot carries the single-playback `36.5 s` MournerMourn rite, walk slot the grieving gait; route, grave choice, spawn rule and phase clock are pure EditMode-tested functions. | `CemeteryMourner{Provider,Plan,Timeline,Presentation,Factory}`, `CityCemeteryMournerController`, `CityPedestrianAssetSetup`, `CityPedestrianModelImporter`, `build-city-pedestrian-3d-model.py`, `ai/city-zones-art-bible.md` §10c | Current |
| Cemetery watchman and gate lodge | A 15-part lodge (`cemetery-lodge-*`, new `Lodge` part kind reusing the concrete/timber/iron styles) stands in the gate-side pocket the planner reserves before benches, graves and trees: timber floor and roof, a watch window on the alley side, an ajar door, a stove pipe, a step and a stool; every blocking part clears the raw street-approach rectangle, and a blueprint whose gate lacks `8.3 m` of lateral room simply has no lodge. Under the eave beside the doorway, over the solid stretch of rear wall, hangs the plot's one warm fixture, the porch bulb (`cemetery-lodge-lamp`, `CityCemeteryLampKind.LodgePorch`) — the only light in the city that never switches off, dimming to a day floor through the registry's `dayIntensity` overload instead of dying with the night factor. On the doorstep beneath it an always-present staged NPC — the extremely snide old watchman (aerodrome cap, telogreika, kirza boots, grey whiskers, one raised brow; hands clasped behind his back through both authored loops) — stands 0.75 m out from the step and 0.30 m aside toward the alley, in front of the ajar leaf, facing the main alley rather than the arch behind his own booth; his stance is read back from the plan's own lodge parts. Talking to him (`E`, cashier-stub contract on its own trigger) serves the next line of a 15-quip seeded repertoire that never repeats back-to-back. | `CemeteryWatchman{Provider,Plan,Quips,Interaction,Presentation,Factory}`, `CityCemeteryPlanner.AddLodge`, `CityPedestrianAssetSetup`, `CityPedestrianModelImporter`, `build-city-pedestrian-3d-model.py`, `Resources/Localization/{ru,en}.json`, `ai/city-zones-art-bible.md` §10c | Current |
| Cemetery gravedigging | The watchman's work, and every grave of it takes four acts on one plot, every one of them worked rather than pressed. He never runs out: `CemeteryGravediggingRegister` keeps one `CemeteryGravediggingController` per grave he has ever given plus the one on offer, and after each acceptance it finds the next vacant plot outward from his post - so the offer stands again the moment the hero comes back, up to `MaximumOpenJobs` (`3`) unfinished holes at a time. Talking to him while he has a plot to point at puts the offer up in place of the prompt (`E` accepts, `Q` refuses without spending it); accepting marks out the vacant plot nearest his post with a pulsing plate, four pegs and the lit kerosene hand lamp - `CityHandLampWorldBuilder`, literally the lake pier's fixture, turned `24` degrees off the grave's axis because it was set down and not installed. The lamp arrives with the job rather than with the hole: every act is timed by its light and the first one most of all, and it is picked up with the last spadeful when the earth goes back. The gear arrives with it too: past the foot of the grave - the only clear ground left once the heap has one flank, the digger and his camera the other and the lamp the head - the coffin waits on two timber blocks with the spade driven into the ground beside it, both placed by `CemeteryGravediggingPlan.CoffinRestGround` / `SpadeRestGround` and validated clear of the worksite. There is exactly one of each: the acts borrow `CemeteryGravediggingController.Spade` and `WaitingCoffin` rather than raising their own, so the spade visibly lifts out of the ground into the work and stands back in it afterwards, and the box that waited is the box that goes down the hole. Spade and blocks live to `Coffined`, the waiting coffin to `Dug`, and the last spadeful tidies all of it away with the lamp. Interacting with that site cuts the plot's rectangle out of the cemetery slab, lines the hole with box-projected fresh earth over a floor `1.6 m` down, heaps the spoil alongside. The second interaction lowers a six-sided timber coffin with an Orthodox cross on the lid — a model of its own, turned boards rather than a box, `1.95 m` long inside a `2.30 x 1.05 m` mouth. The third fills the hole in: the cut is removed and the slab made whole again, the pit dressing and the coffin go with it, and a fresh three-course mound plus one of the four single-grave monuments — from `CityCemeteryPlanner.CreateGraveParts`, slab omitted, stone and silhouette fixed by the plot's own hash — stand in their place. The lamp goes with them: it stood there because there was a hole to see into. Its feedback line is what sends the hero back to the gate. `QuestId.DigTheGrave` is one repeatable log entry over however many holes are open: `GameSessionState.TryAdvanceGraveWork` puts it up on any `Marked` and closes it only when nothing is unfinished, and `QuestLogState.TryActivate` revives a completed repeatable quest in place rather than adding a second line. The watchman's window pays `150` per closed grave, and pays for all of them at once in a single sum before he will talk about the next plot, announced through the formatted-feedback path (`ShowFormattedFeedback` keeps the catalog key and composes the number at the view's one resolution point) — the only place the player is shown the wage, since cash otherwise lives two menus deep. Every worksite is a pure function of its own record in `CemeteryGraveWorkLedger` (stage and epitaph per plot id, held by `GameSessionState`), so the whole yard - a half-dug hole here, a finished stone there - is restored exactly on each city build; a plot id the current seed no longer has is logged and skipped rather than guessed at. An invisible cap holds the hero out of a pit he cannot climb out of. Each act runs as a modal session (`CemeteryGraveWorkController`) that takes `BarMinigameModalLock`, blends the camera onto the grave and leases the hero out of sight so the spade is the only animated thing - an accepted deviation from `ai/contextual-animation-standard.md`. Digging and filling share one `3 x 2` lattice of three courses each (`18` in all), and both are a choice of square and a press: pick a segment with `A`/`D`, press `E`, one course goes. The whole difficulty is the lattice rule - no segment may go deeper than its shallowest neighbour, which forbids pillars and cannot deadlock - and it is read in the hole rather than on a panel, because `CityCemeterySegmentFrameWorldBuilder` outlines the chosen square down there at the height its earth currently stands. There is no timing bar in either act and no kinds of ground: both had one, and both were cut at the user's direction as the same shot demanded eighteen times over a distinction the hero could neither see nor answer. The HUD for both is one line naming two keys. `CityCemeteryProgressivePitWorldBuilder` states the half-dug hole as one earth block per segment from the floor to its working face, so a stroke costs one small combined mesh and never a rebuild of the cemetery slab. The coffin comes down on four slings over the open mouth with no timber across it, paid out by the hero - `Q` the left of the shot, `E` the right, bound to screen sides rather than to head and foot because which end is which depends on the plot's heading against the digger's side. Nothing held means nothing moves. What makes it a game is that the balance point at which the box hangs level crawls as the fresh ground gives under one bearer and then the other, further than the tolerance is wide: standing still loses it because level walks out from under a box that never moved, and holding both keys loses it because the two rates differ and the point keeps going. Rope only ever goes out, so every correction is paid for in depth. The gauge draws the moving band and a needle for the balance he controls. One constraint is load bearing and stated in the settings: the point must crawl slower on average than the slower rope can move the balance, or chasing it is not hard but impossible - an earlier pass had exactly that. The stone lies flat by the head from the moment the job is taken and the lamp and spade now live to `Sealed`, because the last act is worked by the same light and driven home with the same spade: press `E` to heave the monument upright (dead weight, no window, sags when let go), then three timed blows from above set it. The camera leans toward the side the stone lies on for that, and walks round to the front of the monument for the plaque. The plaque carries three lines - a name nobody gave, a span nobody knows, and one the player types, held to eight words by `CemeteryEpitaph` and written once through `GameSessionState.TrySetGraveEpitaph` under its own grave's plot id - `CemeteryPlaqueSurface.Initialize` binds each board to the grave it stands on, so two finished stones never read the same line. The board is fixed to the stone's own measured front face - found by walking the monument's triangles down in bands from `0.74` to `0.44` of its height and taking the first band solid and wide enough to carry a plate, which is what puts it on the head of a stele and between the arms of a cross rather than on the plinth - and carries its words as world-space TextMeshPro: `CemeteryPlaqueFont` builds a dynamic-atlas font asset at runtime from `Roboto-Regular` (Apache-2.0, full Cyrillic) in `Assets/Resources/Fonts`, so anything a player can type has a letter and no generated atlas is committed. TMP Essential Resources are imported under `Assets/TextMesh Pro`, which is what `TMP_Settings` needs to exist at all. There is no field to type into: the line is taken off `Keyboard.onTextInput` and appears on the brass as it is typed, and reading a finished board takes the modal lock and brings the camera to it rather than opening a panel. The board is fitted only once the stone is upright, because `Attach` measures the monument to find its face. `CemeteryPlaqueReadInteraction` stays on the finished grave for reading it again. One session serves the whole yard: `ICemeteryGraveWorkSession.TryBegin` takes the grave as an argument and `CemeteryGraveWorkController` binds to whichever hole the hero is standing over, and to whichever finished board he stops to read, because there are several worksites and one camera. A session commits nothing until it finishes and takes back everything it built on `Esc`, so the ledger still carries only the stage; `Filled` exists as its own rung because closing a hole cannot be undone. | `CemeteryGravedigging{Plan,Controller,Register}`, `CemeteryGraveWork{Stage,Ledger}`, `ICemeteryWorkGiver`, `CemeteryGraveDigSiteInteraction`, `CityCemetery{GroundWorldBuilder,GroundExcavation,PitWorldBuilder,CoffinWorldBuilder,SealedGraveWorldBuilder}`, `CityHandLampWorldBuilder`, `InteractionPromptView.ShowFormattedFeedback`, `CityCemeteryWorldBuilder.BuildPartBatches`, `CityCemeteryPlan.Plots`, `CemeteryWatchmanInteraction`, `GameSessionState.TryEarnCash`, `CemeteryGraveWork{Controller,Stance,View}`, `ICemeteryGraveWorkSession`, `CemeteryGraveLatticeModel`, `CemeteryStrokeModel`, `CemeteryCoffinLowerModel`, `CemeteryStoneSettleModel`, `CemeteryShovelAnimator`, `CemeteryGraveSlings`, `CityGravediggerShovelWorldBuilder`, `CityCemeteryProgressivePitWorldBuilder`, `CityCemeterySegmentFrameWorldBuilder`, `CemeteryPlaque{Font,Surface}`, `BarMinigameModalLock`, `PlayerPresentationVisibility` | Current |
| Cemetery ravens | From the session's first sealed grave (`CemeteryGraveWorkLedger.FirstSealedPlotId`), two procedural synthesized-voice ravens hold the cemetery for good: one on that grave's mound crown, one on vacant ground a few steps off, heads tracking passers-by; both flush into the fog at `3.5 m`, return to the same two points at `33.6 m`, and stay silent, unstartled and neutral-eyed while a grave-work session runs. | `CemeteryRaven{Provider,RigAnchors,Factory,Plan,PoseRules,IdleModel,HeadModel,FlightModel,DirectorModel,Actor}`, `CityCemeteryRavenController`, `CemeteryRaven{CallSynthesis,Voice}`, `CemeteryGraveWorkLedger.FirstSealedPlotId`, `CityCemeterySealedGraveWorldBuilder.GetMoundCrownPoint`, `tools/build-cemetery-raven-3d-model.py` | Current |
| Outdoor raven roosts | Sparse always-already-perched pairs of the same ravens on bird-logic spots across the three outdoor scenes (City ≤`8`, road ≤`4`, village ≤`3`), planned per area with hard exclusions and per-scene fog-derived distances; they reuse the parameterized cemetery director/flight models and shared-clip voices over one refcounted caw set, deactivating beyond a per-scene radius. | `RavenRoost{Plan,Controller}`, `{City,MountainRoad,AlpineVillage}RavenRoostPlanner`, `RavenCallClipCache`, parameterized `CemeteryRaven{DirectorModel,FlightModel,Voice}` | Current |
| Seacoast precinct | Dedicated deterministic dressing of the north-edge waterfront as the place where the city runs out, zoned around the river mouth: a dead port west of it (concrete mol into the sea, frozen derrick, scattered ruins), a granite esplanade at the centre carrying the abandoned municipal boat station that moved here from the drained lake (hut with a boarded hatch and the «ПРОКАТ ЛОДОК» board, single-railed sea pier ending in the shared kerosene hand lamp, chained slipway, hauled hull row showing all four variants), and a wild east shore (rotten breakwater pile row marching into the fog, driftwood, dune grass, a lone wreck, a stranded listing barge ~13 m out, a worn bluff stair). A timber footbridge crosses the mouth on the esplanade axis; the river pours into the sea over a mouth spill (a sloped sheet of the river's own material continued from its last sheet's exact north-edge height — `CitySeacoastFrame.MouthWaterY` — down under the sea's datum, so the shared world-driven waves hide the joint and the crossing swells read as churn), and stepped sand-faced `MouthBank` runs close the channel's otherwise-open cut through the sand row from the last quay wall to the waterline. Raised decks carry validated parapet-continuity contracts (mol sides+head bridged only by its root stair; esplanade sea wall cut exactly for the pier and slipway; footbridge railed both sides), and the mol, pier and footbridge contribute walkable rects through `CitySeacoastPlanner.AppendWalkableFootprints` — the open sea is never contributed. Full-width quay thresholds adopt both `3 m` river promenades' north ends; short transverse rails close only the extra waterside lips. A coast lane joins the pedestrian 2-core graph from the street spur to an east terminal ring, and the esplanade benches join the shared bench-sit pass. Sand/concrete/plank/hull batches carry measured `CitySeacoast*Albedo` sheets; the esplanade granite reuses the river quay sheet. | `CitySeacoast{Plan,Planner,WorldBuilder,SurfaceAppearance}`, `CitySeaResources`, `CityPedestrianSeacoastPlanner`, `CitySurfSound`, `CityWaterSurfaceFactory`, `CityWaterResources`, `tools/build-city-seacoast-textures.py`, `ai/city-zones-art-bible.md` §10d | Current |
| Sea water | The sea takes the river's shader as a second material given a real travelling swell: `_WaveHeight 0.20`, `_WaveLength 14`, `_FlowDirection (0,-1)` shoreward — rollers come in at ~0.85 m/s instead of standing and breathing, the summed `1.73x` crest (0.346 m) stays inside the surface factory's raised 0.45 m culling headroom and under the pier deck's validated `+0.40` floor (both test-pinned), and a shore fade anchored by the builder at the inner shelf's seaward edge holds the trough to 0.12 m over the 0.15 m-clearance silt while ramping to full height 12 m out. The wave reads through the mattress's shading lessons moved into the shader — `_SlopeGain 2.6` steepened normals, `_FacetStrength 0.6` per-triangle facets from screen-space derivatives, `_CrestShading` value shifts and relative-crest whitecaps in the surf's own white. `CityWaterWaveModel` is the displacement's CPU mirror (contract-tested against the shader source), which is what the fisherman's float rides. Sheets are chunked ≤48 m so far ones frustum-cull, run a cosmetic 23 m apron into the fog past the map edge, and ride over a jittered two-step silt shelf whose depth sits inside `_FoamDistance`, so the depth-threshold foam draws a wandering surf line along the whole sand edge by itself. Its ripple map is the sea's own isotropic sheet (`CitySeaWaterNormal`, anisotropy-bounded like the pond recipe that proved it out), its foam borrows the river's white water, and it switches on `_AdditionalSpecular` so the pier's hand lamp lays a glitter road. Night factor and rain reach both bodies through one registry. | `CitySeaResources`, `CityWaterWaveModel`, `CityWaterResources`, `CityRiverWater.shader`, `CityRiverResources`, `tools/build-city-seacoast-textures.py` | Current |
| Lighthouse island | The abandoned fishing island offshore, at the edge of visibility on purpose: a fixed world-space rock mound `35 m` past the waterline north of the pier (the pier points at it; the sheets' 18 m apron caps how much farther it could go), carrying two ruined shacks, leaning poles, a fallen jetty, a heeled wreck and the lighthouse that left the mol head — a `~15 m` banded tower whose lantern genuinely rotates. The scene's Exp2 fog would erase ordinary geometry there, so the island renders as one baked vertex-coloured mesh on its own no-fog shader: a distance-graded haze mix toward the city fog colour (`0.75` at ≤20 m easing to `0.92` at ≥38 m — outlines only from the shore, legibly firmer from the pier head), plus a `43-47 m` camera-distance self-fade so it dissolves before the 48 m far plane can clip it — barely there from the esplanade and sand, clearer from the pier head, gone from every street. The lantern is presentation-only additive geometry (no real Light: 16 m range cannot reach the shore and the fog eats halos): a lens core over the gallery and two opposed beam cones on an additive no-fog shader, azimuth read absolutely each frame from pure seed+game-minute rules (`9°`/game-minute, flash twice per 40 s revolution, seed turns only the phase), lens surging as a beam sweeps the camera bearing, everything night-gated by hand against the site registry's factor without registering. No colliders, no walkable or bounds contribution; planner returns null without a coast plan; the map pins its dot past the chart's north edge at the true easting. Beam HDR sits at lens colour ×4 over the 0.60 bloom threshold — judgeable only in play mode. | `CityLighthouseIsland{Plan,Planner,MeshFactory,WorldBuilder,Resources}`, `CityLighthouseLanternController`, `CityLighthouseIsland.shader`, `CityLighthouseBeam.shader`, `CityMapView.DrawSeacoastLandmarks` | Current |
| Seacoast fisherman | The boat station's one permanent inhabitant, moved to the sea with the station: a staged NPC standing at the end of the pier on the rail-free side, tipped out over the end board, facing open water with his back to the shore, his stance read back from the coast plan's own deck parts and his float sitting on the sea's own top. Talking to him (`E`, cashier-stub contract on a trigger docked behind him rather than in front) serves the next of a 15-line seeded repertoire (`seacoast.fisherman.*`) that never repeats back-to-back. His register is the watchman's exact inversion and is enforced as a rule, not a mood: never the second person, never a question, at most `55` characters, weather and superstition only — the crucian became flounder and nothing else needed to change. The staged art keeps its lake-era names (`LakeFisherman3D`, `lake_fisherman_v1`): the model does not care which water it was built beside. The pipe ember, its short warm point light and the plume are all driven from the idle clip's own phase, never from a second timer; the line is struck from the live rod tip to the waterline the coast frame carries. | `SeacoastFisherman{Provider,Plan,Quips,Interaction,Presentation,Factory,RigAnchors,PipeEffect,Line}`, `CityPedestrianAssetSetup`, `tools/build-city-pedestrian-3d-model.py`, `Resources/Localization/{ru,en}.json`, `ai/city-zones-art-bible.md` §10d | Current |
| Park chess set inhabitants and wire lamp | The Central Park chess set gets its two permanent inhabitants and its one light. Two old men are seated on drawn bench planks, one per table, on seats that are each other rotated 180 degrees about the middle of the set (`-seat-a1` and `-seat-b2`): elbows on the board, head in both hands, turned toward one another across the set and neither looking up. The two remaining planks stay unclaimed and sittable, which is the point of the place — the hero takes either one across the board from its man, in past the plank end (see the game-table seat row). Each stance is read back from the same `AppendBenchSeats` call the hero's sit interaction uses, and each man claims his own seat through `CityBenchSeatClaims` for the life of the City. They are the art library's only bench sitters, so the generator grew `perch_seat_height_m` — hip underside to soles, proved equal to the drawn `0.540 m` plank across all 288 frames of both loops — and the runtime stands the seat of each coat on the timber every frame rather than pinning a sole. Chess and draughts share one board, so each reference runs on two channels re-derived from the piece: a king's tulle against one thick draught worn flat and raked back, and a bone check against bone circles run on the diagonal. Above the set a wire crosses between two park trees (across the tables, not along them: the tree grid leaves no aligned pair on that line) carrying two enamel cones, of which one still burns over the set's middle and covers both boards, weak and warm, day floor included. No talk stub yet — but see the row below: they are not silent, they simply never address the hero. | `ParkChessPlayer{Provider,Plan,Presentation,Factory}`, `ParkCheckersPlayer{Provider,Plan,Presentation,Factory}`, `CityParkChessLamp{Plan,WorldBuilder}`, `CityDecorationWorldBuilder.AppendBenchSeats`, `CityBenchSeatClaims`, `CityPedestrianAssetSetup`, `tools/build-city-pedestrian-3d-model.py`, `ai/city-zones-art-bible.md` §10 | Current |
| Park chess-set men | Both boards carry a full set of turned 3D pieces: `32` chessmen on the chess player's table in the starting position and `24` draughts on the neighbour's, three rows a side on the dark squares. Seven authored meshes (`1910` triangles for the lot) built by `tools/build-city-chess-set-3d-model.py`, sized entirely from the drawn `0.15 m` square — king `215 mm`, draught `23 mm` — imported readable and combined by the runtime into four meshes, one per colour per board, on the board's own timber sheet. Placement is proved rather than eyeballed: `a1` dark, a light square in each man's near-right corner, queen on her own colour, back rank `RNBQKBNR`, knights turned at the opponent, draughts on dark squares only. A seeded few millimetres and few degrees per man reads as a set laid out by hand; nothing ever moves. **The colouring of the drawn board was corrected here** — both men had a dark square on their right, which no empty board shows. | `CityChessBoardGeometry`, `CityChessSetPlan`, `CityChessSetWorldBuilder`, `CityChessSetProvider`, `CityChessSetAssetSetup`, `RuntimePrimitiveFactory.CreateCombinedMeshes`, `CityDecorationWorldBuilder.BuildChessBoard`, `tools/build-city-chess-set-3d-model.py` | Current |
| Park board games | Both boards are playable. Taking either free plank starts or resumes a real match on that table: full legal chess on the chess player's, full Russian draughts on the neighbour's. The hero is dark at both, because dark is what the drawn set already had nearest the free plank, so the old man opens. The camera drops to the hero's own view, leaning right over the stone (`1.06 m` over the plank and `0.34 m` forward of it, `72` degrees; yaw `+-55`, pitch `-6..75`), with the resting pitch derived as the bisector of the board's near and far edge angles so the field is centred in the frame by construction. The whole head comes off so he can look through it — by rig rule (`head`, `neck`, `face.*`) rather than by mesh name, because the head is twenty-two meshes — and nothing below the collar is ever hidden. The table's two static batches are swapped for one object per man on the same seven meshes and the same timber sheet, carried one step per jump with a lift arc, and the taken man is swept off. Picking is a plane read, not sixty-four colliders: LMB or a keyboard cursor selects, legal destinations light as dots and captures as red plates, RMB turns the head, `E` still stands up and `R` restarts a finished game. Nothing is drawn over the board. Every state a player has to know — greeting, whose move, check, promotion, a man taken, why a click was refused, the result, the offer of another game — is a line the man opposite speaks, ten cues of two lines each per board, rate-limited to one every `3 s` with the result and the offer forced through; `CityParkQuarrelController` is suppressed for the sitting, because one bubble anchor cannot carry both a game and a decade-old argument. The engines are negamax with piece-square tables (chess, depth `3`, quiescence over captures) and material/advancement (draughts, depth `5`, forced-capture extension), both capped by a node budget and both finishing with a random pick inside a slack window — which is what makes the old man beatable rather than perfect. Positions persist for the life of the City. | `ChessRules`, `ChessEngine`, `ChessMatch`, `DraughtsRules`, `DraughtsEngine`, `DraughtsMatch`, `BoardGameContracts`, `CityBoardGamePlan`, `CityBoardGamePieces`, `CityBoardGameMarkers`, `CityBoardGameController`, `CityBoardGameView`, `CityChessSetMen`, `Player3DHeadVisibility`, `CityBenchSitInteraction.SeatedChanged` | Current |
| Park chess-set quarrel | The two men hate each other on the only grounds available: one plays chess and despises draughts, the other the reverse. While the hero is within `22 m` of the middle of the set (`25 m` to stop — hysteresis), one of them shouts every `5 s`, strictly alternating, opener drawn from the city seed. The shout is an authored two-second beat (`ChessJeer`/`CheckersJeer`) mixed over the brooding loop: head out from under the hands and `64°` round to the neighbour, left arm thrown up, held, and dropped slower than it rose. Both men share one unmirrored pose because their two seats are a 180-degree rotation of each other, which puts each one's neighbour `2.2 m` ahead and `3.7 m` to his own left. The line appears the frame the clip crosses its full-extension key, types out at `34` characters a second in an IMGUI panel over the speaker's live head bone, and stays up until the neighbour answers — one bubble on screen at a time. Twelve localized lines each, seeded so neither repeats back to back. Leaving earshot cancels the beat and the bubble; returning restarts after `1.2 s`. Sitting down at either board suppresses the whole quarrel until the hero stands again. No sound. | `CityParkQuarrelController`, `ParkQuarrelTimeline`, `ParkQuarrelTaunts`, `NpcSpeechBubbleView`, `ParkChessPlayerPresentation.BeginTaunt`, `ParkCheckersPlayerPresentation.BeginTaunt`, `CityPedestrianAssetRegistry.ActionClip`, `RetroUiTheme`, `LocalizationService` | Current |
| City world builder | Build canonical surfaces plus validated sibling plans: continuous terrain, the sampled natural-ground south-west mountain-corner closure, graded streets, river, stairs, guarded drops, the fixed Nightlife arch shelter, fringe Yards and special precincts. Buildings and public slabs extend foundations downward; structural fringe masses are physical, while thin seated marks, supported wear caps and cables stay visual. | `CityWorldBuilder`, `CityTerrainSurfaceWorldBuilder`, `CityMountainBoundary*`, `CityRiverWorldBuilder`, `CityStreetSurface{Plan,Planner}`, `CityArchShelter*`, `CityFringeYard*`, `CityOpenArea{DecorationPlan,WorldBuilder}`, `RuntimePrimitiveFactory` | Current |
| City street surface presentation | Road v2's `8 m` street footprint with a `6 m` carriageway between two raised `1 m` sidewalks; Road v2.1 exposes a flush `8 x 8 m` asphalt apron at selected bus nodes, including boundary corners whose pads stand on open yard ground. Shared albedos through MPBs, no material instances. | `CityGenerationSettings`, `CityStreetSurface{Plan,Planner}`, `CityStreetIntersectionSelector`, `CityBusIntersectionSelector`, `CityExteriorAppearance` | Current |
| Central Park surfaces | Eight packaged albedos cover the whole park: lawn, path and plaza project down at their own metre pitch; trunks, canopies, hedges, benches and the four landmarks (fountain, bandstand, chess tables, playground) use box-projected world UVs. Compensated tints preserve every authored colour; the Home view repeats the path. Landmarks ride a second batch axis inside the flat decoration layer. | `CityParkSurfaceAppearance`, `CityWorldBuilder`, `CityDecorationWorldBuilder`, `CityTerrainSurfaceWorldBuilder`, `RuntimePrimitiveFactory`, `HomeExteriorViewBuilder` | Current |
| City and Home street pedestrians | One deterministic radius-safe sidewalk/zebra graph feeding a per-runtime population profile (City `8` day / `3` night over `13` pooled models, Home balcony `5` / `2` over `8`), filled in batches of two on a short cadence and dispersed by lane and peer distance. Each slot takes a seeded pick from an ordered five-archetype catalog; ordinary designs repeat; the airborne, the single lamp-bearing, the four bus-riding and the one boiling-kettle designs are declared per archetype, and the lamp design caps at one pooled instance. Fog-band spawn and recycle never depend on camera direction or frustum. | `Runtime/City/NPC`, `CityPedestrianPopulationProfile`, `CityGameRoot`, `HomeInteriorRoot`, `RoadWalkableArea`, `Resources/Pedestrians/{CityPedestrian3D,ChairCarrierPedestrian3D,KettleHatPedestrian3D,LongArmPedestrian3D,HelmetLampPedestrian3D}` | Current |
| NPC Human V2 anatomy | All `24` rigged humanoid NPCs use the exact 31-bone Hero V2 A-pose Avatar at a `0.835 m` rest pelvis. The Kettle Hat and all three shelter residents carry validated `256 px` detail atlases through shared materials; the other twenty remain flat colour. Canonical abnormal silhouettes stay explicit overlays. | `NpcHumanV2AssetSetup`, NPC model generators and seven asset pipelines | Current |
| City Route 01 bus | One immutable right-hand Street-only, grade-safe grand loop through every required precinct, with coalesced/inserted stops and one pooled real-scale actor. The bus owns a `48 m` visibility-bounded rear diesel, hero-only positional cabin/body layer, causal pneumatics at both real doorways, folding doors, driver, sprung body, night lights and rain-driven wipers. **Gap:** the runtime is City-only — Home reconstructs a static pole, and the map has no live vehicle marker. | `Runtime/Vehicles`, `CityBus{Plan,Planner,Actor,Audio,Director,Presentation,Factory,AssetRegistry}`, `CityBusTargetRoutePlanner`, `CityBusCoverageStopPlanner`, `CityBusWideTurnPlanner`, `CityMapBusOverlay` | Partial |
| Pedestrian bench rests | Every `3.5 s` one roaming walker near a free sittable bench may divert to it (p `0.4`, at most `2` at once, `30 m` graph reach): Dijkstra guidance to the bench's node, a short scripted off-network crossing (max `6 m`, capsule released like a bus doorway), a seated `15-30 s` rest on the bench anchor and the same crossing back. Seats are shared through one claim registry with the hero's sit interaction; the hero's yard bench is excluded by design. | `CityBenchRest{Plan,Planner}`, `CityBenchNpcRestController`, `CityBenchSeatClaims`, `CityPedestrianActor`, `CityBenchSitInteraction`, `CityGameRoot` | Current |
| Route 01 passengers | A three-place cabin counting the hero: he boards either fully open door into reserved window seat `07` under the authored `BusBoardEnter`/`BusRideLoop`/`BusAlightExit` and a roll-free seated camera, while up to two ambient walkers wait beside a stop pole, board seated and alight at a seeded later stop. **Gap:** fare, destination choice, passenger persistence and live tracking are out of scope. | `CityBusRide{Plan,Controller}`, `CityBusStopWait{Plan,Planner}`, `CityBusNpcPassengerController`, `CityBus{Actor,Director}`, `CityPedestrian{Actor,Presentation,Resources}` | Partial |
| City visual diversity and collision proxies | One stable ordered decoration plan over 24 visual families expanded into six shared-material styles per `48 m` chunk, with a per-kind `None`/`Detail`/`Blocking` proxy catalog for grounded structural recipes only. Phone booths and dumpsters repeat under spacing/coverage rules (`55/90 m` booths; `40 m` and `65/100 m` dumpsters) and expose recipe-derived interaction docks. The park playground is the one recipe with a moving part: its frame, cross beams, bench and two A-frame proxies are baked as usual, while its two seats hang outside the batch as hinged rigid bodies (`±50°`, ropes drawn taut from the top beam) that anything with a `CharacterController` pushes up to its own walking pace. | `CityDecoration{Descriptor,Plan,Planner,Validator,WorldBuilder}`, `CityDecorationCollisionCatalog`, `CityStaticCollisionBuilder`, `CityStreetUtilityDock`, `CityPlayground{Geometry,Swing,SwingBuilder}` | Current |
| Road-edge fences | Street-union rails where the outward side is water, unmapped or outside the active footprint, plus true Street-terminal caps and one default-only `4 m + 4 m` `CornerGuard` L at the north-east urban-core road cap beside `[12,11]`; that local guard does not continue along the yards or waterfront. River promenades support their bank roads, while bridge decks remain support-only with authored parapets/stair gaps. | `RoadFence{Planner,Plan,WorldBuilder}`, `CitySurfaceDescriptor`, `CityRiverPlan` | Current |
| Supermarket interior and finite shelf purchases | A runtime-composed `16 x 11 x 3.6 m` shop holding exactly one unit of five products; a modal fixed-camera browser commits an atomic cash/inventory/source purchase and removes the physical unit, and sold IDs stay absent until a new game. | `SupermarketInterior{LayoutPlan,LayoutPlanner,LayoutValidator,WorldBuilder,Root}`, `Supermarket{ShelfView,ProductView,ShelfShopController}`, `SupermarketPurchaseRules` | Current |
| Supermarket watcher cashier | One bespoke animation-free 3D clerk on the shared 31-bone Avatar behind the checkout: the five re-parented PIVOT_Neck segments lie along a quadratic pursuit curve, so the head — pinned to the curve tip by its authored attachment point — travels the whole hall (up to `18 m` of neck) to hover beside the hero, the sample-verified cubic staple climbing over any shelf or counter in the way; a caught glance reels it back with hysteresis, pinches the pupils and freezes the `6.5 s` blink cycle. The prefab is passive (no colliders/lights/audio) and reachable only through its provider asset; a separate talk-stub trigger answers `E` with a placeholder line. | `SupermarketCashier{Provider,AssetRegistry,Factory,Presentation,Actor,SurveillanceState,BlinkState,Interaction}`, `tools/build-supermarket-cashier-3d-model.py`, `SupermarketCashierPlan` | Current |
| Supermarket CCTV corner cameras | Four chunky ceiling-corner camera units resolved from the room plan; each head servos at `240°/s` toward the hero (aimed from the very first frame), with a fake-emissive recording LED, no colliders and no real light. | `SupermarketSecurityCamera{,WorldBuilder}`, `RuntimePrimitiveFactory`, `SupermarketInteriorRoot` | Current |
| Supermarket fluorescent atmosphere | An explicit shadowless six-practical budget over the one directional fill: a cold point under each fluorescent row, one warm accent over the checkout and cashier, a cool cold-shelf spill, and one row flickering on a deterministic stepped pattern that also dims its fake-emissive tube. | `SupermarketInteriorAtmosphere`, `SupermarketFluorescentFlicker`, `RuntimeSceneSetup.EnsureSupermarketInterior` | Current |
| City day/night presentation | Apply one time sample to directional/ambient/reflection light, windows and registered electric glow. Production uses `1` bar + `11` street Spots; the nearest practical leases one street slot (`1+10+1`, still 12), and the under-mountain tunnel exception retains a `0.22` day floor without creating a Light. | `RuntimeSceneSetup`, `CityDayNightController`, `GameTimeDayNightRules`, `CityNight*`, `CityFringePracticalAnchor`, `CityWindowAppearance`, `CityNightGlowRegistry`, `CityLightHalo` | Current |
| Deterministic exterior weather | The pure slot schedule drives rain, sound, lightning/thunder and wind in City/Home - and in the CITY it is floored: CityEternalRainShaper (the city's own ICityWeatherShaper, on the controller and the four direct readers - rain-field init, river hook, bus wipers, home balcony) keeps a 0.18 drizzle under every slot, so the city scene never stops raining, only varies, and its wet film and puddles never dissolve. The balcony's rain field stands on its own anchor half a field out from the facade - born on the facade plane, so the lens is inside it (the fog's `25.5 m` anchor had left every streak `12 m` off and fogged) - with the hero's building as a local shelter so nothing crosses the glazed bedroom. The SAME slot is re-read on the mountain road as snow through one optional `ICityWeatherShaper` hook on the controller - no second writer, no re-rolled weather - where altitude both floors and amplifies it (the summit snows even in a Clear slot, because a mountain has its own weather) and drives the swaying conifers through a shader global; one cross-scene film darkens shared surfaces and feeds at most `42` road puddles. In the open tunnel, the combined shelter provider gives the player-following rain a dry core and clears only local fog particles. Both areas above the city now carry the SAME `CityFogField` as the street - same component, same shared atmosphere material, same `36`-sheet cap and same `0.120` peak alpha; only the Exp2 haze around it stays per-area (`0.070` grey-green city, `0.026` mountain, `0.017–0.045` warm village, breathing with the raw gust rhythm — the one per-area haze that moves) - and there the weather owner clears it, off the same predicate that gives their snow its dry core, because outside the City it is already the one thing that knows whether he is under a roof. **Gap:** rain does not yet dim ambient daylight or alter the grade. | `GameWeatherRules`, `CityWeatherController`, `CityWetSurface{Rules,Registry}`, `CityPuddle{Planner,WorldBuilder}`, `CityRainField`, `CityFogField`, `CityTunnelShelterController`, `CityClothWindRegistry`, `CityRainSound`, `CityThunderSound`, `ICityWeatherShaper`, `CityEternalRainShaper`, `MountainRoadWeather{Rules,Shaper}`, `MountainRoadWindDriver`, `MountainRoadWindSound` | Partial |
| Exterior cloud ceiling | One camera-relative `220`-triangle hemisphere stays just inside each exterior far plane, follows the shared absolute-time wind and blends into existing haze. City/Home share one sky; road/village use local profiles; true interiors and non-balcony Home shots show none. | `ExteriorCloud{AssetMetadata,Profile,MotionRules,Resources,Field,CaptureCamera}`, `ExteriorCloud.shader`, exterior roots | Current |
| Runtime cloth rags | Skinned PhysX-cloth panels built at runtime (top row pinned, mesh owned through the generated-mesh owner): the simulated topology is strictly single-sided because cloth recomputes particle normals from every triangle and a reversed same-vertex duplicate cancels them into specular glitter; the back face comes from one shared cull-off clone of the primitive material with per-renderer matte MPBs. Six authored torn rags hang from the Last Route island's broken canopy — the home-exterior vista stays cloth-free — and take the weather wind through the registry; the city-wide wind dressing hangs its own pieces through the same factory under a separate root. WindZone is unused by design: PhysX cloth only reads its own acceleration vectors. | `ClothPanelFactory`, `CityClothWindRegistry`, `CityDistrictPointOfInterestWorldBuilder`, `RuntimePrimitiveFactory` | Current |
| City wind dressing | One cross-zone plan hangs up to `64` simulated cloth/rope pieces off structures the other dressers already draw — awning rags on four market stalls, scaffold shrouds on the outer guard rails, six courtyard wash lines on their own drawn poles and sagging rope, pipe-rack tarps and sling ends, eight fire-escape banners, one bandstand pennant, pier nets, mooring ends, wreath ribbons, service tarps and dead cable tails — anchors picked by stride across each district so pieces meet an ordinary walk (a street-level floor per urban district is test-pinned); every piece wind-registered, only the walk-through courtyard wash body-registered, only line poles colliding, and the bar-side yard, lighthouse island, drained-lake block, tunnel forecourt, flood works and stone terraces hanging nothing by rule. | `CityWindDressing{Plan,Planner,Validator,WorldBuilder}`, `CityRopeSpanGeometry`, `ClothPanelFactory`, `CityClothWindRegistry` | Current |
| Scene and place music | Six shipped themes use measured per-track trims and a `12 kHz` low-pass to meet one roughly `-30.5 LUFS` background target. One mixing rule still hands themes over `4 s` out then `1 s` in without overlap. **Gap:** the cemetery place-theme slot remains empty. | `MusicMix`, `SceneMusicPlayer`, `CityLocationMusic{Director,Zones}`, `HomeMusicPlayer`, `HomeSmokingMusicPlayer`, `SceneTransitionService`, `Resources/Audio/*` | Partial |
| Common audio mix | Every snapshot keeps one causal hierarchy under `-6 dB` Master: Music/Beds behind local Details, World, Gameplay and UI; compensated sends preserve each scene's reverb/echo character while dry actions move forward. | `GameAudioMixer`, `BarPromenadeAudio.mixer`, `AudioMixerAssetSetup`, scene roots | Current |
| Retro SFX and ambience | Deterministic generated mono `22050 Hz` UI/world/bar effects with bounded pools and cooldowns, plus layered Home and Stairwell soundscapes on layout-derived spatial anchors. | `RetroSfx`, `RetroAudioService`, `RetroAmbience`, `InteriorSoundscape{Synthesis,AnchorPlanner}`, `{Home,Stairwell}Soundscape` | Current |
| Causal City soundscape | Exact fixture-owned anchors drive the shared City layer; the tunnel adds one visible faulty ballast with a `5.6 m` buzz and crackle only on real light dips. All eleven cues now have a physical owner: the park swing creaks at the top of each wide enough arc, from the plank, so it only ever sounds because someone pushed it. | `CitySound{SourceDescriptor,scapePlan,scapePlanner,SchedulePlanner,Occlusion}`, `CitySoundscape{AnchorPlanner,Director}`, `CityPlaygroundSwing`, `CityTunnel{LightingController,LampSoundSynthesis}`, `CitySurfSound`, `CityThunderSound` | Current |
| Home alarm clock | One validated bed-relative clock and nightstand reusing a 28-segment display: flicker frozen `05:59`, switch to solid `06:00` only on Wake, then follow session time without rebuilding geometry. | `HomeAlarmClock{Plan,Builder,Synthesis}`, `HomeAlarmClock`, `GameSessionState`, `GameAudioMixer` | Current |
| Road, park and ground navigation | Constrain player XZ motion to canonical roads/ground/bridges/river landings and the first `11 m` of the open south tunnel; cave water, the fenced south-west infill and ground behind every other toe stay excluded. Pedestrian navigation does not enter the tunnel. | `RoadWalkableArea`, `CityGroundTraversalPlan{,ner}`, `CityRoadGroundBoundaryPlan`, `CityVerticalTraversalPlan`, `CityRiverPlan`, `CityMountainBoundaryPlan`, `CityTerrainSurfacePlan`, `PlayerMotor` | Current |
| City route planning | Deterministic ordered shortest paths over generated street and park-path edges with a binary min-heap. | `Runtime/Map`, `CityLayout` | Current |
| Player motor | Tank controls: forward is `2.6 m/s` walk or `4.2 m/s` run while either Shift/L3 is held; backpedal stays `1.4 m/s`, A/D yaw stays `150°/s`, and existing acceleration/braking plus intoxication feed actual constrained motion into the Idle/Walk/Run blend. Fatigue adds no debuff; scripted approaches always walk. | `PlayerMotor`, `Player3DCharacterPresentation`, Input System, `CharacterController`, `IWalkableArea` | Current |
| Third-person chase camera | Close exterior/interior framing with damped yaw, bounded `-20°..55°` pitch and focus; RMB mouse, right-stick or arrow-key input orbits on both axes (walking is WASD-only; the seated board game keeps the arrows for its cursor), while idle motion, status reactions and obstacle shortening never rotate the player. | `PlayerCameraFollow`, `IntoxicationStageRules`, `BarMinigameModalLock` | Current |
| Home fixed camera | Three authored MainRoom/Bathroom/Balcony poses hard-cut with hold hysteresis instead of following the player; only quarter-strength status rotation around the fixed base pose. | `HomeCameraShot{,Selector}`, `HomeFixedCameraController`, `PlayerCameraFollow` | Current |
| Home player visibility | Explicitly registered furniture, door and rail renderer groups dither toward an authored alpha floor when they cross a camera ray to the hero, then restore; every collider and GameObject stays intact. | `HomeOcclusion{Registry,Resolver}`, `HomePlayerOcclusionController`, `HomeOccluderDither` material/shader | Current |
| Modular 3D hero presentation | Production `Player3DV2.prefab` in all eight gameplay roots: `1.75 m`, 34 parts / 1,984 triangles, 31 bones, adult 7.4946-head anatomy, five-state face atlas and painted open olive field jacket/boots, driving the shared Idle/Walk, contextual, status, fall and grounded-boot presentation. | `Player3DVariant.ProductionV2`, `Player3D{Resources,AssetRegistry,CharacterPresentation,FaceAtlasPresenter,RagdollController}`, `PlayerFactory`, `Assets/Player3D/V2`, `tools/build-player-3d-model-v2.py` | Current |
| Retained Hero V1 fallback | The byte-frozen 73-part burgundy/strap `Player3D.prefab` and portrait remain packaged and selectable only through explicit `Player3DVariant.ProductionV1`; ordinary gameplay, first-person subsets and inventory select V2. | `Player3DVariant.ProductionV1`, `Resources/Player/Player3D`, `Assets/Player3D`, `tools/build-player-3d-model.py` | Current |
| Silent Hill attention | The hero's head turns toward noticeable interactables, pedestrians and magnet-marked characters inside a `3.6 m`/`±75°` cone with wider release hysteresis, people-first ordering and no overhead targets (a focus above `2.1 m` over his feet is never noticeable); the glance is an additive post-animation head/neck turn (`62/38`, yaw `±68°`, pitch `-32°..+10°`) that stands down under modal clips, handoffs and the ragdoll. | `PlayerAttention{Rules,Controller,Magnet}`, `Player3DCharacterPresentation`, `PlayerFactory`, `YardWheelchairFactory` | Current |
| Continuous 3D player interactions | The shared `Positioning -> Enter -> Loop -> Exit` path with independent authored entry/action/exit poses, sample-then-align pelvis anchors, moving-target support and owned idempotent cleanup. Ordinary location doors use it for a planted `DoorUse` lean/right-hand press before committing the transition. Normative contract: `ai/contextual-animation-standard.md`. | `PlayerAnimatedInteraction{Timeline,Controller}`, `PlayerDoorAction{Plan,Controller,Target}`, `IPlayerClipPresentation`, `Player3DCharacterPresentation`, `PlayerMotor` | Current |
| Bed sleep and wake | The only contextual action authored on eased curves with staggered keys, and the only one whose furniture is built around the pose instead of beside it. The `3.75 s` `BedEnter` and `6.0 s` `BedExit` lead with the pelvis and legs and let the chest, head, arms and face arrive a few frames behind; the sleep loop breathes asymmetrically off the mattress rather than into it. Waking is a sit-up with four separate beats — half-crouch on the mattress, right leg over the near edge, then the left, then stand — and a dedicated key holds the left leg up until the right boot is down. Because a clip owns the rig ungrounded, the generator measures how far the supine back, the lifted head and the seated weight hang below the pelvis bone, `PlayerCharacterDimensions` mirrors those three numbers, and the mattress, pillow, blanket and crumpled shirt are then placed against them. The mattress and pillow tops are deformable vertex grids: they dent under the sleeper by his parts' actual penetration (full sink under the torso, shallower under feet), the sleeping hip descends by the same `BedSleeperSinkDepth` so he lies in the dent, and the dent lingers through the whole wake and refills over ~1.5 s behind the rising body — including after a cancelled sleep. The tops are faceted per-cell quads with steepened dent normals, because a smooth bowl of this depth is invisible on the noisy albedos (capture-verified). The bedside seat takes no dent (boot-pinned). **Gap:** the lie-down's roll onto the side is unasserted — the shared pelvis transition carries one waypoint, not a support profile. | `HomeBedInteraction{,Plan}`, `PlayerCharacterDimensions`, `HomeInteriorWorldBuilder.BuildBed`, `tools/build-player-3d-model-v2.py` + shared `build-player-3d-model.py` `validate_bed_support_contract`, `HomeBedDressingGeometryTests` | Current |
| City bench and park game-table seats | One trigger per sittable plank in the City — yard, park, cemetery, points of interest, bus shelters and street decorations — all derived from the drawn timber and shared with pedestrian rest claims. The eight ordinary park planks are split four per region, derived from real ParkPath runs and carry one facing through mesh, collision and the path-height sit dock. An ordinary plank is backed onto from the front under the bus seat's clips. The two free park game planks instead dock off the plank end and offer chess or draughts. | `CityParkBenchPlanner`, `CityBenchSit{Plan,WorldBuilder}`, `CityBenchSitInteraction`, `CityChessBoardGeometry`, `CityStaticCollisionBuilder`, `PlayerAnimatedInteractionController` | Current |
| The Ferryman's car and its passenger seat | The night district's last route island parks one authored saloon per seed — nose out at a way in, off the paved circle, absent rather than nudged when nothing fits — with the one man left on the island perched on its bonnet throwing a coin. Saying yes to his single question plays a scene rather than a fade: he shoves off the metal onto the lot, the body rocks on its springs, he walks round the nose and opens his own door on its real hinge. Only then is the seat beside him offered, and the hero takes it with the same choreography hands-mirrored — handle, pull, step back under the swing, in under the roofline with a hand on the frame, down, and the leaf hauled shut. Both leaves are pure functions of the clip that is pulling them, never of a second timer. **And so is the BODY.** `PlayerAnimatedInteractionPelvisTransition` carries a hold and a settle either side of its waypoint, and the hero's four are keys of his own clip: he stands at the dock until `door_clear 0.34`, reaches the doorway at `seat_step 0.52`, is down on the seat at `seat_down 0.78` and pulls the leaf shut sitting in it. Without the hold the pelvis travelled from the clip's FIRST frame — he was `0.905 m`, ninety per cent of the way to the doorway, by the time the leaf finished opening, so he walked through a door he was at the same time miming a pull on, while the Ferryman beside him waited for his (`TravelStartPhase 0.36`). Without the settle he was still sliding into the seat while the clip already had him sitting in it. Outward the same rule holds him down until the leaf is shoved open at `0.24`; the leaf is deliberately still closing as he walks away, because that is what a person does. Seated, the camera becomes his eyes at `0.78 m` over the seat anchor and `0.12` forward, `62` degrees, `+-105` of yaw so he can turn to the driver, with the whole head hidden by rig rule; it is taken as his hips leave the doorway and given back a third of the way through standing up. One warm `3.30 m` lamp post is fitted in front of the car and turned back down the bonnet, keeping a daytime floor because he has to be lit whenever anybody walks up. **The dash in front of him is a dash now**, composed in the generator around a glovebox aperture (the doorway's aperture-by-omission rule, with its own hole check) and dressed with a binnacle whose speedometer needle follows `LastRouteCarDriver.Speed`, vents, an ashtray, a tunnel with the gear lever and handbrake, pedals, a column stalk, the ignition key, visors and a mirror stalk — `63` meshes, `2880` triangles. Two things on it answer him: the radio, split down the middle into a power knob (driver's side, the dial lights through a property block on a slot whose emission keyword stays ON) and an eight-detent tuning knob that slides the needle along the dial, and the glovebox lid on a hinge at the aperture's bottom edge, dropped `78` degrees towards him clear of his knees and EMPTY. The radio is silent by decision; only the clicks are heard, through the car's own cue source. Selection is by GAZE, not by the interactor's sphere: his capsule is still standing at the door dock outside the car, so nothing on the dash could ever be inside `1.65 m` of it; the seat's one interactable evaluates its own camera, casts a ray at the drawn bounds of the bezel and the lid, and answers for whatever it lands on — before its own height check, or the mountain leg (`26 m` above the island dock) would refuse silently — while "stand up" is offered only when he is looking at none of it. The look limit went `42 → 50` degrees down so the lid is reachable. State lives on `GameSessionState.CarDashboard`, because the tunnel raises a NEW car. | `LastRouteCar{Plan,Factory,Doors,Suspension}`, `LastRouteCarDashboard{,State,Target,Gaze}`, `LastRouteCarRadioModel`, `LastRouteCarGloveboxTimeline`, `LastRouteCarSeat{Plan,ViewPlan,Interaction}`, `LastRouteFerryman{Plan,Factory,Presentation,BoardingPlan,BoardingTimeline,Coin,Coat}`, `CityDistrictPointOfInterestWorldBuilder.TryDescribeFerryman{CarStance,LampStance}`, `Player3DHeadVisibility`, `PlayerAnimatedInteractionController` | Current |
| The last route | The car goes. The leaf shuts over the hero and it pulls off the lot and drives the city's real streets — a Dijkstra over `CityLayout.RoadEdges`, the undirected street grid, laid out `1.5 m` into the right-hand lane by mitred vertex offsetting and rounded at every square junction — across the south-west yard's drive-clear forecourt corridor and `15 m` into the tunnel, where the screen goes under and `AreaArrivalToken.Ferryman` asks for the Mountain Road. On the default seed that is `266 m` in about `48 s`, inside a `35-75 s` band a test holds: routing it over `CityBusPlan` instead (whose `Nodes`/`Links` are Route 01 itself, one closed one-way circuit) sent the car `4.8 km` the long way round the loop to reach a portal `170 m` away, and every other assertion passed while it did. It leaves the carriageway at the point square with the forecourt opening and not at the junction nearest it — `CitySurfacePlan` puts an access centre at the MIDDLE of its frontage edge, so both ends of that block tie to the centimetre, the tie fell to the far one, and the car drove `13 m` past its own turning and swung back through `135°` to reach it. Every leg also reaches the corner rounder at its FULL length, the fine `1.5 m` sampling the drive model reads the road by being put back afterwards: a corner can only be cut as deep as half its shorter leg, so pre-subdividing the forecourt run held that one turn to a cut of `0.75 m` and `1536` degrees of heading per metre, against the `18` an ordinary junction asks for and the `2.2 m/s²` the profile is willing to lean. Both defects were silent — length, ends, seams and drive duration all stayed inside their own assertions while the car pivoted. That turn is also the only place on either leg where the car leaves its own lane, so it is a give-way: the line is set `6.5 m` back up the LANE from the turning (about `5.6 m` back along the finished road, the arc having shortened it), where the car is still square in its own lane rather than part way round the swing. It waits there for a bus coming the OTHER way or for anyone walking over the mouth, wants the way clear for `0.4 s` before it pulls out, commits the moment the line is nearer than its own braking distance (standing on the brakes through a line you are already over is worse than taking the turn), and never waits longer than `15 s`, because a walker who stops on the kerb must not be able to end the one ride out of the city. Direction is a rule and not an optimisation: the crossing STARTS in the car's own lane and Route 01 lays its links at the same `1.5 m` off the same crown, so a bus merely following the car sweeps straight through it and would otherwise hold him at the line for the full fifteen seconds with nothing crossing. And the portal is one vertex carrying the tunnel floor's own height, never the forecourt ground plus a step up onto it: two points sharing an X and a Z are a segment pointing straight up, `BuildVertexForwards` averages that into a forward pitched `45°`, and with every leg now reaching the rounder at full length the car reared in the hero's face at the mouth. It comes back out of the mountain's tunnel still moving at `5.2 m/s` with the same man at the wheel, climbs all `620 m` and stops in the middle of the terminal apron. The mountain half is ARMED in `Awake` and started a frame or two later, once `SceneTransitionService.IsTransitioning` has actually cleared — inside that window `PlayerAnimatedInteractionController` force-completes every interaction, so seating him there seated him and threw him straight back out. The map stays open and readable throughout the ride; only the two teleports and the area travel refuse, through `GameSessionState.IsRidingTheFerryman`. Speed is a pure model: a forward sweep of a backward pass over every vertex inside the braking horizon, so it lifts off *before* a hairpin instead of discovering it from inside one, and the R`7.5 m` bends pull it to about `3.5 m/s` on their own. The hero's root is carried by the car the bus's way — offsets captured once and rewritten each `LateUpdate`, never a reparent, with motor, `CharacterController` and contact shadow all put down for the journey — and `CanInteract` refuses the exit while the wheels are turning. On arrival the seat re-solves its whole plan against a car now `26 m` higher and hundreds of metres away, because every point in `LastRouteCarSeatPlan` is world-space and was worked out on the island. The mountain leg resumes through `BeginPositionedLoop`, a looping start that OWNS the root: the plain `BeginLooping` is for a body that stands up where it sat down, and its `placeAtExitOnCompletion = false` silently refused the bind that keeps the seated pelvis on the car's live anchor and left `exitHip` at the tunnel — so the DRAWN hero rode the whole climb standing at the tunnel mouth while only his capsule went up, and `CarAlightExit` then played correctly six hundred metres from the car. Getting out re-aims at the dock as the re-solved plan reads it. The climb can also be cut short: `F10` takes the screen under at `0.6 s`, puts the car at the cafe with both men in it FROM INSIDE THE BLACK - six hundred metres in one frame is a glitch in any framing - and brings it back at `0.8 s`. The jump moves the DISTANCE and nothing else (`LastRouteCarDriver.SkipToEnd`), so what follows is the ordinary arrival rather than a second one written for the skip. Then he gets out, and only then does the Ferryman: `FerrymanBoard` and `FerrymanDismount` played time-reversed by written `SetTime` on the parked one-shots (no negative playable speeds, no new art — each is authored to end on the pose the next one opens on, so backwards it begins there), with the trudge path walked the other way under a forward-playing walk cycle. He climbs onto the bonnet, the coin comes back, and he stays. **And the car yields.** Beyond the one authored give-way, a road-anchored `2 m`-chord probe watches the car's own path to a stopping-distance horizon every frame: the bus (tail to predicted nose, `2.7 m` corridor - opposite lanes run at `3.0 m` so lawful oncoming traffic never triggers, and there is deliberately NO same-way exclusion, since the same-way bus dwelling `10 s` in the shared lane line IS the collision partner at `2.2 m/s` closure) and every walker (`1.2 m` + radius; a bus-stop walker stands `1.74 m` off the lane line and stays clear). `LastRouteCarTrafficYieldModel` inherits the give-way's three rules whole: cannot-stop-does-not-try (never parks inside a junction, in the lane the bus yields to no one in), `0.4 s` settle against heading flicker, and an `18 s` standstill cap past the longest lawful dwell with no re-arm on the same conflict. One hold writer: the give-way component advances both rules and the model's single slot takes the min. Deadlock is impossible by construction - the bus was deliberately NOT taught to see the car; the wait graph stays acyclic and `TrafficYield_HoldGeometryKeepsTheWaitGraphAcyclic` pins the arithmetic that keeps the held hero outside the bus's `1.71 m` corridor. The probe grid is anchored to the ROAD, not the car - anchored to the car it crept with it, and the car crawled its own quantisation into the bus at half a metre a second. **And he steers.** The rim (`PIVOT_SteeringWheel`, grips authored ON it as its children) rolls in `LastRouteCarDriver.ApplyWheels` from the same smoothed `steeringDegrees` as the front wheels — ratio `3.0`, cap `±100°`, `33 × 3.0 = 99` rail-to-rail at full lock (the bus's `3.55` against a `33°` clamp would pin the cap through every corner) — about an axis MEASURED off the two grips and pointed at the driver's seat, never the imported node basis, and deliberately without the bus's negation: this raked column faces the driver where the bus's faces the windshield, so the copied sign would roll the rim left on a right turn in mirror. `Halt()` straightens the wheel, because the alighting clips start from hands drawn on an unturned rim. His palms ride it by the bus driver's own two-bone IK, now shared as `SeatedArmIk` (post-hint recovery `2 → 4` iterations — at full lock two left a palm `2.2 cm` off the `2 cm` grip contract), applied in his `LateUpdate` strictly after `graph.Evaluate`, targets eased `0.35 s` from the clip's own socket pose to the live grips only in the Driving phase, arm bones bound at prefab build onto `LastRouteFerrymanRigAnchors` (nothing is found by name at runtime). Pinned by mirrors of the bus's precedent tests: EditMode rim ratio/sign/rest, PlayMode grip contact `≤ 0.02 m` through a saturating corner under `AlwaysAnimate`. **And it is heard.** `LastRouteCarAudio` hangs five source-first voices off the same root the bus's way: a petrol-four engine loop in the bay under the bonnet, driven by the pure `LastRouteCarEngineModel` (three-speed box with `0.84`/`0.36` hysteresis and a `0.32 s` clutch dip, load from acceleration plus grade so the `8%` climb opens the tone rather than raising the revs, an ignition ladder that cranks `1.05 s` on the island and comes out of the mountain's tunnel already running), a 2D cabin loop up while the hero is seated, a rear-axle tyre loop on `WetAsphalt` or `PackedSnow`, a bridge-deck thrum with joint thumps at both abutments, and one cue voice for starter, key-off and door latches. The tunnel is an `AudioReverbFilter` faded by room level off `MountainRoadRoot.IsInsideTunnel` / `CityTunnelShelterController`, and the mountain wind bed is muffled behind the glass through `SetEnclosure` without a second writer of the wind. | `LastRouteRideController`, `LastRouteCar{DrivePath,DriveModel,Driver,GiveWay,GiveWayModel}`, `LastRouteCityDeparturePlanner`, `LastRouteMountainDrivePlanner`, `LastRouteFerrymanAlightingTimeline`, `LastRouteFerrymanRideStage`, `LastRouteFerrymanRigAnchors`, `SeatedArmIk`, `LastRouteCarTrafficYieldModel`, `LastRouteRide{FadeView,SkipHintView}`, `GameSessionState.TryAdvanceFerrymanRide`, `MountainRoadRoot.BuildLastRoute`, `LastRouteCar{Audio,EngineModel}`, `MountainRoadWindSoundPlayer.SetEnclosure` | Current |
| Home bed sleep | One trigger on the door-side bed edge; guided walk, neutral settle, then `BedEnter`/`BedSleepLoop`/`BedExit` through a real bedside sit. Normal completion clears session fatigue; cancellation preserves it. | `HomeBedInteraction{Plan,}`, `PlayerAnimatedInteractionController`, `GameSessionState`, localization catalogs | Current |
| Home balcony smoking | Balcony dock with guided city-facing entry, `SmokeEnter`/`SmokeLoop`/`SmokeExit` on a socket-bound cigarette, periodic mouth plume, a permanent rail ashtray and a bounded city-biased camera push-in with drift and exact restoration. | `HomeBalconySmoking{Plan,Interaction,Timeline,CameraDrift,ExhaleEffect}`, `HomeBalconyWorldBuilder`, `HomeSmokingMusicPlayer` | Current |
| Home refrigerator | A derived cabinet, clear approach, first-person pose and eight storage slots; a modal open → browse → inspect → close flow driven by a prefab-derived right-arm subset under a world-visibility lease, where `Take` commits atomically. **Gap:** `Use` is registered but unavailable. | `HomeRefrigerator{Plan,WorldBuilder,View,Interaction,InteractionTimeline,FirstPersonHand}`, `HomeRefrigeratorItem*`, `Player3DFirstPersonSubset` | Partial |
| Player shadows | Every production mesh casts and receives ordinary URP shadows, while one shared analytic contact quad raycasts onto the live surface normal and expands through both authored and physics fall phases. | `Player3DCharacterPresentation`, `Player3DRagdollController`, `PlayerContactShadow{,.shader}`, `PlayerPresentationMetrics` | Current |
| Interaction/UI | One post-composite `640x360` soot/bone IMGUI skin with flat nested frames, stable grain and grayscale focus. Prompts keep identical pointer/key guards; the south-tunnel refusal uses the same feedback view without inventing an `E` action. | `PlayerInteractor`, `InteractionPromptView`, `InventoryTargetInteractionController`, `CityBusRideController`, `CityTunnelTravelController`, `Runtime/{Interaction,UI}`, `RetroUiTheme` | Current |
| F9 debug controls | City/Bar/Mountain Road open the debug window for `±20` intoxication steps and direct day `1–7` selection that preserves `HH:MM` and needs; City and Mountain Road also toggle their scene-local map teleport. Home instead skips directly to City and opens that map mode after the transition. | `MinigameDebugWindow`, `HomeDebugCityMapShortcut`, `CityMapController`, `MountainRoadRoot`, `GameSessionState` | Current |
| Structured session diagnostics | One bounded UTF-8 NDJSON stream with build/session/scene/seed context and correlated transition and Unity-message events; rotates at 5 MiB keeping three archives, `F8` snapshots. Format: `ai/debug-log.md`. | `Runtime/Diagnostics`, instrumented state boundaries, `MinigameDebugWindow` | Current |
| Bar activity flavour | The authored default home bar keeps its former `SplitTheG` identity by blueprint/seed/cell; explicit custom multi-bar layouts retain stable row-major activity assignment. After the minigame cut the kind only flavours interior dressing. | `BarActivityKind`, `BarActivityAssignment`, `BuildingLot`, `GameSessionState` | Current |
| Area map UI | Switchable City / Mountain Road / Village tabs; City retains canonical surfaces/Route 01 and cuts the one internal cemetery/church passage into both precinct outlines without treating it as a street gate, while Mountain Road draws the complete climb, all ten hairpins, bridge, terminal, cafe and cableway. The village reuses the mountain overlay TYPE rather than a second one — both tabs are one polyline and one rectangle, and here the polyline is the lane and the rectangle is `TerrainBounds`, the inhabited bowl plus its `12 m` margin — and names only the station, the house at the top, the chapel, the adit and the graves, because a village where every door is labelled is a menu. Its house is labelled «Дом на вершине» and not «Дом матери»: the §6 registry lifted the place, not the events. Only the current tab has the player marker; confirming another requests area travel. | `CityMap{Controller,View,AreaController,AreaView,MountainRoadOverlay,AlpineVillageOverlay}`, `CityMapTeleportGrounds`, `CityMapBusOverlay`, `MinigameDebugWindow` | Current |
| Map XYZ inspection | Either tab's ordinary XYZ mode selects every catalogued semantic point, rules an even teleport lattice over the whole chart (city cell `26 m` anchored on the city origin, `8 m` from the tunnel portal on the mountain road) and shows a persistent highlight, area and one-decimal world coordinates. A square is the last-resort pick behind every named marker, dead squares are scrimmed, and the other tab draws lines only. The mode carries its own teleport to any marker or live square in the current area — no F9 arming, which used to hide the button entirely — and a point on the OTHER tab becomes a travel that carries its coordinate through the transition. Route editing stays out, and the two modes are otherwise independent. | `CityMapTeleport{Lattice,Grounds}`, `CityMapPointDescriptor`, `CityMap{AreaController,Controller,View}` | Current |
| Map arrival ground | The clamp belongs to the area the player stands in, because both scenes share one coordinate system and the mountain route starts on top of the city. City keeps the walkable mask plus a road-then-ground height sample and subtracts building and church-yard fixture footprints; the mountain road derives height exactly from route samples, the plateau slab and the tunnel floor. | `ICityMapTeleportGround`, `CityMap{City,MountainRoad}TeleportGround`, `MountainRoadRoot`, `RoadWalkableArea`, `MountainRoadWalkableArea` | Current |
| Scene transition | Ordinary location doors first complete the source-scene `DoorUseEnter/Loop/Exit` gesture at an explicit grounded dock; only its terminal neutral completion may reserve the one transfer guard, load `DoorTransition` in Single mode, preload the destination and activate it at final blackout. | `PlayerDoorAction{Plan,Controller,Target}`, `SceneTransitionService`, `SceneIds`, `GameSessionState` | Current |
| Area loading transition | Map travel Single-loads black `AreaLoading`, shows unscaled async progress, then Single-loads City, Mountain Road or the village; no two worlds ever coexist. A request may carry an arrival coordinate (`AreaArrivalToken.MapPoint`), armed with the token before destination activation; each root clamps it to its own ground and falls back to its ordinary spawn. | `AreaTravel{Types,Service}`, `AreaLoadingRoot`, `SceneIds`, `CityGameRoot`, `MountainRoadRoot`, `AlpineVillageRoot` | Current |
| Door transition presentation | A deterministic `3.15 s` unscaled fixed-camera door sequence in a black void, tinted warm on entry and cold on exit, with generated latch and hinge cues. | `DoorTransition{Root,Timeline,Direction}`, `RuntimeSceneSetup`, `RetroSfx` | Current |
| Session state | Reset a complete run on launch, then preserve blueprint ID and seed, active bar and activity, ordered route, return kinds, clock and day index, cash, needs, intoxication and the balance sequence across scene loads. | `GameSessionState`, `CityBlueprintCatalog`, `GameTimeState`, `PlayerNeedsProgressionState`, `CityReturnKind`, `HomeArrivalKind` | Current |
| Bar drink retail and physical service | Nine localized drinks at fixed prices through a seated first-person counter: nine physical bottles, an atomic cash/drink commit, prefab-derived arm subsets, a world-space pour and a three-second drink from the matching vessel. | `BarDrink{Catalog,PresentationCatalog,ServicePlan,ServiceTimeline,ShopController,BottleView,VesselView,FirstPersonArms}`, `DrinkPurchaseRules`, `BarCounterStation` | Current |
| Intoxication stages and presentation | Map `0-100` into Sober plus five named 20-point stages driving movement speed, additive 3D bone poses, camera, world-composite effects and a URP chromatic-aberration/lens-distortion volume (togglable), with recovery that accelerates toward sober and pauses under modal ownership. | `IntoxicationStageRules`, `IntoxicationStatusController`, `IntoxicationRenderState`, `IntoxicationLensVolumeDriver`, `IntoxicationHudView`, `Player3DCharacterPresentation` | Current |
| Balance checks and falling | Above `60`, a seeded fixed-step arrow challenge that never starts on a surface steeper than `12°` and never disables locomotion until failure; failure plays a directional Fall clip, hands the rig to 13 kinematic-to-dynamic bodies, then blends into a full-body side-specific Rise. The gameplay root and `CharacterController` stay upright and fixed. | `BalanceChallengeModel`, `IntoxicationStatusController`, `BalanceCheckView`, `PlayerFallAnimationTimeline`, `Player3DRagdollController`, `PlayerContactShadow` | Current |
| Bar interior and exterior | The validated `22 x 16 x 4.8 m` room keeps its Residential dressing and `SplitTheG`; complete fixed-metre `bar_exterior_v2` has a sealed timber frontage and inset textured foundation in City and full Home. Unity retains collision, entrance, transition and light authority. | `BarInteriorLayout{Plan,Planner,Validator}`, `BarDistrictIdentity`, `BarAssetRegistry`, `BarAssetSetup`, `BarInteriorWorldBuilder`, `CityBarFacadeWorldBuilder`, `BarExteriorSurfaceAppearance`, `BarDrinkServiceWorldBuilder`, `BarInteriorRoot` | Current |
| Player home interior | A validated `10 x 8 x 3.4 m` main-room and bathroom plan with clear entry, main and bathroom paths and furniture outside protected circulation, plus a real window and open door onto a walkable balcony at `4.7 m` street elevation. | `HomeInteriorLayout*`, `HomeBalconyLayout*`, `HomeInteriorWorldBuilder`, `HomeBalconyWorldBuilder`, `HomeBathroomBuilder`, `HomeInteriorRoot` | Current |
| Player-home stairwell | A side-aware `8.6 x 9.6 x 6.25 m` scene connecting street lobby, middle landing and apartment door over 48 visible steps and three seam-free ramps, with the flight above the hero blocked, three height-selected fixed shots and a green desaturated grade. | `StairwellLayout*`, `Stairwell{WorldBuilder,DressingBuilder,InteriorRoot,FixedCameraController,InteriorAtmosphere,Soundscape}` | Current |
| City facade presentation | The legacy 4×4 path remains only for clipped Home crossings. Ordinary prototypes bind 24 sheets; the dedicated bar, supermarket and player-home exteriors bind their own semantic materials, with Home rebuilding the same nine player-home sheets around its physical balcony. | `CityFacade{Grid,Appearance}`, `CityBuilding{SurfaceAppearance,WindowSlotAppearance}`, `{Bar,Supermarket,PlayerHome}ExteriorSurfaceAppearance`, `CityWorldBuilder`, `Home{ExteriorView,BalconyWorld}Builder` | Current |
| City window presentation | Pure block/floor/side keys quantize each district's lit share separately in every authored row: at least one warm street-lamp-coloured pane, and at least one dark pane where the row has room. Selected panes retain the §20 day floor; their full frame/curtain/blind atlas cell affects both albedo and emission instead of reading as a flat card. Bar/Home/Supermarket material families remain explicit but share the same warm hue. | `CityDistrict{ArtProfile,PresentationPlan,PresentationPlanner}`, `CityWindowAppearance`, `CityExteriorAppearance`, `CityBuildingWindowSlotAppearance`, `CityWorldBuilder`, `HomeExteriorViewBuilder`, `tools/build-city-{buildings-3d-model,window-textures}.py` | Current |
| Stairwell surface presentation | Eight cached opaque albedos applied through projection-aware deterministic UV scale and MPB values on one shared `RuntimePrimitiveLit`, with linear-albedo-compensated palette tints and no material instances. Projection and hash math live in the shared `SurfaceAppearanceCore`. | `StairwellSurfaceAppearance`, `SurfaceAppearanceCore`, `Stairwell{WorldBuilder,DressingBuilder}`, `Resources/Stairwell/Textures` | Current |
| Mountain Road surface presentation | Fifteen measured surface families on one of two shared materials - `RuntimePrimitiveLit` for everything, and `MountainFoliageLit` for exactly the three conifer-crown batches, which need a vertex stage that bends - applied through property blocks with no per-object material: six are printed for the climb (cold asphalt, forest floor, wind snow, layered stone, conifer needles, bark/deadwood) and nine are BORROWED from City, Home and Supermarket sheets whose bytes are untouched and whose compensation is re-solved against the mountain's own tints. UVs were fixed before the sheets landed: the road kerb unwraps over its edge, the plateau and apron share the road's frame across the entry seam, soil and snow share one set of normals, ridges/boulders take a faceted box projection, crowns unroll by arc against height ABOVE THEIR OWN FOOT (the only channel all four wrapped passes share), and the bridge deck moved onto the same batch as its girders. The authored cafe owns the separate six-sheet contract above. Glass, the tunnel dark, emissive lenses, thin cable and the flickering lamp lens stay flat by a test-enforced exclusion list. | `MountainRoadSurfaceAppearance`, `MountainRoad{Surface,Terrain,Scenery}MeshFactory`, `MountainRoad{World,BridgeWorld}Builder`, `MountainCablewayWorldBuilder`, `tools/build-mountain-road-textures.py`, `Resources/Textures/MountainRoad*` | Current |
| Home surface presentation | Twelve cached opaque home albedos (wallpaper, plaster, planks, dark wood, laminate, upholstery, linen, tile, enamel, painted metal, concrete, rug) applied metre-tiled through the shared surface core on the one shared `RuntimePrimitiveLit`; builder colours survive as tints under the city-facade linear compensation rule, small props and decal overlays stay flat by an explicit test-enforced exemption list, and the occlusion dither shader samples `_BaseMap` so textures survive visibility fades. | `HomeSurfaceAppearance`, `HomeSurfacePrimitives`, `Home{InteriorWorldBuilder,BathroomBuilder,BalconyWorldBuilder,InteriorDressingBuilder,RefrigeratorWorldBuilder}`, `tools/build-home-textures.py`, `Resources/Home/Textures` | Current |
| Stairwell cat | One 3D near-black Cheshire trickster perched on the middle landing rail (the last sprite conversion; no armature — pivot empties the actor adopts and articulates): continuous player-tracking head, the untouched idle timings as pose deltas, and a default-hidden comically-wide grin that draws itself in from the arc center while the head turns over the shoulder to the camera. Talk/Interact opens the shared target menu; feeding consumes one `OpenStewCan` through synchronized player clips and a head-down eating pose. The grin trigger is deliberately unwired — a future script drives `StairwellInteriorRoot.CatGrin`. | `StairwellCat{Plan,Actor,Provider,Factory,RigAnchors,HeadYawModel,PoseRules,GrinTimeline,GrinController,IdleModel,Feeding*}`, `StairwellCatInteraction`, `StairwellCatGrin.shader` | Current (grin trigger: future script) |
| Home exterior context | Reconstruct a bounded collider-free view of the real home street inside `HomeInterior` from the same blueprint ID and seed, including transformed decoration, a static Route 01 pole and pedestrians enabled only for the Balcony shot. No bus actor is composed, by decision. | `HomeExteriorContext{Plan,Planner}`, `HomeExteriorViewBuilder`, `CityBusStopWorldBuilder`, `CityExteriorAppearance`, `HomeDayNightController` | Current |
| Player home atmosphere | A fog-free post-processed interior on an explicit five-light budget with co-located emitters and halos, now clock-driven end to end: the window passes through a dusk amber between its warm day and blue night poles, the main lamp and hallway rise after dark while ambient and the directional fill sink cold, and the Balcony shot still borrows City's light and hands the indoor mood back on exit. | `HomeInteriorAtmosphere`, `HomeDayNightController`, `HomeBalconyExteriorAtmosphere`, `HomeBathroomLight{Fixture,Flicker}`, `HomeSoundscape` | Current |
| Home bathroom scenes | A rebuilt Soviet shower stall (L-rail, folded curtain group, mixer, hose, tilted head, soap shelf) and three modal scenes on one shared skeleton: guided walk-in (no teleports), Bézier camera from the pinned bathroom shot, debounced stop, idempotent restore. Toilet is a privacy cut to the ajar-door frame with an off-frame flush; the shower draws the curtain over the hero and runs water, steam and a crossfaded hiss loop; teeth brushing shoots from the mirror plane into the hero's face while a procedural CCD right arm works the gripped toothbrush, foam and rinse beats included. Toilet/shower relieve stress on completion, brushing once per game day. | `HomeBathroomSceneInteraction`, `Home{Toilet,Shower,TeethBrushing}Interaction` + timelines, `HomeShowerWaterEffect`, `HomeTeethBrushingArmPose`, `HomeBathroomBuilder`, `HomeSoundscape.SetShowerWaterAmount`, `RetroSfxId.{ToiletFlush,TeethBrushScrub}` | Current |
| Bar patrons and bartender | City pedestrian prefabs fill the guest anchors, palette-varied and seated through the shared seat contract or standing at tables. The bartender anchor is staffed by the dedicated 1.75 m, six-armed provider-bound NpcHumanV2 figure and its service choreography. | `BarPatronWorldBuilder`, `BarBartender{Provider,WorldBuilder,Presentation,ServiceChoreography}`, `CityPedestrian{Resources,AssetRegistry,Presentation}` | Current |
| Bar cinematic atmosphere | Six shadowless practical lights, bar-only post-processing, bounded dust, scene-local spatial cues and a skippable `1.35 s` arrival reveal that restores follow and orbit state before any modal opens. | `BarInteriorAtmosphere`, `BarSoundscape`, `BarArrival{Timeline,Presentation}`, `RuntimeSceneSetup` | Current |
| Automated tests | EditMode and PlayMode coverage over deterministic plans, asset and rig contracts, interaction lifecycles, atomic transactions, audio routing, PS1 presentation and scene round trips, across 262 test source files in two test assemblies over shared infrastructure. | `Assets/Tests/{EditMode,PlayMode,Infrastructure}`, `tools/build-player-3d-model.py` validators | Current |
| Automated test audio guard | Silence global listener output for every EditMode and PlayMode run without pausing sources or DSP, then restore the previous volume. | `BarPromenade.TestSupport`, `AutomaticTestAudioMute` | Current |

## Primary intended flow

Structural backbone only. Exact ordering, tuning and edge cases live in
`ai/project-overview.md` and `ai/architecture-notes.md`.

```text
build index 0 -> MainMenu -> BeginNewGame + HomeArrival.OpeningSleep
  -> HomeInterior sleeping opening
     -> 05:59 clock shot -> 5 s input lock -> Wake Up / Quit
     -> Wake -> 06:00 + session clock starts -> alarm hold -> 6 s wake
     -> ordinary Home control -> StairwellInterior -> City
     -> debug F9 -> 06:00 if needed -> direct City at home
                 -> teleport enabled + map opens after transition

session clock -> one-based day -> DAY N announcement + inventory day/time
              -> day/night sample -> City + Home lighting, Home clock display
              -> elapsed minutes -> hunger (1440) + fatigue (1080) -> inventory bars
seed + session clock -> weather slots -> rain intensity -> rain field + rain bed
                     -> storm windows -> lightning flash + delayed thunder

map -> City / Mountain Road tabs -> confirm other area
  -> AreaLoading Single load -> black progress bar -> destination Single load
  -> MountainRoad -> tunnel -> ten-hairpin climb around high bridge -> plateau

blueprint ID + seed -> immutable blueprint -> validated sparse layout
  -> typed area cells -> surfaces, navigation, city map
  -> one Residential bar across from home / supermarket / district public places
  -> street surface plan -> Road v2 carriageway + sidewalks + Road v2.1 aprons
     -> pedestrian graph -> profile-sized player-relative walker population
     -> Route 01 loop -> stops + poles -> one pooled bus actor
        -> board prompt -> seat 07 -> ride -> alight at a later stop
  -> night fixture plan -> lamps, signals, halos
  -> decoration plan -> visuals + collision proxies
  -> fence plan -> rails with clearance openings

eight gameplay roots -> PlayerFactory -> Resources/Player/Player3DV2.prefab
  -> 38 Actions: Idle / Walk / heavy Run blend + face atlas + additive status bones
  -> Shift or L3 + forward -> 4.2 m/s run; backpedal and scripted approaches walk
  -> actual constrained speed owns Run weight; intoxication scales it, fatigue does not
  -> contextual actions: bed, smoking, cat feeding, bus board/ride/exit
  -> failed balance -> Fall clip -> bounded ragdoll -> Rise -> Relaxed
  -> first-person subsets: bar bottles, refrigerator
  -> inventory portrait

player -> interaction -> SceneTransitionService -> DoorTransition
  -> preload destination -> activate at blackout
  -> BarInterior   -> activity-flavoured dressing + seated/standing 3D patrons
                   -> counter station -> physical drink service
  -> SupermarketInterior -> finite shelves -> atomic purchase
  -> StairwellInterior   -> cat -> Talk / feed
  -> HomeInterior        -> bed, balcony smoking, refrigerator, three fixed shots
                         -> bounded same-seed exterior reconstruction

all gameplay results -> GameSessionState -> preserved across Single loads
state boundaries     -> correlated rotating debug.log (NDJSON)
scene root           -> audio mixer snapshot + scene music + ambience
URP post-processing  -> PS1 composite -> crisp soot/bone IMGUI overlay
```
