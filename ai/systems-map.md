# Systems map

An index, not a specification. One row per system: what it guarantees, where its
code lives, and whether it is real.

Detail belongs elsewhere and is not repeated here:

- normative decisions and exact tuning values: `ai/architecture-notes.md`;
- current end-to-end behaviour: `ai/project-overview.md`;
- file-by-file layout: `ai/system-tree.md`;
- player-visible history: `ai/release-notes.md`.

## Status vocabulary

| Status | Meaning |
| --- | --- |
| `Current` | Implemented and verified in the repository. |
| `Partial` | Implemented, but a named part of *this system's own* intent is missing. The row must state the gap. |
| `Planned` | Intended; no implementation in the repository. |
| `Deferred` | Explicitly outside the present milestone; see `ai/project-overview.md`. |

A row never carries a status outside this table. Product-level scope cuts
(save data, economy, weather extras beyond rain) are `Deferred` in
`ai/project-overview.md` and do not make an otherwise complete system
`Partial`.

## Systems

| System | Guarantee | Key files | Status |
| --- | --- | --- | --- |
| Unity URP foundation | Rendering profiles, seven build scenes and PC renderer settings. | Unity `6000.5.9f1`, URP `17.5.0`, `PC_Renderer.asset` | Current |
| PS1 world composite | Footprint-average the post-processed frame to `640x360`, apply intoxication warp/ghost/chroma, blend 35% RGB555 and point-upscale. | `Runtime/Rendering`, `IntoxicationRenderState`, `Ps1Composite` material/shader, `Ps1PresentationProfile` | Current |
| City composition | Exact seven-scene bootstrap and runtime roots. | `Runtime/Core`, `SceneIds` | Current |
| Startup waking opening | Fresh run: frozen `05:59`, five-second ordinary-input lock, Wake Up/Quit, then a continuous wake. Home F9 may instead start `06:00`, enter City at home and open the debug-teleport map. | `MainMenuRoot`, `HomeOpening{Controller,Timeline}`, `HomeDebugCityMapShortcut`, `CityGameRoot`, `GameSessionState` | Current |
| Session clock and day/night rules | Persistent scaled clock from `06:00` at one game minute per real second (`24 h = 1440 s`); night/dawn/day/dusk sampling that survives scene loads and stops at `timeScale = 0`. | `GameTimeState`, `GameTimeRuntime`, `GameTimeDayNightRules`, `GameSessionState` | Current |
| Session time displays | The same session `HH:MM` on the physical Home alarm clock and in the inventory Status panel. | `HomeAlarmClock`, `InventoryView`, `GameSessionState` | Current |
| Gameplay pause menu | Escape/Start takes the shared modal lock in all five gameplay scenes, freezes scaled time, pauses gameplay audio and draws a localized Resume/Start Over/Quit menu with default-No confirmation. | `PauseMenu{Model,Controller}`, `BarMinigameModalLock`, `RetroAudioService`, scene roots | Current |
| Hero inventory | Localized fullscreen item screen under the shared modal lock in all five gameplay scenes: needs bars, cash, 3D portrait, stack grid, live rotating 3D item preview and contextual Eat/Drink/Examine. Stacks and world-source IDs persist across scene loads. | `Inventory{Types,State,MenuModel,ItemModelFactory,ConsumableCatalog,Controller,View,IconLibrary,ItemPreviewRenderer}`, `Resources/Player/Player3DPortrait`, `GameSessionState` | Current |
| Hunger, stress, fatigue and consumption | Clamped `0-100` session needs driven by the same scaled clock (hunger over `1440` game minutes, fatigue over `1080`); food, alcohol and completed bed rest each commit relief exactly once. **Gap:** the values drive no gameplay debuff yet. | `PlayerNeedsProgressionState`, `PlayerNeedsRules`, `InventoryConsumableCatalog`, `DrinkRules`, `GameSessionState` | Partial |
| Inventory-backed target interactions | Reusable Talk/Interact target menu with a single-stack item requirement, default-No confirmation and atomic one-shot consumption that refunds a thrown startup. | `InventoryItemRequirement`, `InventoryTargetInteraction{Definition,Model,Controller}`, `IInventoryTargetInteractionHandler`, `PlayerInteractor` | Current |
| Quest log and journal | Catalog-driven session quests (`FeedTheCat` auto-active on a new game, completed exactly once when the cat feeding begins); J/Select opens a localized journal under the shared modal lock in all five gameplay scenes; a quest-reserved item refuses ordinary eating with an "I'm not hungry yet" line. | `Quest{Types,LogState}`, `JournalController`, `GameSessionState`, `StairwellCatInteraction`, `InventoryController` | Current |
| Stairwell quest descent gate | While `FeedTheCat` is active, crossing `0.35 m` below the cat's landing on the way down shows the Silent Hill style "feed the cat first" line and walks the hero back up to the landing facing the cat before input returns. | `StairwellQuestDescentBlocker`, `PlayerMotor.MoveTowardsInteractionPose`, `InteractionPromptView`, `StairwellInteriorRoot` | Current |
| City blueprint and layout model | One immutable stable-ID blueprint per session: validated connected sparse cells, typed urban/open areas, four graph-separated bars, one home, one supermarket and district public lots. | `CityBlueprint`, `CityBlueprintCatalog`, `CityLayoutGenerator`, `CityLayout`, `CitySurfacePlan`, `CityDistrict`, `CityTravelDistance` | Current |
| City elevation and exterior stairs | One deterministic river-valley plan gives default road nodes about `8.1 m` of range with a peak near `10.08 m`; ordinary ground and beach use one continuous sampled terrain contract, the sea stays at datum `0`, the lake is a local elevated basin, and every urban district keeps one guarded stair street. Declared decks may cross non-walkable water; overlapping walkable levels and tunnels remain absent. | `CityElevation{Plan,Planner,Validator,Rebaser,StairPlacement}`, `CityTerrainSurfacePlan`, `CityTerrainSurfaceWorldBuilder`, `CityExteriorStair{Plan,Planner,Validator,WorldBuilder}`, `CityRoadGroundBoundaryPlan`, `CityTerrainSafetyWorldBuilder` | Current |
| City river and embankments | A `10 m` north-south channel expands the default envelope to `13 x 12` without removing its 144 lots. Two `3 m` promenades, two Road v2 bridges, one `2.8 m` timber park bridge and four open stair-served lower landings form the only crossings and waterside access; each bridge is built on its `SpanBounds` — the channel plus a quay-wall seat — so structure, guards and the crossing's own road or path surface end at the water and never stand on a promenade. Four measured sheets carry the solid surfaces — flag paving at `3.2 m`, rusticated quay courses at `2.2 m`, painted iron at `1.2 m`, channel silt at `2.0 m` — on the shared primitive material; the road bridges take the iron and quay sheets and the footbridge the park's timber, each keeping its authored tint. The water is a subdivided single-sided grid on its own hand-written URP shader: world-space wave displacement, a linear ripple derivative map and a foam mask, and depth-driven absorption, refraction and bank foam composited by hand from the URP depth and opaque copies. A silt floor `1.10 m` down closes the channel the terrain pass cuts away, and bridge piers reach it. The lake now shares the shader through its own still-water material (see the Lake precinct row). **Gap:** the sea is still a flat box on the shared primitive material, so the river mouth shows a tonal step; the shader takes `_FlowDirection` as a parameter so it can adopt it unchanged. | `CityRiver{Definition,Plan,Planner,Resources,SurfaceAppearance,WorldBuilder}`, `CityWaterSurfaceFactory`, `CityRiverWater.shader`, `SurfaceAppearanceCore`, `CityBlueprintCatalog`, `CityLayout`, `RoadWalkableArea` | Partial |
| Yard wheelchair rider | One isolated `pipeback_roller_v1` NPC drifts forever around the yard site contract's authored ring — ridden on bare ground, nothing drawn for the lap — in a ragged ground-locked push-and-coast rhythm (`1.35 m` cycle, `1.42x` surge to `0.62x` coast). The presentation adopts the static wheel/caster meshes under their authored `PIVOT_*` anchors, so tyres, rims, spokes and casters roll with covered distance (differential and forward roll sign included) while `PipebackIdle`/`PipebackRoll` animate the skeletal rider and the bellows pumps on the push phase. A serialized provider keeps the prefab outside `Resources` and the pedestrian pool. | `YardWheelchair{Motion,Plan,Presentation,Actor,Factory,Provider}`, `Resources/City/YardWheelchairProvider.asset`, `CityGameRoot` | Current |
| Bar-side yard composition | The walkable roadless gap immediately left of the home-frontage bar holds the dead tree and sparse traces, plus a phone booth and dumpster leaning on the bar wall clear of the rider's circuit via shared reserved-ground anchors. Nothing is drawn for the circuit itself: the chair rides bare ground on the yard site contract's authored ring. Its dead lamp stays dead; one deliberately unreal cold `240`-intensity Spot on the neighbouring supermarket keeps the full circuit inside a bright inner cone, casts hard `0.95` shadows day and night outside `NightFactor`, has no volumetric beam and never tracks the rider. | `CityOpenAreaDecoration{Plan,Planner}`, `CityOpenAreaWorldBuilder`, `HomeYardUtilityPlanner`, `ai/city-zones-art-bible.md` §10a | Current |
| Cemetery precinct | Dedicated deterministic dressing of the `3 x 2` eastern cemetery: textured gravel alleys from an arched leaf-open iron gate, hash-varied graves in six monument silhouettes over three stone tints with post-grounded оградки and offerings, perimeter birches/firs, sittable timber benches beside the main alley (fed into the shared bench-sit pass via `CityWorldResult.CemeteryPlan`), and a `4-9` lamp chain of night-scaled gas mantles along its whole length that dies by day; stone/gravel/soil batches carry four dedicated `CityCemetery*Albedo` sheets. | `CityCemetery{Plan,Planner,WorldBuilder,SurfaceAppearance}`, `CityBenchSitPlan.CreateAll`, `tools/build-cemetery-textures.py`, `ai/city-zones-art-bible.md` §10c | Current |
| City yards | Five typed undecorated open precincts replace the former boundary voids: one eastern fringe pocket near the home (distinct from the inter-building bar-side yard) and four southern/western strips, each with street access, continuous sampled `OpenGround`, walkability and a «Двор» name the map reveals on hover. They host no signature stairs, bus corners or decoration. | `CityBlueprint{,Catalog}`, `CitySurfacePlan`, `CityTerrainSurfacePlan`, `CityElevationPlanner`, `CityRoadGroundBoundaryPlan`, `CityBusIntersectionSelector`, `CityWorldBuilder`, `CityMapView` | Current |
| District public places | One non-building public lot per default urban district at `18 m` minimum sides, with canonical bounds and full-width street approaches, built as four free-standing physical places. Five scripted POI albedos pave every public ground and fully texture the drying yard (frames, laundry cloth, bench) and the last route island (painted metal, layered poster paper, timber, cloth rags). Two night-scaled POI realtime lights: the drying yard's pole floodlight and the island's mast service floodlight. | `CityDistrictPointOfInterest{Plan,Planner,WorldBuilder}`, `CityPointOfInterestSurfaceAppearance`, `CityNightSiteLightRegistry`, `CityLandUseKind`, `CityLayoutGenerator` | Current |
| Drying yard babushkas | Three authored staged NPCs on the Residential public place: two grandmothers beat the rack's hung carpets with Soviet plastic beaters on desynchronized loops, one smokes apart watching. One shared staged model with both hand props, role-enabled; colliderless with attention magnets, outside the pedestrian pool, stances derived from the same recipe transform as the drawn carpets. | `DryingYardBabushka{Provider,Plan,Presentation,Factory}`, `CityDistrictPointOfInterestWorldBuilder.TryDescribeBabushkaStances`, `CityPedestrianAssetSetup`, `build-city-pedestrian-3d-model.py` | Current |
| Weighbridge attendants and needle | Two authored staged NPCs on the Industrial cold weighbridge: the weigher reads the indicator beside the mechanism — never across the deck axis — checking the dial, leaning to the linkage and chalking the deck edge, while the weighed worker paces the deck's long axis slaved to his clip's normalized time and stands square at centre through the authored `0.36-0.64` pause. The scale answers weight: a City-root controller eases the `Scale Needle` off its authored `28°` rest (`34°` sweep, `0.45 s` attack / `0.90 s` release) while the worker's pause or the hero stands on the walkable deck; the Home-exterior rebuild never claims the registry slot. One shared staged model, chalk enabled only for the weigher; colliderless with attention magnets, outside the pedestrian pool. | `WeighbridgeAttendant{Provider,Plan,Presentation,Factory}`, `CityWeighbridgeNeedleController`, `CityWeighbridgeIndicatorRegistry`, `CityDistrictPointOfInterestWorldBuilder.TryDescribeWeighbridge{Stances,Deck}`, `CityPedestrianAssetSetup`, `build-city-pedestrian-3d-model.py` | Current |
| Cemetery mourner | The cemetery's one scripted transient NPC: while the hero is within `28 m` of the grounds and the `180 s` cooldown has passed, a woman in deep mourning (near-black coat and veil, `988` triangles, all four palette variants dark with the bouquet as the one colour note) spawns on the street by the director's spirit — `76 m` away or outside the camera's horizontal cone — walks in through the one gate along the main alley to a deterministic random unenclosed grave, lays her bouquet at the authored offering spot, sobs for exactly `30 s`, wipes her eyes and walks out; the instance and her laid bouquet despawn together. The rite finishes even if the hero leaves; only past `88 m` unseen does it end early. Idle slot carries the single-playback `36.5 s` MournerMourn rite, walk slot the grieving gait; route, grave choice, spawn rule and phase clock are pure EditMode-tested functions. | `CemeteryMourner{Provider,Plan,Timeline,Presentation,Factory}`, `CityCemeteryMournerController`, `CityPedestrianAssetSetup`, `CityPedestrianModelImporter`, `build-city-pedestrian-3d-model.py`, `ai/city-zones-art-bible.md` §10c | Current |
| Cemetery watchman and gate lodge | A 15-part lodge (`cemetery-lodge-*`, new `Lodge` part kind reusing the concrete/timber/iron styles) stands in the gate-side pocket the planner reserves before benches, graves and trees: timber floor and roof, a watch window on the alley side, an ajar door, a stove pipe, a step and a stool; every blocking part clears the raw street-approach rectangle, and a blueprint whose gate lacks `8.3 m` of lateral room simply has no lodge. Under the eave beside the doorway, over the solid stretch of rear wall, hangs the plot's one warm fixture, the porch bulb (`cemetery-lodge-lamp`, `CityCemeteryLampKind.LodgePorch`) — the only light in the city that never switches off, dimming to a day floor through the registry's `dayIntensity` overload instead of dying with the night factor. On the doorstep beneath it an always-present staged NPC — the extremely snide old watchman (aerodrome cap, telogreika, kirza boots, grey whiskers, one raised brow; hands clasped behind his back through both authored loops) — stands 0.75 m out from the step and 0.30 m aside toward the alley, in front of the ajar leaf, facing the main alley rather than the arch behind his own booth; his stance is read back from the plan's own lodge parts. Talking to him (`E`, cashier-stub contract on its own trigger) serves the next line of a 15-quip seeded repertoire that never repeats back-to-back. | `CemeteryWatchman{Provider,Plan,Quips,Interaction,Presentation,Factory}`, `CityCemeteryPlanner.AddLodge`, `CityPedestrianAssetSetup`, `CityPedestrianModelImporter`, `build-city-pedestrian-3d-model.py`, `Resources/Localization/{ru,en}.json`, `ai/city-zones-art-bible.md` §10c | Current |
| Cemetery gravedigging | The watchman's one job, and it takes four acts on one plot, every one of them worked rather than pressed. Talking to him while the work is `Unclaimed` puts the offer up in place of the prompt (`E` accepts, `Q` refuses without spending it); accepting marks out the vacant plot nearest his post with a pulsing plate, four pegs and the lit kerosene hand lamp - `CityHandLampWorldBuilder`, literally the lake pier's fixture, turned `24` degrees off the grave's axis because it was set down and not installed. The lamp arrives with the job rather than with the hole: every act is timed by its light and the first one most of all, and it is picked up with the last spadeful when the earth goes back. The gear arrives with it too: past the foot of the grave - the only clear ground left once the heap has one flank, the digger and his camera the other and the lamp the head - the coffin waits on two timber blocks with the spade driven into the ground beside it, both placed by `CemeteryGravediggingPlan.CoffinRestGround` / `SpadeRestGround` and validated clear of the worksite. There is exactly one of each: the acts borrow `CemeteryGravediggingController.Spade` and `WaitingCoffin` rather than raising their own, so the spade visibly lifts out of the ground into the work and stands back in it afterwards, and the box that waited is the box that goes down the hole. Spade and blocks live to `Coffined`, the waiting coffin to `Dug`, and the last spadeful tidies all of it away with the lamp. Interacting with that site cuts the plot's rectangle out of the cemetery slab, lines the hole with box-projected fresh earth over a floor `1.6 m` down, heaps the spoil alongside. The second interaction lowers a six-sided timber coffin with an Orthodox cross on the lid — a model of its own, turned boards rather than a box, `1.95 m` long inside a `2.30 x 1.05 m` mouth. The third fills the hole in: the cut is removed and the slab made whole again, the pit dressing and the coffin go with it, and a fresh three-course mound plus one of the four single-grave monuments — from `CityCemeteryPlanner.CreateGraveParts`, slab omitted, stone and silhouette fixed by the plot's own hash — stand in their place. The lamp goes with them: it stood there because there was a hole to see into. Only that third act completes `QuestId.DigTheGrave`, and its feedback line is what sends the hero back to the gate. The watchman's window then pays `150` once, announced through the new formatted-feedback path (`ShowFormattedFeedback` keeps the catalog key and composes the number at the view's one resolution point) — the only place the player is shown the wage, since cash otherwise lives two menus deep. The whole worksite is a pure function of `GameSessionState.GraveWorkStage`, so every stage is restored exactly on each city build. An invisible cap holds the hero out of a pit he cannot climb out of. Each act runs as a modal session (`CemeteryGraveWorkController`) that takes `BarMinigameModalLock`, blends the camera onto the grave and leases the hero out of sight so the spade is the only animated thing - an accepted deviation from `ai/contextual-animation-standard.md`. Digging and filling share one `3 x 2` lattice of three courses each (`18` in all), and both are a choice of square and a press: pick a segment with `A`/`D`, press `E`, one course goes. The whole difficulty is the lattice rule - no segment may go deeper than its shallowest neighbour, which forbids pillars and cannot deadlock - and it is read in the hole rather than on a panel, because `CityCemeterySegmentFrameWorldBuilder` outlines the chosen square down there at the height its earth currently stands. There is no timing bar in either act and no kinds of ground: both had one, and both were cut at the user's direction as the same shot demanded eighteen times over a distinction the hero could neither see nor answer. The HUD for both is one line naming two keys. `CityCemeteryProgressivePitWorldBuilder` states the half-dug hole as one earth block per segment from the floor to its working face, so a stroke costs one small combined mesh and never a rebuild of the cemetery slab. The coffin comes down on four slings over the open mouth with no timber across it, paid out by the hero - `Q` the left of the shot, `E` the right, bound to screen sides rather than to head and foot because which end is which depends on the plot's heading against the digger's side. Nothing held means nothing moves. What makes it a game is that the balance point at which the box hangs level crawls as the fresh ground gives under one bearer and then the other, further than the tolerance is wide: standing still loses it because level walks out from under a box that never moved, and holding both keys loses it because the two rates differ and the point keeps going. Rope only ever goes out, so every correction is paid for in depth. The gauge draws the moving band and a needle for the balance he controls. One constraint is load bearing and stated in the settings: the point must crawl slower on average than the slower rope can move the balance, or chasing it is not hard but impossible - an earlier pass had exactly that. The stone lies flat by the head from the moment the job is taken and the lamp and spade now live to `Sealed`, because the last act is worked by the same light and driven home with the same spade: press `E` to heave the monument upright (dead weight, no window, sags when let go), then three timed blows from above set it. The camera leans toward the side the stone lies on for that, and walks round to the front of the monument for the plaque. The plaque carries three lines - a name nobody gave, a span nobody knows, and one the player types, held to eight words by `CemeteryEpitaph` and written once through `GameSessionState.TrySetGraveEpitaph`. The board is fixed to the stone's own measured front face - found by walking the monument's triangles down in bands from `0.74` to `0.44` of its height and taking the first band solid and wide enough to carry a plate, which is what puts it on the head of a stele and between the arms of a cross rather than on the plinth - and carries its words as world-space TextMeshPro: `CemeteryPlaqueFont` builds a dynamic-atlas font asset at runtime from `Roboto-Regular` (Apache-2.0, full Cyrillic) in `Assets/Resources/Fonts`, so anything a player can type has a letter and no generated atlas is committed. TMP Essential Resources are imported under `Assets/TextMesh Pro`, which is what `TMP_Settings` needs to exist at all. There is no field to type into: the line is taken off `Keyboard.onTextInput` and appears on the brass as it is typed, and reading a finished board takes the modal lock and brings the camera to it rather than opening a panel. The board is fitted only once the stone is upright, because `Attach` measures the monument to find its face. `CemeteryPlaqueReadInteraction` stays on the finished grave for reading it again. A session commits nothing until it finishes and takes back everything it built on `Esc`, so `GameSessionState` still carries only the stage; `Filled` exists as its own rung because closing a hole cannot be undone. | `CemeteryGravedigging{Plan,Controller}`, `CemeteryGraveWorkStage`, `CemeteryGraveDigSiteInteraction`, `CityCemetery{GroundWorldBuilder,GroundExcavation,PitWorldBuilder,CoffinWorldBuilder,SealedGraveWorldBuilder}`, `CityHandLampWorldBuilder`, `InteractionPromptView.ShowFormattedFeedback`, `CityCemeteryWorldBuilder.BuildPartBatches`, `CityCemeteryPlan.Plots`, `CemeteryWatchmanInteraction`, `GameSessionState.TryEarnCash`, `CemeteryGraveWork{Controller,Stance,View}`, `ICemeteryGraveWorkSession`, `CemeteryGraveLatticeModel`, `CemeteryStrokeModel`, `CemeteryCoffinLowerModel`, `CemeteryStoneSettleModel`, `CemeteryShovelAnimator`, `CemeteryGraveSlings`, `CityGravediggerShovelWorldBuilder`, `CityCemeteryProgressivePitWorldBuilder`, `CityCemeterySegmentFrameWorldBuilder`, `CemeteryPlaque{Font,Surface}`, `BarMinigameModalLock`, `PlayerPresentationVisibility` | Current |
| Lake precinct | Dedicated deterministic dressing of the `4 x 4` north-eastern lake as an abandoned municipal boat station. The blueprint grid is untouched; the builder insets its own waterline inside the `2 x 2` water cells — a `36 m` octagon with `5.2 m` cut corners instead of a `52 m` square — and fills the remainder with a walkable clay bank ring (level shelf plus a `21%` откос) over a colliderless silt bed cap. A continuous tarred timber revetment stands `0.59 m` above the bank toe around the whole waterline, cut only for the pier deck that bridges it and the chained slipway, with a validated continuity contract so the precinct can have no invisible edge; the generic terrain guard rail is skipped there in consequence. Because that bank sits on cells the blueprint marks `Water`, and a water cell is never walkable, the precinct also contributes its bank ring and pier deck to the walkable mask through `CityLakePlanner.AppendWalkableFootprints` — strips grown `1.2 m` past the cell line so they overlap the shore rather than abut it, plus an eight-step staircase filling each cut corner; the pond itself is never contributed. Without it the player is clamped at the water-cell boundary and the whole station is sealed behind a `52 x 52 m` invisible box. An `18 m` single-railed pier walks out on piles that reach the bed and ends in the station's one working light: a small kerosene-warm hand lamp standing on the rail cap at the very end, object-scale rather than a municipal post. That fixture is `CityHandLampWorldBuilder` and is shared with the gravedigger's worksite lamp — one lamp, built once. The shore carries no fixtures at all, and that lamp never switches off, day or night — its glass is deliberately kept out of the glow registry so it reads lit at noon, and it is what puts the only highlight on the water; four upturned hull variants sit hauled up on rests; the rental hut faces the gate with a boarded hatch and a «ПРОКАТ ЛОДОК» board. Plank/bank/hull batches carry three measured `CityLake*Albedo` sheets. | `CityLake{Plan,Planner,WorldBuilder,SurfaceAppearance,Resources,BankMeshFactory}`, `CityWaterSurfaceFactory.CreateBevelledSurface`, `CityWaterResources`, `CityTerrainSafetyWorldBuilder`, `tools/build-city-lake-textures.py`, `ai/city-zones-art-bible.md` §10d | Current |
| Lake still water | The lake takes the river's shader as a second material told there is no current. `_FlowDirection` zero now genuinely means still: the crest phases freeze and the trains breathe in place, the ripple sheet walks a closed orbit instead of drifting, and the middle train turns across the other two so the standing pattern is a lattice rather than corduroy. Its ripple map is a separate isotropic sheet whose directionlessness is measured (`slopeAnisotropy`, bounded `0.80..1.25`) rather than asserted, its foam is a green duckweed scum, and it alone switches on `_AdditionalSpecular`, which reads URP additional lights so the shore lamps lay a glitter path on the water — a lit highlight, not a mirror, so it exists only within a fixture's own range. Fog moved to the fragment stage: interpolating it per vertex banded the sheet. Night factor and rain reach both bodies through one registry. **Gap:** a faint low-amplitude contour banding (~3% of range) remains on the surface at grazing angles; it is below the PS1 composite's dither in practice but not diagnosed. | `CityLakeResources`, `CityWaterResources`, `CityRiverWater.shader`, `CityRiverResources`, `tools/build-city-lake-textures.py` | Partial |
| Lake fisherman | The boat station's one permanent inhabitant: a staged NPC standing at the end of the pier on the rail-free side, tipped out over the end board, facing the water with his back to the shore, his stance read back from the plan's own deck parts. Talking to him (`E`, cashier-stub contract on a trigger docked behind him rather than in front) serves the next of a 15-line seeded repertoire that never repeats back-to-back. His register is the watchman's exact inversion and is enforced as a rule, not a mood: never the second person, never a question, at most `55` characters, weather and superstition only. The staged art is a hooded man in a municipal-yellow oilskin — the precinct's one worn saturated colour — with a rod in both hands and a smouldering pipe in his teeth. Both fists really are on the stick: the rod is one rigid part on the right hand, so the arm angles were fitted numerically against `ACC_RodGrip`/`ACC_RodTip` rather than eyeballed, and the loop breathes on the spine chain only, because a breath authored on the clavicles opens his hands off the rod once per lap. The pipe ember, its short warm point light and the plume are all driven from that clip's own phase (`4` breaths per `8 s` lap, peak draw at mid-breath, plume lagging `0.18` of a breath), never from a second timer. The line is struck from the live rod tip to the waterline the lake plan measured. | `LakeFisherman{Provider,Plan,Quips,Interaction,Presentation,Factory,RigAnchors,PipeEffect,Line}`, `CityPedestrianAssetSetup`, `tools/build-city-pedestrian-3d-model.py`, `Resources/Localization/{ru,en}.json`, `ai/city-zones-art-bible.md` §10d | Current |
| Park chess set inhabitants and wire lamp | The Central Park chess set gets its two permanent inhabitants and its one light. Two old men are seated on drawn bench planks, one per table, on seats that are each other rotated 180 degrees about the middle of the set (`-seat-a1` and `-seat-b2`): elbows on the board, head in both hands, turned toward one another across the set and neither looking up. The two remaining planks stay unclaimed and sittable, which is the point of the place — the hero takes either one across the board from its man, in past the plank end (see the game-table seat row). Each stance is read back from the same `AppendBenchSeats` call the hero's sit interaction uses, and each man claims his own seat through `CityBenchSeatClaims` for the life of the City. They are the art library's only bench sitters, so the generator grew `perch_seat_height_m` — hip underside to soles, proved equal to the drawn `0.540 m` plank across all 288 frames of both loops — and the runtime stands the seat of each coat on the timber every frame rather than pinning a sole. Chess and draughts share one board, so each reference runs on two channels re-derived from the piece: a king's tulle against one thick draught worn flat and raked back, and a bone check against bone circles run on the diagonal. Above the set a wire crosses between two park trees (across the tables, not along them: the tree grid leaves no aligned pair on that line) carrying two enamel cones, of which one still burns over the set's middle and covers both boards, weak and warm, day floor included. No talk stub yet — but see the row below: they are not silent, they simply never address the hero. | `ParkChessPlayer{Provider,Plan,Presentation,Factory}`, `ParkCheckersPlayer{Provider,Plan,Presentation,Factory}`, `CityParkChessLamp{Plan,WorldBuilder}`, `CityDecorationWorldBuilder.AppendBenchSeats`, `CityBenchSeatClaims`, `CityPedestrianAssetSetup`, `tools/build-city-pedestrian-3d-model.py`, `ai/city-zones-art-bible.md` §10 | Current |
| Park chess-set men | Both boards carry a full set of turned 3D pieces: `32` chessmen on the chess player's table in the starting position and `24` draughts on the neighbour's, three rows a side on the dark squares. Seven authored meshes (`1910` triangles for the lot) built by `tools/build-city-chess-set-3d-model.py`, sized entirely from the drawn `0.15 m` square — king `215 mm`, draught `23 mm` — imported readable and combined by the runtime into four meshes, one per colour per board, on the board's own timber sheet. Placement is proved rather than eyeballed: `a1` dark, a light square in each man's near-right corner, queen on her own colour, back rank `RNBQKBNR`, knights turned at the opponent, draughts on dark squares only. A seeded few millimetres and few degrees per man reads as a set laid out by hand; nothing ever moves. **The colouring of the drawn board was corrected here** — both men had a dark square on their right, which no empty board shows. | `CityChessBoardGeometry`, `CityChessSetPlan`, `CityChessSetWorldBuilder`, `CityChessSetProvider`, `CityChessSetAssetSetup`, `RuntimePrimitiveFactory.CreateCombinedMeshes`, `CityDecorationWorldBuilder.BuildChessBoard`, `tools/build-city-chess-set-3d-model.py` | Current |
| Park board games | Both boards are playable. Taking either free plank starts or resumes a real match on that table: full legal chess on the chess player's, full Russian draughts on the neighbour's. The hero is dark at both, because dark is what the drawn set already had nearest the free plank, so the old man opens. The camera drops to the hero's own view, leaning right over the stone (`1.06 m` over the plank and `0.34 m` forward of it, `72` degrees; yaw `+-55`, pitch `-6..75`), with the resting pitch derived as the bisector of the board's near and far edge angles so the field is centred in the frame by construction. The whole head comes off so he can look through it — by rig rule (`head`, `neck`, `face.*`) rather than by mesh name, because the head is twenty-two meshes — and nothing below the collar is ever hidden. The table's two static batches are swapped for one object per man on the same seven meshes and the same timber sheet, carried one step per jump with a lift arc, and the taken man is swept off. Picking is a plane read, not sixty-four colliders: LMB or a keyboard cursor selects, legal destinations light as dots and captures as red plates, RMB turns the head, `E` still stands up and `R` restarts a finished game. Nothing is drawn over the board. Every state a player has to know — greeting, whose move, check, promotion, a man taken, why a click was refused, the result, the offer of another game — is a line the man opposite speaks, ten cues of two lines each per board, rate-limited to one every `3 s` with the result and the offer forced through; `CityParkQuarrelController` is suppressed for the sitting, because one bubble anchor cannot carry both a game and a decade-old argument. The engines are negamax with piece-square tables (chess, depth `3`, quiescence over captures) and material/advancement (draughts, depth `5`, forced-capture extension), both capped by a node budget and both finishing with a random pick inside a slack window — which is what makes the old man beatable rather than perfect. Positions persist for the life of the City. | `ChessRules`, `ChessEngine`, `ChessMatch`, `DraughtsRules`, `DraughtsEngine`, `DraughtsMatch`, `BoardGameContracts`, `CityBoardGamePlan`, `CityBoardGamePieces`, `CityBoardGameMarkers`, `CityBoardGameController`, `CityBoardGameView`, `CityChessSetMen`, `Player3DHeadVisibility`, `CityBenchSitInteraction.SeatedChanged` | Current |
| Park chess-set quarrel | The two men hate each other on the only grounds available: one plays chess and despises draughts, the other the reverse. While the hero is within `22 m` of the middle of the set (`25 m` to stop — hysteresis), one of them shouts every `5 s`, strictly alternating, opener drawn from the city seed. The shout is an authored two-second beat (`ChessJeer`/`CheckersJeer`) mixed over the brooding loop: head out from under the hands and `64°` round to the neighbour, left arm thrown up, held, and dropped slower than it rose. Both men share one unmirrored pose because their two seats are a 180-degree rotation of each other, which puts each one's neighbour `2.2 m` ahead and `3.7 m` to his own left. The line appears the frame the clip crosses its full-extension key, types out at `34` characters a second in an IMGUI panel over the speaker's live head bone, and stays up until the neighbour answers — one bubble on screen at a time. Twelve localized lines each, seeded so neither repeats back to back. Leaving earshot cancels the beat and the bubble; returning restarts after `1.2 s`. Sitting down at either board suppresses the whole quarrel until the hero stands again. No sound. | `CityParkQuarrelController`, `ParkQuarrelTimeline`, `ParkQuarrelTaunts`, `NpcSpeechBubbleView`, `ParkChessPlayerPresentation.BeginTaunt`, `ParkCheckersPlayerPresentation.BeginTaunt`, `CityPedestrianAssetRegistry.ActionClip`, `RetroUiTheme`, `LocalizationService` | Current |
| City world builder | Build only canonical blueprint surfaces: triangulated continuous ground/beach meshes, conforming park plazas, flat district pads with `4 m` terrain blends, graded streets, flat special precincts, local lake/sea water, the river, stairs and guarded drops. Buildings and public slabs extend foundations downward while authored tops stay fixed; colliders exist only on physical surfaces. | `CityWorldBuilder`, `CityTerrainSurfaceWorldBuilder`, `CityRiverWorldBuilder`, `CityStreetSurface{Plan,Planner}`, `CityOpenArea{DecorationPlan,WorldBuilder}`, `CityDistrictPointOfInterestWorldBuilder`, `CityExteriorAppearance`, `RuntimePrimitiveFactory` | Current |
| City street surface presentation | Road v2's `8 m` street footprint with a `6 m` carriageway between two raised `1 m` sidewalks; Road v2.1 exposes a flush `8 x 8 m` asphalt apron at selected bus nodes. Shared albedos through MPBs, no material instances. | `CityGenerationSettings`, `CityStreetSurface{Plan,Planner}`, `CityStreetIntersectionSelector`, `CityBusIntersectionSelector`, `CityExteriorAppearance` | Current |
| Central Park surfaces | Eight packaged albedos cover the whole park: lawn, path and plaza project down at their own metre pitch; trunks, canopies, hedges, benches and the four landmarks (fountain, bandstand, chess tables, playground) use box-projected world UVs. Compensated tints preserve every authored colour; the Home view repeats the path. Landmarks ride a second batch axis inside the flat decoration layer. | `CityParkSurfaceAppearance`, `CityWorldBuilder`, `CityDecorationWorldBuilder`, `CityTerrainSurfaceWorldBuilder`, `RuntimePrimitiveFactory`, `HomeExteriorViewBuilder` | Current |
| City and Home street pedestrians | One deterministic radius-safe sidewalk/zebra graph feeding a per-runtime population profile (City `8` day / `3` night over `13` pooled models, Home balcony `5` / `2` over `8`), filled in batches of two on a short cadence and dispersed by lane and peer distance. Each slot takes a seeded pick from an ordered five-archetype catalog; ordinary designs repeat, the airborne, the single lamp-bearing and the four bus-riding designs are declared per archetype, and the lamp design caps at one pooled instance. Fog-band spawn and recycle never depend on camera direction or frustum. | `Runtime/City/NPC`, `CityPedestrianPopulationProfile`, `CityGameRoot`, `HomeInteriorRoot`, `RoadWalkableArea`, `Resources/Pedestrians/{CityPedestrian3D,ChairCarrierPedestrian3D,KettleHatPedestrian3D,LongArmPedestrian3D,HelmetLampPedestrian3D}` | Current |
| City Route 01 bus | One immutable right-hand Street-only, grade-safe loop through every district POI and then Home, with flat junction/stop pads and 3D samples driving actor height and pitch; one pooled real-scale actor owns folding doors, driver, engine, sprung body, night lights and rain-driven wipers. **Gap:** the runtime is City-only — Home reconstructs a static pole, and the map has no live vehicle marker. | `Runtime/Vehicles`, `CityBus{Plan,Planner,Actor,Director,Presentation,Factory,AssetRegistry}`, `CityBusTargetRoutePlanner`, `CityBusWideTurnPlanner`, `CityMapBusOverlay` | Partial |
| Pedestrian bench rests | Every `3.5 s` one roaming walker near a free sittable bench may divert to it (p `0.4`, at most `2` at once, `30 m` graph reach): Dijkstra guidance to the bench's node, a short scripted off-network crossing (max `6 m`, capsule released like a bus doorway), a seated `15-30 s` rest on the bench anchor and the same crossing back. Seats are shared through one claim registry with the hero's sit interaction; the hero's yard bench is excluded by design. | `CityBenchRest{Plan,Planner}`, `CityBenchNpcRestController`, `CityBenchSeatClaims`, `CityPedestrianActor`, `CityBenchSitInteraction`, `CityGameRoot` | Current |
| Route 01 passengers | A three-place cabin counting the hero: he boards either fully open door into reserved window seat `07` under the authored `BusBoardEnter`/`BusRideLoop`/`BusAlightExit` and a roll-free seated camera, while up to two ambient walkers wait beside a stop pole, board seated and alight at a seeded later stop. **Gap:** fare, destination choice, passenger persistence and live tracking are out of scope. | `CityBusRide{Plan,Controller}`, `CityBusStopWait{Plan,Planner}`, `CityBusNpcPassengerController`, `CityBus{Actor,Director}`, `CityPedestrian{Actor,Presentation,Resources}` | Partial |
| City visual diversity and collision proxies | One stable ordered decoration plan over 24 visual families expanded into six shared-material styles per `48 m` chunk, with a per-kind `None`/`Detail`/`Blocking` proxy catalog for grounded structural recipes only. Phone booths and dumpsters repeat under spacing/coverage rules (`55/90 m` booths; `40 m` and `65/100 m` dumpsters) and expose recipe-derived interaction docks. The park playground is the one recipe with a moving part: its frame, cross beams, bench and two A-frame proxies are baked as usual, while its two seats hang outside the batch as hinged rigid bodies (`±50°`, ropes drawn taut from the top beam) that anything with a `CharacterController` pushes up to its own walking pace. | `CityDecoration{Descriptor,Plan,Planner,Validator,WorldBuilder}`, `CityDecorationCollisionCatalog`, `CityStaticCollisionBuilder`, `CityStreetUtilityDock`, `CityPlayground{Geometry,Swing,SwingBuilder}` | Current |
| Road-edge fences | Street-union rails only where the outward side is water, unmapped or outside the active footprint, plus caps at true Street terminals; river promenades support their bank roads, while bridge decks are support-only and keep their authored parapets/stair gaps. | `RoadFence{Planner,Plan,WorldBuilder}`, `CitySurfaceDescriptor`, `CityRiverPlan` | Current |
| Supermarket interior and finite shelf purchases | A runtime-composed `16 x 11 x 3.6 m` shop holding exactly one unit of five products; a modal fixed-camera browser commits an atomic cash/inventory/source purchase and removes the physical unit, and sold IDs stay absent until a new game. | `SupermarketInterior{LayoutPlan,LayoutPlanner,LayoutValidator,WorldBuilder,Root}`, `Supermarket{ShelfView,ProductView,ShelfShopController}`, `SupermarketPurchaseRules` | Current |
| Supermarket watcher cashier | One bespoke animation-free 3D clerk on the shared 31-bone Avatar behind the checkout: the five re-parented PIVOT_Neck segments lie along a quadratic pursuit curve, so the head — pinned to the curve tip by its authored attachment point — travels the whole hall (up to `18 m` of neck) to hover beside the hero, the sample-verified cubic staple climbing over any shelf or counter in the way; a caught glance reels it back with hysteresis, pinches the pupils and freezes the `6.5 s` blink cycle. The prefab is passive (no colliders/lights/audio) and reachable only through its provider asset; a separate talk-stub trigger answers `E` with a placeholder line. | `SupermarketCashier{Provider,AssetRegistry,Factory,Presentation,Actor,SurveillanceState,BlinkState,Interaction}`, `tools/build-supermarket-cashier-3d-model.py`, `SupermarketCashierPlan` | Current |
| Supermarket CCTV corner cameras | Four chunky ceiling-corner camera units resolved from the room plan; each head servos at `240°/s` toward the hero (aimed from the very first frame), with a fake-emissive recording LED, no colliders and no real light. | `SupermarketSecurityCamera{,WorldBuilder}`, `RuntimePrimitiveFactory`, `SupermarketInteriorRoot` | Current |
| Supermarket fluorescent atmosphere | An explicit shadowless six-practical budget over the one directional fill: a cold point under each fluorescent row, one warm accent over the checkout and cashier, a cool cold-shelf spill, and one row flickering on a deterministic stepped pattern that also dims its fake-emissive tube. | `SupermarketInteriorAtmosphere`, `SupermarketFluorescentFlicker`, `RuntimeSceneSetup.EnsureSupermarketInterior` | Current |
| City day/night presentation | Apply the shared time sample to City directional/ambient/reflection lighting and the lamp/bar-light night factor; the same factor dims the five lit-window family materials and every registered electric glow (neon, sign lightboxes, porch/site lamps, the hero's balcony window) to a `0.10x` dead tint by day. Always-on exceptions are explicit: the yard Spot, traffic signals and the weighbridge indicator face. Fog, backdrop, `48 m` far clip and grade stay fixed, and the worst-case local realtime budget is `18` (`12` atmosphere + `4` bus + `1` helmet + `1` yard), with directional sources separate. | `RuntimeSceneSetup`, `CityDayNightController`, `GameTimeDayNightRules`, `CityNight*`, `CityWindowAppearance`, `CityNightGlowRegistry`, `CityOpenAreaWorldBuilder`, `CityFogField`, `CityLightHalo` | Current |
| Deterministic exterior weather | A pure `(seed, absolute game minutes)` schedule of `90`-minute clear/light-rain/heavy-rain/thunderstorm slots with `5`-minute smoothstep ramps drives a player-following rain streak field, a synthesized rain bed, and per-`12`-minute hashed lightning strikes with a transient directional flash and distance-delayed synthesized thunder; one shared wind sample (slot-hashed bearing, weather-kind strength, continuous deterministic gusts and sway) leans the rain drift in both scenes and pushes every registered runtime cloth. The balcony shot shows and sounds the same weather, a bus-riding hero gets a rain-free cabin core, and the fixed fog/grade/day-night lighting contract stays untouched. **Gap:** rain does not yet dim ambient daylight or wet surfaces. | `GameWeatherRules`, `CityWeatherController`, `CityRainField`, `CityClothWindRegistry`, `CityRainSound`, `CityThunderSound`, `CityLightningFlashLight`, `CityGameRoot`, `HomeBalconyExteriorAtmosphere` | Partial |
| Runtime cloth rags | Skinned PhysX-cloth panels built at runtime (top row pinned, mesh owned through the generated-mesh owner): the simulated topology is strictly single-sided because cloth recomputes particle normals from every triangle and a reversed same-vertex duplicate cancels them into specular glitter; the back face comes from one shared cull-off clone of the primitive material with per-renderer matte MPBs. Six authored torn rags hang from the Last Route island's broken canopy in City only — the home-exterior vista stays cloth-free — and take the weather wind through the registry. WindZone is unused by design: PhysX cloth only reads its own acceleration vectors. | `ClothPanelFactory`, `CityClothWindRegistry`, `CityDistrictPointOfInterestWorldBuilder`, `RuntimePrimitiveFactory` | Current |
| Scene music | One scene-owned looping theme per root through a low-pass filter and the shared `Music` group, with unscaled fade envelopes, preload-then-activate handoff and Balcony pause/resume at Home. **Gap:** only `city_theme` and `bar_theme` ship; supermarket, stairwell, home and smoking are optional empty slots. | `SceneMusicPlayer`, `HomeMusicPlayer`, `HomeSmokingMusicPlayer`, `SceneTransitionService`, `Resources/Audio/*` | Partial |
| Common audio mix | One Resources mixer for music, ambience, world/gameplay SFX and UI, with `-6 dB` master headroom, reverb/echo returns and per-scene snapshots. | `GameAudioMixer`, `BarPromenadeAudio.mixer`, `AudioMixerAssetSetup`, scene roots | Current |
| Retro SFX and ambience | Deterministic generated mono `22050 Hz` UI/world/bar effects with bounded pools and cooldowns, plus layered Home and Stairwell soundscapes on layout-derived spatial anchors. | `RetroSfx`, `RetroAudioService`, `RetroAmbience`, `InteriorSoundscape{Synthesis,AnchorPlanner}`, `{Home,Stairwell}Soundscape` | Current |
| Home alarm clock | One validated bed-relative clock and nightstand reusing a 28-segment display: flicker frozen `05:59`, switch to solid `06:00` only on Wake, then follow session time without rebuilding geometry. | `HomeAlarmClock{Plan,Builder,Synthesis}`, `HomeAlarmClock`, `GameSessionState`, `GameAudioMixer` | Current |
| Road, park and ground navigation | Constrain player XZ motion to streets, park, `OpenLand`, `BuildableGround`, both river promenades, bridge decks and four lower stair landings; water stays excluded. Road/ground and ground/ground connectors and guards use sampled endpoint heights, while a pure audit inventories every seam/frontage and its authorized spawn-road component. | `RoadWalkableArea`, `CityGroundTraversalPlan{,ner}`, `CityRoadGroundBoundaryPlan`, `CityVerticalTraversalPlan`, `CityVerticalTraversalAudit`, `CityRiverPlan`, `CityTerrainSurfacePlan`, `PlayerMotor` | Current |
| City route planning | Deterministic ordered shortest paths over generated street and park-path edges with a binary min-heap. | `Runtime/Map`, `CityLayout` | Current |
| Player motor | Camera-relative movement to `2.6 m/s` with acceleration and braking, plus a bounded scripted approach that keeps the ordinary gait, settles at an exact authored pose and reports a stall instead of teleporting. | `PlayerMotor`, Input System, `CharacterController`, `IWalkableArea` | Current |
| Third-person chase camera | Close exterior/interior framing with damped yaw, bounded `-20°..55°` pitch and focus; RMB mouse or right-stick input orbits on both axes, while idle motion, status reactions and obstacle shortening never rotate the player. | `PlayerCameraFollow`, `IntoxicationStageRules`, `BarMinigameModalLock` | Current |
| Home fixed camera | Three authored MainRoom/Bathroom/Balcony poses hard-cut with hold hysteresis instead of following the player; only quarter-strength status rotation around the fixed base pose. | `HomeCameraShot{,Selector}`, `HomeFixedCameraController`, `PlayerCameraFollow` | Current |
| Home player visibility | Explicitly registered furniture, door and rail renderer groups dither toward an authored alpha floor when they cross a camera ray to the hero, then restore; every collider and GameObject stays intact. | `HomeOcclusion{Registry,Resolver}`, `HomePlayerOcclusionController`, `HomeOccluderDither` material/shader | Current |
| Modular 3D hero presentation | One `Player3D.prefab` in all five gameplay roots: `1.75 m` Generic rig, 73 independent meshes, 31 bones and one shared material with per-mesh palette blocks, driving a damped Idle/Walk blend plus additive status poses and grounded boot pinning. | `Player3D{Resources,AssetRegistry,CharacterPresentation,RagdollController}`, `PlayerFactory`, `Assets/Player3D`, `tools/build-player-3d-model.py` | Current |
| Silent Hill attention | The hero's head turns toward noticeable interactables, pedestrians and magnet-marked characters inside a `3.6 m`/`±75°` cone with wider release hysteresis, people-first ordering and no overhead targets (a focus above `2.1 m` over his feet is never noticeable); the glance is an additive post-animation head/neck turn (`62/38`, yaw `±68°`, pitch `-32°..+10°`) that stands down under modal clips, handoffs and the ragdoll. | `PlayerAttention{Rules,Controller,Magnet}`, `Player3DCharacterPresentation`, `PlayerFactory`, `YardWheelchairFactory` | Current |
| Continuous 3D player interactions | The shared `Positioning -> Enter -> Loop -> Exit` path with independent authored entry/action/exit poses, sample-then-align pelvis anchors, moving-target support and owned idempotent cleanup. Normative contract: `ai/contextual-animation-standard.md`. | `PlayerAnimatedInteraction{Timeline,Controller}`, `IPlayerClipPresentation`, `Player3DCharacterPresentation`, `PlayerMotor` | Current |
| City bench and park game-table seats | One trigger per sittable plank in the City — yard, park, cemetery, points of interest, bus shelters and the seats hidden in street decorations — all derived from the drawn timber and shared with the pedestrian rest claims. An ordinary plank is backed onto from the front under the bus seat's own clips. The two free park game planks cannot be: the table is one solid block from board to grass and out past both planks, so those dock off the plank end on the sitter's right, route onto that single lane, go in under `ChessSeatEnter`/`ChessSeatPlayLoop`/`ChessSeatExit`, and offer the game on the board (`interaction.play_chess` / `interaction.play_checkers`) instead of a sit. | `CityBenchSit{Plan,WorldBuilder}`, `CityBenchSitInteraction`, `CityChessBoardGeometry`, `CityStaticCollisionBuilder`, `PlayerAnimatedInteractionController` | Current |
| Home bed sleep | One trigger on the door-side bed edge; guided walk, neutral settle, then `BedEnter`/`BedSleepLoop`/`BedExit` through a real bedside sit. Normal completion clears session fatigue; cancellation preserves it. | `HomeBedInteraction{Plan,}`, `PlayerAnimatedInteractionController`, `GameSessionState`, localization catalogs | Current |
| Home balcony smoking | Balcony dock with guided city-facing entry, `SmokeEnter`/`SmokeLoop`/`SmokeExit` on a socket-bound cigarette, periodic mouth plume, a permanent rail ashtray and a bounded city-biased camera push-in with drift and exact restoration. | `HomeBalconySmoking{Plan,Interaction,Timeline,CameraDrift,ExhaleEffect}`, `HomeBalconyWorldBuilder`, `HomeSmokingMusicPlayer` | Current |
| Home refrigerator | A derived cabinet, clear approach, first-person pose and eight storage slots; a modal open → browse → inspect → close flow driven by a prefab-derived right-arm subset under a world-visibility lease, where `Take` commits atomically. **Gap:** `Use` is registered but unavailable. | `HomeRefrigerator{Plan,WorldBuilder,View,Interaction,InteractionTimeline,FirstPersonHand}`, `HomeRefrigeratorItem*`, `Player3DFirstPersonSubset` | Partial |
| Player shadows | Every production mesh casts and receives ordinary URP shadows, while one shared analytic contact quad raycasts onto the live surface normal and expands through both authored and physics fall phases. | `Player3DCharacterPresentation`, `Player3DRagdollController`, `PlayerContactShadow{,.shader}`, `PlayerPresentationMetrics` | Current |
| Interaction/UI | Select nearby entrances, exits, bed, smoking dock, refrigerator, stations, counter and bus triggers; localized world prompts are full pointer targets repeating the same availability guards and action path as E/Enter/gamepad South. | `PlayerInteractor`, `InteractionPromptView`, `InventoryTargetInteractionController`, `CityBusRideController`, `Runtime/{Interaction,UI}`, `RetroUiTheme` | Current |
| F9 debug controls | City/Bar open the debug window for `±20` intoxication steps; City also toggles map teleport. Home instead skips directly to City and opens that map mode after the transition. | `MinigameDebugWindow`, `HomeDebugCityMapShortcut`, `CityMapController`, `GameSessionState` | Current |
| Structured session diagnostics | One bounded UTF-8 NDJSON stream with build/session/scene/seed context and correlated transition and Unity-message events; rotates at 5 MiB keeping three archives, `F8` snapshots. Format: `ai/debug-log.md`. | `Runtime/Diagnostics`, instrumented state boundaries, `MinigameDebugWindow` | Current |
| Bar activity flavour | Assign the first four stable row-major bars distinct activity kinds through one pure resolver (later bars fall back to `Cocktail`); after the minigame cut the kind only flavours interior dressing such as the beer-pong table and stage. | `BarActivityKind`, `BarActivityAssignment`, `BuildingLot`, `GameSessionState` | Current |
| City map UI | Draw canonical surfaces, river/promenades, three bridge styles, player arrowhead, destinations, itinerary and Route 01 with clipping and X/Y pan. Non-urban precincts are drawn rather than captioned — outline, motif, marked street opening, and the lake's authored waterline, pier and hut from the built `CityLakePlan`; no place name is printed, every name is a hover tooltip. Test teleport selects every lot and, through one appended index band, every open precinct - clicking any of its cells arrives a stride inside its authored street gate, clamped to the walkable mask. Home F9 opens it on City arrival. No live bus marker. | `CityMap{Controller,View,Viewport,AreaOverlay}`, `CityMapBusOverlay`, `MinigameDebugWindow`, `HomeDebugCityMapShortcut` | Current |
| Scene transition | One guard across the full transfer: load `DoorTransition` in Single mode, preload the destination with activation blocked, and activate only at the final blackout. | `SceneTransitionService`, `SceneIds`, `GameSessionState` | Current |
| Door transition presentation | A deterministic `3.15 s` unscaled fixed-camera door sequence in a black void, tinted warm on entry and cold on exit, with generated latch and hinge cues. | `DoorTransition{Root,Timeline,Direction}`, `RuntimeSceneSetup`, `RetroSfx` | Current |
| Session state | Reset a complete run on launch, then preserve blueprint ID and seed, active bar and activity, ordered route, return kinds, clock and day index, cash, needs, intoxication and the balance sequence across scene loads. | `GameSessionState`, `CityBlueprintCatalog`, `GameTimeState`, `PlayerNeedsProgressionState`, `CityReturnKind`, `HomeArrivalKind` | Current |
| Bar drink retail and physical service | Nine localized drinks at fixed prices through a seated first-person counter: nine physical bottles, an atomic cash/drink commit, prefab-derived arm subsets, a world-space pour and a three-second drink from the matching vessel. | `BarDrink{Catalog,PresentationCatalog,ServicePlan,ServiceTimeline,ShopController,BottleView,VesselView,FirstPersonArms}`, `DrinkPurchaseRules`, `BarCounterStation` | Current |
| Intoxication stages and presentation | Map `0-100` into Sober plus five named 20-point stages driving movement speed, additive 3D bone poses, camera and world-composite effects, with recovery that accelerates toward sober and pauses under modal ownership. | `IntoxicationStageRules`, `IntoxicationStatusController`, `IntoxicationRenderState`, `IntoxicationHudView`, `Player3DCharacterPresentation` | Current |
| Balance checks and falling | Above `60`, a seeded fixed-step arrow challenge that never starts on a surface steeper than `12°` and never disables locomotion until failure; failure plays a directional Fall clip, hands the rig to 13 kinematic-to-dynamic bodies, then blends into a full-body side-specific Rise. The gameplay root and `CharacterController` stay upright and fixed. | `BalanceChallengeModel`, `IntoxicationStatusController`, `BalanceCheckView`, `PlayerFallAnimationTimeline`, `Player3DRagdollController`, `PlayerContactShadow` | Current |
| Bar interior layout and world | One deterministic validated `22 x 16 x 4.8 m` layout with seven zones, four connected clear paths, one reachable activity fixture and one counter station, composed into shell, counter, seating, stage and activity dressing. | `BarInteriorLayout{Plan,Planner,Validator}`, `BarInteriorWorldBuilder`, `BarDrinkServiceWorldBuilder`, `BarInteriorRoot` | Current |
| Player home interior | A validated `10 x 8 x 3.4 m` main-room and bathroom plan with clear entry, main and bathroom paths and furniture outside protected circulation, plus a real window and open door onto a walkable balcony at `4.7 m` street elevation. | `HomeInteriorLayout*`, `HomeBalconyLayout*`, `HomeInteriorWorldBuilder`, `HomeBalconyWorldBuilder`, `HomeBathroomBuilder`, `HomeInteriorRoot` | Current |
| Player-home stairwell | A side-aware `8.6 x 9.6 x 6.25 m` scene connecting street lobby, middle landing and apartment door over 48 visible steps and three seam-free ramps, with the flight above the hero blocked, three height-selected fixed shots and a green desaturated grade. | `StairwellLayout*`, `Stairwell{WorldBuilder,DressingBuilder,InteriorRoot,FixedCameraController,InteriorAtmosphere,Soundscape}` | Current |
| City facade presentation | Eight district wall albedos plus a shared roof cap on the one shared `RuntimePrimitiveLit`, tiled by the building's own bay and floor grid rather than by metres so the baked window band lands on the real panes, with a per-lot whole-cell rotation and a linear-space tint compensation that never clamps. Roof caps are tiled by metres; a plinth is not expressible in a repeating sheet and is not built. | `CityFacade{Grid,Appearance}`, `CityWorldBuilder`, `HomeExteriorViewBuilder`, `tools/build-city-facade-textures.py`, `Resources/Textures/CityFacade*` | Current |
| City window presentation | Every facade pane carries a four-variant frame/mullion/glass sheet (plain, curtains, blinds, lamp) picked by a stable hash; lit families share five runtime materials that lerp between unlit day glazing and their warm/cold/bar/home/supermarket glow on the same night factor as the lamp bulbs, in City and the bounded Home exterior alike. Dark panes stay dark glazing around the clock. | `CityWindowAppearance`, `CityExteriorAppearance`, `City{WorldBuilder}`, `HomeExteriorViewBuilder`, `CityNightWorldResult`, `tools/build-city-window-textures.py`, `Resources/Textures/CityWindowAlbedo` | Current |
| Stairwell surface presentation | Eight cached opaque albedos applied through projection-aware deterministic UV scale and MPB values on one shared `RuntimePrimitiveLit`, with linear-albedo-compensated palette tints and no material instances. Projection and hash math live in the shared `SurfaceAppearanceCore`. | `StairwellSurfaceAppearance`, `SurfaceAppearanceCore`, `Stairwell{WorldBuilder,DressingBuilder}`, `Resources/Stairwell/Textures` | Current |
| Home surface presentation | Twelve cached opaque home albedos (wallpaper, plaster, planks, dark wood, laminate, upholstery, linen, tile, enamel, painted metal, concrete, rug) applied metre-tiled through the shared surface core on the one shared `RuntimePrimitiveLit`; builder colours survive as tints under the city-facade linear compensation rule, small props and decal overlays stay flat by an explicit test-enforced exemption list, and the occlusion dither shader samples `_BaseMap` so textures survive visibility fades. | `HomeSurfaceAppearance`, `HomeSurfacePrimitives`, `Home{InteriorWorldBuilder,BathroomBuilder,BalconyWorldBuilder,InteriorDressingBuilder,RefrigeratorWorldBuilder}`, `tools/build-home-textures.py`, `Resources/Home/Textures` | Current |
| Stairwell cat | One rear-view billboard cat on the middle landing rail with a player-tracking head, idle and rare grooming; Talk/Interact opens the shared target menu, and feeding consumes one `OpenStewCan` through synchronized player clips and a cat sprite track. | `StairwellCat{Plan,Actor,SpriteLibrary,LookSelector,IdleModel,Feeding*}`, `StairwellCatInteraction`, `InventoryTargetInteractionController` | Current |
| Home exterior context | Reconstruct a bounded collider-free view of the real home street inside `HomeInterior` from the same blueprint ID and seed, including transformed decoration, a static Route 01 pole and pedestrians enabled only for the Balcony shot. No bus actor is composed, by decision. | `HomeExteriorContext{Plan,Planner}`, `HomeExteriorViewBuilder`, `CityBusStopWorldBuilder`, `CityExteriorAppearance`, `HomeDayNightController` | Current |
| Player home atmosphere | A fog-free post-processed interior on an explicit five-light budget with co-located emitters and halos, now clock-driven end to end: the window passes through a dusk amber between its warm day and blue night poles, the main lamp and hallway rise after dark while ambient and the directional fill sink cold, and the Balcony shot still borrows City's light and hands the indoor mood back on exit. | `HomeInteriorAtmosphere`, `HomeDayNightController`, `HomeBalconyExteriorAtmosphere`, `HomeBathroomLight{Fixture,Flicker}`, `HomeSoundscape` | Current |
| Home bathroom scenes | A rebuilt Soviet shower stall (L-rail, folded curtain group, mixer, hose, tilted head, soap shelf) and three modal scenes on one shared skeleton: guided walk-in (no teleports), Bézier camera from the pinned bathroom shot, debounced stop, idempotent restore. Toilet is a privacy cut to the ajar-door frame with an off-frame flush; the shower draws the curtain over the hero and runs water, steam and a crossfaded hiss loop; teeth brushing shoots from the mirror plane into the hero's face while a procedural CCD right arm works the gripped toothbrush, foam and rinse beats included. Toilet/shower relieve stress on completion, brushing once per game day. | `HomeBathroomSceneInteraction`, `Home{Toilet,Shower,TeethBrushing}Interaction` + timelines, `HomeShowerWaterEffect`, `HomeTeethBrushingArmPose`, `HomeBathroomBuilder`, `HomeSoundscape.SetShowerWaterAmount`, `RetroSfxId.{ToiletFlush,TeethBrushScrub}` | Current |
| Bar patrons | The city pedestrian prefabs reused indoors: pooled 3D guests instanced on the layout NPC anchors (bartender anchor left empty), palette-varied, seated in booths through the shared seat contract or standing at tables, idling through the shared presentation. | `BarPatronWorldBuilder`, `CityPedestrian{Resources,AssetRegistry,Presentation}`, bar layout anchors | Current |
| Bar cinematic atmosphere | Six shadowless practical lights, bar-only post-processing, bounded dust, scene-local spatial cues and a skippable `1.35 s` arrival reveal that restores follow and orbit state before any modal opens. | `BarInteriorAtmosphere`, `BarSoundscape`, `BarArrival{Timeline,Presentation}`, `RuntimeSceneSetup` | Current |
| Automated tests | EditMode and PlayMode coverage over deterministic plans, asset and rig contracts, interaction lifecycles, atomic transactions, audio routing, PS1 presentation and scene round trips, across 147 test files in two assemblies. | `Assets/Tests/{EditMode,PlayMode,Infrastructure}`, `tools/build-player-3d-model.py` validators | Current |
| Automated test audio guard | Silence global listener output for every EditMode and PlayMode run without pausing sources or DSP, then restore the previous volume. | `BarPromenade.TestSupport`, `AutomaticTestAudioMute` | Current |

## Primary intended flow

Structural backbone only. Exact ordering, tuning and edge cases live in
`ai/project-overview.md` and `ai/architecture-notes.md`.

```text
build index 0 -> MainMenu -> BeginNewGame + HomeArrival.OpeningSleep
  -> HomeInterior sleeping opening
     -> 05:59 clock shot -> 5 s input lock -> Wake Up / Quit
     -> Wake -> 06:00 + session clock starts -> alarm hold -> 6 s wake
     -> ordinary Home control -> StairwellInterior -> City
     -> debug F9 -> 06:00 if needed -> direct City at home
                 -> teleport enabled + map opens after transition

session clock -> day/night sample -> City + Home lighting, Home clock display
              -> elapsed minutes -> hunger (1440) + fatigue (1080) -> inventory bars
seed + session clock -> weather slots -> rain intensity -> rain field + rain bed
                     -> storm windows -> lightning flash + delayed thunder

blueprint ID + seed -> immutable blueprint -> validated sparse layout
  -> typed area cells -> surfaces, navigation, city map
  -> four bars / home / supermarket / district public places
  -> street surface plan -> Road v2 carriageway + sidewalks + Road v2.1 aprons
     -> pedestrian graph -> profile-sized player-relative walker population
     -> Route 01 loop -> stops + poles -> one pooled bus actor
        -> board prompt -> seat 07 -> ride -> alight at a later stop
  -> night fixture plan -> lamps, signals, halos
  -> decoration plan -> visuals + collision proxies
  -> fence plan -> rails with clearance openings

five gameplay roots -> PlayerFactory -> Resources/Player/Player3D.prefab
  -> Idle / Walk blend + face + additive status bones
  -> contextual actions: bed, smoking, cat feeding, bus board/ride/exit
  -> failed balance -> Fall clip -> bounded ragdoll -> Rise -> Relaxed
  -> first-person subsets: bar bottles, refrigerator
  -> inventory portrait

player -> interaction -> SceneTransitionService -> DoorTransition
  -> preload destination -> activate at blackout
  -> BarInterior   -> activity-flavoured dressing + seated/standing 3D patrons
                   -> counter station -> physical drink service
  -> SupermarketInterior -> finite shelves -> atomic purchase
  -> StairwellInterior   -> cat -> Talk / feed
  -> HomeInterior        -> bed, balcony smoking, refrigerator, three fixed shots
                         -> bounded same-seed exterior reconstruction

all gameplay results -> GameSessionState -> preserved across Single loads
state boundaries     -> correlated rotating debug.log (NDJSON)
scene root           -> audio mixer snapshot + scene music + ambience
URP post-processing  -> PS1 composite -> crisp retro IMGUI overlay
```
