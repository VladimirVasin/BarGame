# Home toilet action assets

Generated by `tools/build-home-toilet-action-3d-model.py` in Blender 5.0.1.
`HomeToiletAction.blend` preserves the export roots at their authored origins;
`AUTHORING_PREVIEW` contains separate display copies only. The PNG is an
authoring view in real metre proportions: the anatomy and lid keep their
physical sizes, and the liquid meshes use a 3 mm stream diameter, 4 mm drops
and a 28 mm splash. Unit-diameter source meshes are never displayed as liquids.

Regenerate from the repository root:

```powershell
& 'C:/Program Files/Blender Foundation/Blender 5.0/blender.exe' --background --factory-startup --python tools/build-home-toilet-action-3d-model.py -- --preview
```

Validate the current exports without writing assets:

```powershell
& 'C:/Program Files/Blender Foundation/Blender 5.0/blender.exe' --background --factory-startup --python tools/build-home-toilet-action-3d-model.py -- --validate-only
```

Use `--preview-only` instead to update only the source view and `.blend`, without
touching imported Unity FBX files or either model manifest.

The validator checks deterministic geometry, nondegenerate triangles, flat
shading, anatomy anchors, lid rotation, VFX bounds, import settings and every
exported FBX's actual round-trip vertices and anchors. Eighteen mesh rays prove
the pedestal clears the water and incoming stream; 25 seat rays pass through
the entire central opening, and separate rays prove the paper and cardboard
rims exist around a real through-hole. The kit contains eleven models,
thirteen meshes and 2,048 triangles.

All manifest coordinates are Unity local metres, +Y up and +Z forward. Export
uses the existing Home Y/Z source swap with face rewinding and FBX -Z forward,
Y up. The Unity importers preserve hierarchy, apply file units and bake axis
conversion; meshes remain readable for pooled effect deformation.

| Resource suffix under `HomeToiletAction/Models/` | Authored contract |
| --- | --- |
| `Anatomy` | Neutral adult anatomy extends +Z by 0.13 m and curves down. `AimPivot` is `(0,0,0)`, `Grip` is `(0,-0.0015,0.025)`, `Outlet` is `(0,-0.020,0.130)`. The baseward grip leaves the distal shaft visible beyond the actual hero hand. |
| `ToiletLid` | Width 0.54 m, depth 0.51 m, thickness 0.025 m. Hinge origin is at the back; closed mesh extends -Z. Local X +90 degrees raises it into +Y. |
| `BowlWater` | Horizontal XZ oval, 0.34 x 0.314 m, upper surface Y0. Place at `(4.05,0.4373,1.40)` to meet the existing toilet bowl's inner slope. |
| `ToiletPedestal` | Rounded ceramic plinth and hollow upper body in the original 0.82 x 0.48 x 0.858 m footprint. Place at `(4.15,0.24,1.40)`; its upper cavity centres at local X-0.10 to match the actual bowl. The original logical collider may remain. |
| `ToiletSeat` | True 24-sided annular ring, 0.54 x 0.05 x 0.506 m, with a 70% opening and no filled bottom disks. Place at the original seat centre `(4.05,0.62,1.40)`. |
| `ToiletPaperRoll` | Upright Y-axis roll, 0.10 m diameter and 0.095 m height, with layered paper and a 0.028 m open cardboard core. Meshes `ToiletPaper` and `ToiletPaperCore` receive separate shared tints. |
| `StreamSegment` | From Z0 to Z1, XY radius 0.5; runtime fits its endpoints to a travelled liquid segment. |
| `Droplet` | Centred unit diameter, eight-sided longitudinal profile. |
| `Splash` | XY unit envelope, shallow crown toward +Z. |
| `Stain` | Irregular XY unit envelope, thin closed relief toward +Z. |
| `WallStain` | Irregular XY unit envelope and downward -Y drips, thin relief toward +Z. |

Anatomy meshes use the production Hero V2 palette values `Skin = AE8D7B` and
`SkinDark = 392D2E`. Runtime assigns the existing shared hero materials; the
production prefab supplies the hand and arm. The kit owns no replacement hand,
hero rig, animation, sound, text or gameplay state. All other materials are also
shared and assigned by the runtime adapter.
