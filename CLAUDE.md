# Repository instructions

Claude Code loads this file. Codex loads `AGENTS.md`. The rules are the same
rules, so this file imports them rather than restating them — a second copy is
how one fact comes to have two values.

@AGENTS.md
